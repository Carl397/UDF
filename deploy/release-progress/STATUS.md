# UDF production release progress

Last updated: 2026-09-22. Current status: **FULL PRODUCTION DEPLOYMENT COMPLETE: PARTY 1.0.7 (8), MATCHING BACKEND/CRM/WEB/WEBSITE, AND SCHEMA 052 ARE LIVE. REGISTERED-WARD GET/PATCH ROUTES AND SAFE PUBLIC ERRORS DEPLOYED; EXTERNAL APK BYTES VERIFIED. LOCAL SCHEMA ALSO 052.** Mail, president artwork and starter-pack remain live. No update announcement or Git push. Browser and authenticated-device acceptance remain unverified. Current results below supersede historical hold and release-identity statements.

## Full production deployment — completed, 2026-09-22
- **Commit follow-up:** user requested “commit now.” Release source is already committed as `914d647`; this follow-up records deployment/verification history and the build-8 receipt only. Unrelated guide-progress edits, guide/media assets and scratch files are excluded. No Git push or new production change is included.
- User explicitly requested “do the full production deployment,” lifting the previous deployment hold, then approved a scoped local release commit for Party 1.0.7 (8). Scope: corrected backend, additive migration 052, CRM/static web, marketing website and matching signed Party APK/download/catalog. Mail and guide/media work are unchanged; no Git push, Play upload or real update announcement. Incomplete browser/device acceptance remains a limitation, not a passed check.
- Fresh read-only production preflight: SSH succeeds; API, nginx, FPM, postfix, dovecot and opendkim active; API NRestarts=0; 14 GB available; schema count/head 51/051. Party SHA-256 remains `127f9b07b03831e8854e1819f2cb97091e9e5c00c30988634b8cfb77226c52b1`; Mail remains `9d1e77b49fe62db4118bad835055a23b6abb46f16e809d34cb4d216916976cd1`.
- Scoped commit `914d647ebb41abf098c9b4d5c36f590923d50c54` contains 22 release-related files; STATUS history, untracked guides/media and secrets excluded. No Git push. Frozen source release `udf-20260922T024600Z`: backend build and static web export 45/45 pages passed, six existing lint warnings, 784 committed-source/artifact checks passed. Prior 121 application regression groups remain applicable; their code inputs are unchanged.
- Signed Party 1.0.7 (8) APK: **30,124,010 bytes**, SHA-256 `1ec86596abb178bb9469c63c00d9d70f11a88168bf3e204da0827184f23ff6a2`. Independent APK/AAB check passed approved signer, package/version, non-debuggable/API36/backup-disabled, unchanged permissions, production API and map/update/ward/safe-error markers; all 187 web files match export/sync/APK/AAB. Build-7 APK/AAB/mapping/manifest preserved under `.tmp-verify/app-update-history/build-7/`; historical catalogs/archives/receipts unchanged.
- Isolated `UDF_Release_20260921` upgrade **7→8 passed** with `adb install -r`, UID 10214 unchanged, version confirmed and COLD launch Status=ok. SECURE and Party window flags present. No uninstall/data clear/downgrade, account login, production ward change, or real notice/download. This is not authenticated-session or rendered-UI acceptance. Browser still reports hidden 0×0 attachment; no retry/mutation. Local dev server restored at 3000; login and API health return 200.
- Upload release `udf-update-20260922T024953Z`, manifest `.tmp-verify/udf-update-20260922T024953Z/upload.json`, artifacts in its `artifacts/`; manifest SHA-256 `0827bf7c4f0076c22d305cb33ee1a409de4f6f334384c905402fe260b9b8e21e`. Backend archive `81e2cef2a02c027d96782b0d75415b9092eff4f1b27555aed1b60e3e6c32b78a`; web `9438c92d4215a55883c4e6deac08f114521ccd7b3c8112d848c4b41dd325347c`; website `f9d4e48c3de255175f1640ef4af4505f365a0ebe869cc88bb725638263691e84`. Native-source receipt checks working inputs against the same commit. Initial staging lacked JAVA_HOME; rerun with Java 17 succeeded without rebuilding or weakening checks. Failed empty staging directory retained.
- **Deployment completed successfully** as `udf-update-20260922T024953Z`; root-only uploaded manifest and all 9 deliverables verified before cutover. Guarded script ran in a transient systemd service, exited 0, and wrote `/root/udf-releases/udf-update-20260922T024953Z/DEPLOYED`. Runtime dependency install/audit: zero vulnerabilities. Migration 052 committed atomically; no member/account/ward rows or real announcement changed. API, nginx, FPM, postfix, dovecot and opendkim remain active; API NRestarts=0. Final read-only database check confirms 52 migrations ending `052_app_releases.sql`, inactive announcement and highest_version=0.
- **Backups/rollback retained:** `/var/backups/udf/udf-update-20260922T024953Z/` holds database dump (pg_restore listing validated), prior backend/web/website archives, prior Party APK, config and service-state receipts. Previous live directories remain alongside current directories. Uploads and already-open-tab static chunks retained; no rollback needed. Catalog parent/release directories root-owned 0755, catalog 0644 at `/var/lib/udf-app-releases/udf-update-20260922T024953Z/`; service-user access and verified artifact passed.
- **Production acceptance evidence:** external web/source verification **819/819**, plus **516 live deployed files** byte-matched the frozen backend/web/website output. Live Express router import confirms GET/PATCH `/me/ward` before `/:id`; external anonymous GET/PATCH correctly return safe 401, and unmatched API route returns generic 404 without details. No real member ward was changed and no authenticated production ward call was claimed. Production web/home/download pages byte-match, update discovery returns 200/no-store/null.
- **Public Party download verified from outside the server:** canonical build-8 APK and `/downloads/udf.apk` both return HTTPS 200, APK MIME, 30,124,010 bytes and the exact signed SHA-256 above. The counted 1.0.7 link redirects correctly to the live alias; this validation logged exactly one ordinary download event. External Mail download still matches its prior 19,543,248 bytes and digest. Receipt: `.tmp-verify/udf-update-20260922T024953Z/production-verification.json`.
- AAB is local only, 30,729,997 bytes, SHA-256 `fe19dd041875f5ef018e9a4710b8f06764db588d2745d11d51e5b875eb1718cf`, preserved with mapping beside the upload manifest. No Play upload, Mail rebuild, guide/media publication, update broadcast, Git push, or additional commit. Test-started emulator shut down normally after confirming the separate probe remained disabled; upgraded build-8 data retained. Remaining limitations: signed-in browser/map/ward acceptance, authenticated-device persistence, and browser-download/installer UI flow are not verified. Existing 1.0.5 users need the manual compatible APK update; a real update notice remains a separate decision. Unrelated CRM reporting/routing issues below were not fixed or certified by this deployment.

## Registered ward and safe public errors — local correction, 2026-09-22
- User reported `No route for GET /api/members/me/ward` and requested generic, sanitized wording for this class of error. Local GET/PATCH routes already exist before generic member routes. Read-only production router inspection in this session confirmed only `/` and `/:id` routes: the held backend release has not been deployed. Anonymous 401 responses are not proof that a protected route exists. No alternate endpoint, fabricated ward/counter, permission bypass or production mutation was introduced.
- Shared frontend API errors now use fixed status/code-based copy, ignoring server messages, details and status text, including older-server responses. Network/body-read failures are sanitized; cancellation remains recognizable and session refresh behavior is preserved. Invalid JSON/error-shaped success and malformed ward status/save data fail safely before token adoption. Registered ward loading/saving uses contextual copy and preserves authoritative re-fetch after an uncertain save; no optimistic allowance decrement.
- Backend unmatched routes, typed errors, unexpected failures and body/schema validation use safe messages without exception/schema details in every environment. Diagnostic exceptions remain server-side. Stable ward codes retain guidance for stale selection, exhausted allowance, invalid selection and unavailable profile. Legacy validation handlers across patrols, bulletins, transparency, projects, ratings, verifications and participations now use this handler; CRM ward/import and card-batch error text is sanitized. CMS duplicate-section guidance remains useful through a fixed public code.
- Verification: backend ward/error suite **23/23**, frontend ward/error suite **15/15**, backend workflow suite **50/50** (rolled back), client workflow suite **14/14**, update/map suite **19/19**. Both TypeScript checks pass; lint has only the same six pre-existing warnings. Running local API (4000) and frontend proxy (3000) both return the expected generic 404 without details. Language-server diagnostics unavailable. Tests cover authenticated real GET/PATCH, limits/concurrency/rollback, safe legacy 404/500/HTML/network/validation failures, stale input, malformed success, no session mutation on failure, and cross-layer wording parity. No rendered UI or new native acceptance is claimed.
- Test corrections: loading the client and component through different tsx module graphs caused an `instanceof` mismatch; the test now uses one CommonJS graph. A legacy workflow expectation was updated from raw `Offline` to safe connection wording. An empty-object patrol test was valid input and reached an INSERT rejected by its FK (no row committed); validation probes now use invalid top-level arrays and a database-enforced read-only connection for all non-ward routers. Final suites pass; isolated ward fixtures were removed and workflow fixtures rolled back.
- **Release impact:** source changes are uncommitted. Existing build-7 APK/AAB, immutable catalog and `udf-20260921T225337Z` archives are preserved as historical artifacts, not deliverables for this fix. Their prior verification/security result does not cover the new source. Prepare a new matching backend/web/native release and immutable artifact identity before publication; do not reuse the old receipt or overwrite catalog bytes. No APK rebuild, publication, production migration/deployment, announcement, email, Git commit or push occurred for this correction. The explicit **Keep hold; restore browser** decision remains in force. Production functionality requires the matching backend deployment after that hold is lifted.

## Requested local/production alignment and correct update APK — 2026-09-22
- **Latest continuation:** user said “proceed,” then explicitly selected **Keep hold; restore browser** after the unchanged blocker was explained. HEAD remains `d0b6a05`; APK and all three frozen deployment archive hashes still match the values below. One read-only browser attachment check found only the hidden production CRM tab at 0×0; no window/navigation retry, screenshot or session mutation. Local `/login` and API `/healthz` return 200. Registered the existing port-3000 server as **UDF Release Acceptance** preview for the user to open; preview registration does not establish a restored browser attachment or visual acceptance. Await a visible, connected browser before resuming checks. No deployment, production writes, new commit, APK rebuild or repeated passed suite in this continuation.
- User explicitly requested deployment and replacement of the mobile-app download, and approved a scoped local release commit. No Git push, Play upload, Mail change or real update announcement is authorized. Scope is prepared map/ward/update functionality plus its backend/schema and public Party download; unrelated CRM reporting deficiencies remain documented below, not certified correct by this release.
- Production preflight: SSH works; API, nginx, FPM, postfix, dovecot and opendkim active; API NRestarts=0; 14 GB free. Migration set is 51 ending `051_leader_multi_ward.sql`. Live Party hash still `127f9b07b03831e8854e1819f2cb97091e9e5c00c30988634b8cfb77226c52b1`, Mail unchanged at `9d1e77b49fe62db4118bad835055a23b6abb46f16e809d34cb4d216916976cd1`. HTTPS mobile page advertises 1.0.5; production update discovery returns 404 before deployment.
- Ordinary local Party APK output was missing. Native-only `:app:assembleRelease` restored the exact approved 1.0.6 (7) artifact in 15 seconds, SHA-256 `239f430cd406e8529aa4446874969c742507367b365919241490b57b4633b6c5`, 30,122,628 bytes. Independent `verify-apk.py updates` passed approved signing, AAB parity and all 187 exported/synced/packaged web assets; AAB SHA-256 remains `3de78181b1be165fc496a3a2f7234254f5b421aaedb48d1034aa01163cce61f8`. No version/signing/source rebuild or repeat of the passed native 6→7 upgrade.
- Local schema 052 applied atomically with its migration-history row using loopback-only `app-update-local.mjs`; one release-state row, no active announcement, no member changes. Local API health is 200 with database healthy, update discovery is 200 / no-store / null, anonymous admin release and ward requests are 401. Existing dev server remains available at port 3000. Both typechecks and 48-permission capability guard passed; unchanged regression suites retain their prior recorded results.
- Marketing download copy and counted version updated locally to 1.0.6. Prepared `deploy-app-update.sh`: pinned checksums, exact incumbent/migration checks, backup-first rollback-capable web/API/APK cutover, atomic additive migration 052, public-byte verification, service-user catalog validation, no announcement. Catalog must be under root-owned `/var/lib/udf-app-releases/`, not `/opt/udf/` whose parent is API-owned. Shell syntax passed. This script has not been executed on production; upload manifests and runtime dependency audit remain post-acceptance gates.
- Browser acceptance remains blocked: a single new-tab attempt still reported hidden 0×0 viewport and could not render/capture localhost or production. Existing authenticated tab preserved; no account changes or downloads. Responsive map/UI, authenticated relaunch and real-device installer acceptance are not claimed. After the scoped commit, the user selected **Run L3 deep security review** and **Hold for visual acceptance**. That hold overrides the earlier deployment request: do not upload, migrate production or publish the APK until acceptance is completed and deployment is resumed.
- Approved local commit `d0b6a05696eb5bf856c6cfb3fee53e4cb7f2caeb` contains 48 release-related files. Secrets, generated APKs, guides and scratch media are excluded. The requested post-commit L3 review reported no security issues. No Git push or further commit; this final progress update remains local.
- Frozen-source backend build and frontend static export **45/45 pages** passed, with the same six existing lint warnings. Built in an isolated source copy, leaving the local dev server and native export untouched. Release `udf-20260921T225337Z`, artifacts `.tmp-verify/udf-20260921T225337Z/artifacts/`; `release.json` binds source hashes to `d0b6a05`. File/source verification: **784 checks passed, zero failures**. Backend archive SHA-256 `7f7390cd85a6412a785ebad00d2db5ce2e448aace6b979ccdd4f5eaebb386471`; frontend `fb6606cccbe8878e78fb92a6b5400b1c63fa4eb30150cd67edee37323d72996c`; website `a90b79b219cd24cb8e658d17ffccb19324aadc43d96ade33603207ce5748a377`. Language-server diagnostics unavailable; compiler, syntax and artifact checks are the available evidence.
- **No production writes, upload, production migration, APK publication, announcement, email or Play upload in this request.** Next: restore a usable browser and complete visual/native acceptance without downgrading/wiping the already upgraded emulator; honor the user's hold. After deployment is resumed, freeze an upload manifest including the APK/catalog/deploy script, revalidate live incumbent state, deploy under the release lock, then verify external APK bytes/redirects and final service/schema state. Do not repeat unchanged passed build or upgrade checks.

## CRM data-source confirmation — read-only, issues found, 2026-09-21
- User asked whether Dashboard, Analytics, Service Delivery, Cases, Escalations, Verifications, Ratings and Resident Reports are actual and pulling correctly from the app. **Database-backed does not mean all screens are correct. No fixes or deployment were authorized or performed in this diagnostic.** Service Delivery is a navigation group, not an eighth data endpoint.
- Production `udf-api` active. All seven page URLs returned HTTPS 200. Six anonymous data-endpoint probes returned 401, correctly requiring authentication. The Browser diagnostic was rejected/unavailable; no signed-in page or authenticated HTTP-response acceptance is claimed. No credentials, tokens or existing sessions were used or changed. Database diagnostics used read-only connections (`transaction_read_only=on` confirmed); output was aggregate counts and non-sensitive code/contracts only.
- Production snapshot: `service_requests=0`, `resident_reports=11`, legacy `ratings=0`, `verifications=0`, `councillor_scorecards=1`. These are stored production counts, not a claim that each record's real-world provenance was validated. Deployed CRM service reads returned Cases total/items 0/0 and Escalations 0/0. National-scope dashboard service totals: active member records 34, open cases 0, open petitions 0, open participations 1. Its separately permission-gated activity queries returned reports 11 and cases 0. No authenticated principal/session was minted for these direct service diagnostics.
- Cases/Analytics/Escalations read `service_requests`; the app's Report to my ward councillor writes `resident_reports`, which is a separate workflow. The 11 reports therefore do not become 11 Cases. Dashboard report activity does read them separately. Analytics reads only the first 1,000 cases and silently substitutes zeros/empty arrays on request failure; Cases and Escalations likewise mask failed fetches as empty lists. Analytics also differs from Dashboard in duplicate/merged-case handling. These cannot be certified as complete, reliable all-app metrics.
- **Ratings and Verifications deployed request mismatch confirmed:** both live page bundles request `limit=200`; executing each deployed query schema rejects 200 and accepts 100. Their route validation would return 400 after authentication; the pages catch errors and display empty results. No authenticated 400 was directly observed in this check.
- **Deployed field contracts also mismatch:** Ratings renders `score`, while the API SELECT returns `rating`; Verifications renders `verifierMemberId/result/notes`, while the API returns `memberId/verdict/note` with verdicts `fixed/not_fixed/partial`. Fixing only the request limit would not fix the displays. Both pages also calculate totals from the loaded list instead of the API total.
- **Monthly councillor scorecard is not wired into CRM Ratings:** the app submits `/scorecards` into `councillor_scorecards`, while CRM Ratings reads `/ratings`. There is one stored monthly scorecard and zero legacy ratings. Do not interpret that page's empty state as no monthly app feedback.
- Resident Reports is wired to the same `/transparency/reports` data as the app and uses server-side filtered totals/pagination. Production has 9 open reports plus 1 resolved and 1 closed. All 11 have no stored councillor assignment, despite active multi-ward councillor coverage for their wards; 7 also have a primary-ward match. No assignment was backfilled. The cause of every historical null assignment was not established.
- Additional deployed multi-ward gap confirmed: resident-report creation still matches only `users.ward_code`; `wardCodeScope` supplied two `wardCodes` returns a predicate for the primary ward only. Secondary-ward reports can therefore miss routing/visibility in these CRM paths. Local report-access/management code likewise uses primary-ward-only staff scope. The legacy Ratings/Verifications list services also lack caller-territory filtering; current tables are empty, so this check did not demonstrate an existing cross-territory disclosure.
- Evidence: terminal results from production schema/service/aggregate checks and external HTTPS bundle inspection in this session; live page chunks `ratings/page-2efac5bee272d379.js`, `verifications/page-d0405f5f03f5f806.js`, `analytics/page-dbde1c48c51ab1bc.js`. Relevant sources: `frontend/src/app/crm/{analytics,engagements,escalations,ratings,verifications,resident-reports}/page.tsx`, `frontend/src/lib/api.ts`, `backend/src/modules/{crm,ratings,verifications,scorecards,transparency}/`, and `backend/src/auth/scope.ts`. No new evidence file containing records or PII was created.
- Next gate: user authorization for remediation; align request limits and DTOs, decide how monthly scorecards and resident reports should be represented, make errors explicit, use complete aggregates, and repair multi-ward routing/scope. Then verify authorized signed-in reads. No source fix, production write, report/rating submission, account creation, email, commit or push in this diagnostic. Member-guide work remains separately blocked below.

## Mobile map/update plan and registered-ward limit — local work, 2026-09-21
- Latest addition: a member may change their registered ward at most **3 times in total**, with a visible remaining-change indicator. Implemented in More → Settings → Registered ward, with a Map shortcut, explicit review/confirmation, server-owned remaining count, and a limit-reached state. Browsing/selecting areas or using GPS does not consume changes. First assignment when no ward exists is free; changing back consumes another change. Staff territory assignment remains an administrative operation, not member self-service.
- Backend `members/wardChanges.ts` uses the existing migration-006 counter/history (no new ward migration), canonical `users.member_id`, row locks, compare-and-set expected ward, strict input, and an atomic counter/history/ward/account-scope transaction. Existing counts/history are not reset by election term or session. Invalid/duplicate/stale/concurrent requests cannot spend extra allowances; a member with an explicit generic write grant cannot use CRUD to bypass the cap. Old member location/district and ward tags are cleared/replaced on a real move to avoid stale geography. No contact/consent/status changes, OTP claim, or email is created.
- Authenticated requests now load live role/ward/region scope so old access tokens do not retain the previous ward's restricted access. The client adopts the new ward access token while retaining the refresh session and preferences, including a 401/refresh race; delayed responses do not restore logged-out or different-user sessions.
- Verification: `npm run test:ward-changes --workspace backend` **17/17** (isolated, empty copies of the local schema; includes real concurrent transactions and rollback on audit failure), `node --import tsx frontend/scripts/ward-change-client.mjs` **7/7** (mocked network/storage only), workflow regressions **50/50** (rolled back), analytics **15/15**. Both TypeScript checks and capability drift guard passed; lint passed with the six unchanged pre-existing warnings. `git diff --check` passed. Language-server diagnostics unavailable. Browser tab creation failed; no screenshot or interactive/device acceptance is claimed.
- Continuation implemented the opt-in map footer in both mobile map tabs: fixed 56px footer outside the gesture/scroll surface, View data below / Back to map, measured available-height sizing, visible scrollbar, resize/content/scroll observation, keyboard and reduced-motion support. Desktop opt-out retains the original wrapper. Responsive/interactive acceptance remains unverified: latest single Browser retry could not create a 390×844 local tab and reported a hidden 0×0 viewport; existing production tab was not repurposed.
- Implemented trusted operator-owned artifact catalog, migration `052_app_releases.sql`, Superadmin-only effective `app_release:manage`, transactional/idempotent publication and withdrawal, immutable artifact/audit rows, safe anonymous no-store update metadata and counted immutable download redirects. Backend release regressions **14/14** passed in isolated empty scratch schemas, including concurrency and audit rollback; migration 052 was not applied to the normal application schema or production. Operator tool `deploy/prepare-app-release.py` verifies package, approved signer, non-debuggable status, increasing build and exact bytes; public verification remains a separate post-publication gate.
- Implemented CRM App updates selection/notes/preview/confirmation/history, Android-only root provider (including pre-login), actual native build/version, startup/resume/reconnect/60-second checks, cancellation/timeout, per-release 24-hour deferral, manual Settings check, stale-notice revalidation and external-browser handoff. Capacitor App 6.0.3 and App Launcher 6.0.4 installed. Existing modal/splash coordination, focus/Escape/Back handling and error states are implemented; focused native runtime results are below, while full visual/modal-overlap acceptance remains pending. Old 1.0.5 APKs lack this provider and require a one-time manual compatible upgrade.
- Latest update/map regressions **19/19** pass; the earlier 18/19 result was a verified tsx CommonJS default-import mismatch in the test, corrected without changing the production component. Both typechecks, 48-permission capability guard and `git diff --check` pass; analytics **15/15** now observed, lint has only six unchanged warnings. Prior workflow **50/50**, release **14/14**, and ward **24/24** passing runs remain applicable; unchanged suites were not repeated. Policy mocks/static markup do not establish browser rendering or native bridge acceptance.
- Preserved the approved incumbent APK under `.tmp-verify/app-update-baseline/udf-6-127f9b07b03831e8854e1819f2cb97091e9e5c00c30988634b8cfb77226c52b1.apk`; SHA-256 rechecked, Party 1.0.5 (6), 30,106,658 bytes, approved signer. The approved plan file remains unchanged.
- **Signed Party 1.0.6 (7) APK/AAB built locally** with Java 17, NODE_ENV unset and absolute production HTTPS API. Next export succeeded; Gradle completed in 4m43s. APK `frontend/android/app/build/outputs/apk/release/app-release.apk`: **30,122,628 bytes**, SHA-256 `239f430cd406e8529aa4446874969c742507367b365919241490b57b4633b6c5`. AAB `frontend/android/app/build/outputs/bundle/release/app-release.aab`: **30,728,569 bytes**, SHA-256 `3de78181b1be165fc496a3a2f7234254f5b421aaedb48d1034aa01163cce61f8`. The known local frontend dev process was paused to avoid shared `.next` writes, then restored on port 3000; `/login` returned 200.
- Added independent `verify-apk.py updates` mode while preserving historical Party and Mail modes. PASS: approved APK/AAB signer, APK v2 signature, non-debuggable, API 36, backup disabled, exact incumbent permission set, zip alignment, bundletool validation/manifest, R8 mapping, no packaged native libraries/secret-like filenames, production API, native App/AppLauncher/Preferences registration, update/map/ward markers and tile-provider screening. All **187 web assets** byte-match export, Capacitor sync, APK and AAB. Static receipt: `deploy/release-progress/app-update-build-7-239f430cd406e8529aa4446874969c742507367b365919241490b57b4633b6c5.json`; its device-test flag describes that static-only verification, not the later runtime result below.
- Prepared an **inactive/unpublished** immutable catalog at `.tmp-verify/app-update-catalog/catalog.json`, baseline build 6, new build 7, `publicVerified=false`; canonical APK copy is beside it. No public-byte verification or notice activation was attempted. This user-owned local preparation is not a deployed root-owned API catalog.
- **Isolated in-place upgrade passed:** `UDF_Release_20260921` initially contained Party 1.0.5 (6). `verify-upgrade.py --execute` installed the preserved approved baseline with `-r`, stored exactly one synthetic Capacitor preference, upgraded to the verified build 7 with `-r`, and confirmed unchanged package UID plus retained preference. No uninstall, app-data clear, AVD wipe, debug-security change, login or account mutation. A separate matching-signer instrumentation APK was used only in this isolated emulator, not added to the Party release. Native bridge `App.getInfo()` returned com.udf.party / 1.0.6 / 7; `Preferences.get()` returned the synthetic value; `AppLauncher.openUrl()` returned completed=true and Android foreground switched to Chrome's first-run activity. The URL was the public mobile information page, not an APK download: actual download/Android installer completion and authenticated-session persistence remain unverified. Evidence: `.tmp-verify/app-update-upgrade/receipt.json`.
- **Synthetic native popup passed in the signed build:** the test intercepted only anonymous update discovery inside the isolated WebView process and returned a clearly labeled fictitious release. One check produced the real modal with Download update / Later, matching notes and focus inside. Android Back dismissed it and the actual device preferences stored a 24-hour release deferral. No Download click, real announcement or API/database mutation. Evidence: `.tmp-verify/app-update-upgrade/prompt-receipt.json`. Fetch interception ended with that test process; normal cold launch subsequently returned Status=ok / COLD, and SECURE window flag remained present. The separate probe package was confirmed disabled; the test-started emulator was shut down normally without wiping. Synthetic preference tests do not prove authenticated-session retention.
- Final `git diff --check` and verifier help/syntax checks passed; language-server diagnostics remain unavailable. Party output SHA-256 still matches the receipt. A final out-of-scope cross-check found the ordinary local Mail APK output path absent (no APK under `mailapp/android/app/build/outputs`); it was not rebuilt or replaced. This does not change the previously published Mail identity and does not establish the state of its live download.
- Remaining gates: restore a usable browser viewport for member/staff map responsive/gesture/GPS acceptance and Superadmin selection/preview/withdraw interaction; complete real artifact download/Android installer acceptance when publication is authorized. The first update-enabled build still needs one manual compatible install for existing 1.0.5 users. Local code, signed artifacts, upgrade persistence and focused native popup tests are complete; do not repeat the successful 6→7 upgrade by downgrading/wiping the now-updated AVD.
- Production unchanged for this task: no deployment, production migration, real member ward change, notice broadcast, APK publication, Git commit/push or Play upload. Existing published Party/Mail receipts remain the actual live identity. No Mail rebuild/change. Deployment of migration/API/catalog, APK publication and a real notice each remain subject to separate authorization; no automatic rollout on a generic continuation.

## Member guide — actual-screen correction pending, 2026-09-22
- **2026-09-22 continuation of “complete the guide and videos” / “proceed”:** localhost returned HTTP 200 at `/`. One read-only browser attachment check still exposed only `https://crm.udf-party.co.za/`, with hidden document and inner/outer/visual/document viewports all 0×0; no localhost tab was available. No new-window retry, navigation, authentication, screenshot, or production/session mutation. The user's earlier visible-preview confirmation remains valid; the capture attachment is the blocker. The existing 3-second encoder receipt was re-read, not regenerated; it remains only a technical smoke test. The guide output folder still contains the rejected draft HTML/PDF/preview/transcript/VTT and no final MP4 or ZIP. No guide source or artifact changed in this continuation. User selected **Upload screenshots** to unblock capture; awaiting actual attachments. Requested current member-view sign-in, registration, Home, Map, rating, Report, Events, Invite and Settings screens with personal details hidden and no credentials/codes. Once received, validate coverage, replace mockups, render full videos, verify and package. All deliverables remain local and unpublished.
- **Latest scope supersedes the earlier production-capture selection below:** user requested screenshots from `http://localhost:3000/`, updated guides and proper videos. Keep all deliverables local and unpublished. Current ordinary HTTP probe returns 200; this does not prove rendered or authenticated UI correctness. Do not reuse production test credentials on localhost without verifying the local environment and authorization.
- **Capture still blocked on continuation:** initial separate-window attempt at 390×844 failed. The `Local UDF Member App` preview was registered and the user explicitly confirmed **Preview is visible**. A subsequent browser-tool check still exposed only the production CRM tab, not localhost; one new-window attempt after that changed condition returned `window.open returned null`, document visibility `hidden`, viewport/window 0×0. This is a tool/preview attachment mismatch, not a missing user confirmation or evidence that localhost is unavailable. Production tab/session preserved. No localhost images saved, local member session unverified, and no forms/account/report/rating/RSVP/consent changes. Next capture requires a restored browser attachment or user-supplied screenshots; do not loop unchanged retries.
- **Prior compiler blocker resolved:** `build_guide.py --compile-video` uses a process-local Swift/Clang VFS overlay only when both installed module maps contain the known duplicate SwiftBridging definition; installed SDK/toolchain files remain unchanged. `render_video.swift` now isolates typed drawing calculations and uses `rotate(by:)`. Working executable: `.tmp-verify/member-guide/render-video`.
- **Isolated encoder smoke test passed:** `.tmp-verify/member-guide/encoder-smoke/encoder-test.mp4` is 1080×1920, 24 fps, H.264, silent, 3 seconds, 72 decoded frames, 24,763 bytes. Receipt `encoder-smoke/video-verification.json` re-read on continuation; SHA-256 `26a5f004bdf3e1e775a5cfcc0da2e11dd27bbd7bfcadb3f158e8f3165536e3b6`. Extracted preview was visually checked upright. This neutral technical test is NOT guide content or a completed walkthrough. Compilation/smoke test were not repeated on continuation.
- **Remaining:** actual localhost screenshots, screenshot-based HTML/PDF/storyboard, full captioned video/transcript/VTT, visual/playback/package checks and final ZIP. Existing drafts still contain rejected mockups; no corrected full video or ZIP is ready. Capture labels must distinguish localhost build from production/native Android. Next gate is a visible capture preview (or user-supplied genuine current screenshots), not more test-member provisioning. No deployment, publication, email, commit or push.
- Historical attempts below are retained; their production-source and compiler-blocker statements are superseded by the latest entries above.
- User rejected the guide images because they do not reflect the actual app. The draft HTML/PDF/storyboard in `resources/marketing/member-guide/` use constructed phone mockups; they are not approved final member-guide visuals. Replace them with verified current screen captures, with sample data clearly identified where applicable. Do not substitute historical admin screens for the member experience.
- Continuation checked the template: the closing style tag is intact; the previously rejected malformed edit did not appear in the file. Existing guide sources and generated drafts remain unchanged during this correction attempt.
- Browser capture is BLOCKED: new-tab creation failed; an existing tab could load the local empty sign-in page and public mobile download page, but the browser view was hidden/detached with a 0x0 viewport and screenshot capture failed. No screenshots were saved. Existing production URL restored; no sign-in, forms, downloads, account changes or production writes. Saved historical screenshots inspected also differ from current source and are unsuitable as current member-guide evidence.
- Previous video build is not complete: terminal output confirms Swift framework-module compilation failed; no successful encoding or video verification receipt was established. Do not claim the MP4 or ZIP is ready.
- User clarified the next source: **capture from the live app**, not localhost; keep the guide unpublished. Retried at `https://crm.udf-party.co.za/` and left that page open. Browser still reports a hidden 0x0 viewport; no images saved and member session/role unverified. This is a capture-view blocker, not evidence that the live app is unavailable.
- User then explicitly authorized creating a test member and completing the guide. Created exactly one bound synthetic member/user in production after a zero-duplicate dry run, using existing encryption/password helpers and a single database transaction. Tagged `member-guide-test-20260921`, role `member`, membership status `pending`, CPT-W079 / CPT-SC17, heat weight 0, synthetic ward representative point (not a residence), no public verification code or membership number, no permission overrides, no contact consents or terms acceptance. Adds one pending test record to totals, not an active membership. No other accounts changed.
- Live test login returned HTTP 200; role, own-ward scope and absence of member-directory/PII permissions verified. The test verifier initially looked for permissions in the JWT; corrected it to use the actual login response, then passed. Both diagnostic sessions were logged out through the normal API. No emails, OTPs, reports, ratings, RSVPs, invitations or consent acceptance. Credentials generated randomly and retained only in mode-0600 private files outside guide artifacts; local copy is Git-ignored. Do not copy credentials into this record or public materials.
- Evidence: `.tmp-verify/member-guide/provision-live-test.mjs` (read-only by default, refuses duplicate apply) and `test-member-verification.json`. Existing live browser tab is an authenticated dashboard, but still hidden with window/document dimensions 0x0; no screenshots saved. Creating the test account did not resolve that capture limitation. Guide remains unchanged and unpublished; video compilation blocker remains.
- Next gate: restore a visible live browser; use the newly created test member without replacing or changing any existing account. Normal first-sign-in terms acceptance remains required. Capture the actual screens, replace mockups across all formats, regenerate and verify the guide/PDF/video/package, and secure the temporary test account after capture. Do not rerun provisioning or create duplicates. No deployment, email, commit or push in this guide task.

## Requested updated Mail APK rebuild — after president publication, 2026-09-21
- User requested “create the updated mail apk now.” Completed the Mail `apk:release` pipeline with Java 17 and NODE_ENV unset: Capacitor sync, branded native overrides and Gradle release build succeeded in 19 seconds. No native source changes were required; this shell loads the current production webmail from `https://mail.udf-party.co.za`, so server-side branding/session/email updates are not bundled into a new native version.
- Verified local output: `mailapp/android/app/build/outputs/apk/release/app-release.apk`; com.udf.mail, version 1.0.0 (1), 19,543,248 bytes, SHA-256 `9d1e77b49fe62db4118bad835055a23b6abb46f16e809d34cb4d216916976cd1`. Gradle reused the valid packaged output for unchanged inputs; byte-identical to the earlier Mail artifact, not a new feature/version release. Approved signing identity retained.
- Added independent `mail` mode to `verify-apk.py`; it avoids unrelated Party/web/debug artifact requirements. Executed successfully: APK v2 signature and approved certificate, non-debuggable, target API 36, disabled backup, cleartext prohibited, exact webmail URL/navigation policy, permission allowlist, ZIP integrity, zip alignment, one fallback web asset identical to source/synced/APK, R8 mapping present and secret-like packaged filename check. `git diff --check` passed.
- Build-only handoff: no production publication, server/account changes, mail sending, device installation, authenticated relaunch test, Play upload, Git commit or push. Existing live releases and historical runtime limitations remain unchanged.

## President artwork, starter-pack PDF and refreshed Party download — 2026-09-21
- User requested the newly built Party APK on `https://udf-party.co.za/mobile`, the supplied president image on that page and in starter-pack email/PDF. Scope is future starter packs only: no resend, member/account mutation, Mail APK change or Play upload.
- Implemented: the complete existing 1024×1536 president poster on the apex mobile page; release 1.0.5 / 30.1 MB and version-tagged counted APK endpoint; inline CID president artwork and a downloadable one-page A4 poster PDF in starter-pack emails. PDF contains the original JPEG, not personalized member data or OTP. Existing councillor/leader sections, OTP behavior and removed manifesto hyperlink remain intact. Corrected email table nesting. MIME now supports mixed → related → alternative plus inline JPEG and downloadable PDF.
- Local verification passed: backend TypeScript build and eight starter-pack regression checks, including independent Python MIME parsing, escaping, OTP-only behavior and councillor/vacancy wording. Synthetic unsent previews: `.tmp-verify/president-starter-pack/`. PDF independently rendered with PDFium and visually checked: complete, upright, uncropped poster with white A4 margins; evidence `.tmp-verify/president-starter-pack/udf-president_1.jpg`. The system Python 3.9 matches the installed PDF preview libraries; Homebrew Python 3.13 could not load their Pillow extension.
- Browser verification BLOCKED: Browser agent could not open preview tabs (window creation blocked/navigation timeout). No website/email responsive measurements or browser screenshots obtained; do not claim visual browser acceptance. No mail was sent by tests.
- Continuation found the Party APK output absent. Native `:app:assembleRelease` restored it in 12 seconds from unchanged inputs; its 30,106,658 bytes and SHA-256 `127f9b07b03831e8854e1819f2cb97091e9e5c00c30988634b8cfb77226c52b1` exactly match the already verified rebuild below. No Next rebuild, version/signing change or dev-server interruption was needed.
- Production preflight passed: API/nginx/mail active, API working directory `/opt/udf/backend`, 14 GB available. Frozen ten-file manifest: `.tmp-verify/udf-president-20260921-8d2a5868/manifest.json`. Uploaded to `/root/udf-releases/udf-president-20260921-8d2a5868/`; guarded cutover completed with `DEPLOYED` marker (0600) and systemd unit `udf-president-8d2a5868` Result=success / ExecMainStatus=0. Incumbent hashes checked under the shared release lock; scoped backups retained at `/var/backups/udf/udf-president-20260921-8d2a5868/` (0700), including the previous Party APK and file ownership/mode metadata. No dependencies, migrations, nginx/mail config or CRM export changes. No commit or push.
- Post-cutover permission check caught a release-umask issue: the new `dist/assets` directory was root-owned 0700, so the initial root-only MIME check did not establish API-user access. Corrected only that public-artwork directory to 0755, confirmed both 0644 files readable by `udf`, and reran the deployed-code/read-only-database/MIME check as the actual `udf` service user: PASS, 792,643-byte synthetic MIME, correct inline CID and downloadable filename. No email sent or OTP issued; no member data changed. Local deployment helper now explicitly sets new-directory traversal permissions and verifies under `udf`; the original executed staging script remains preserved as historical evidence. No API restart was needed for the permission correction because attachments are read per email.
- Production JPEG SHA-256 `925201bc3c5f5d656b928ebd62825a14ce7c12f1d29a6841317dcda3857c46a6`; one-page PDF SHA-256 `08cf57a7820edaef9d1140d9abab1ca321380e5e12efd7c52dec7003ee49b908` (285,500 bytes). MIME decoding independently confirmed exact bytes. Existing uncontested-ward wording, OTP-only resend and absence of manifesto hyperlinks verified against deployed code and read-only production data.
- External HTTPS verification passed: `/mobile`, versioned CSS and poster match frozen local bytes; all three download buttons use `?v=1.0.5`; real APK is 30,106,658 bytes, SHA-256 `127f9b07b03831e8854e1819f2cb97091e9e5c00c30988634b8cfb77226c52b1`, Android package content type, attachment disposition and no-cache. Counted endpoint returns 302 to the verified APK; that check intentionally records one download event. Evidence: `.tmp-verify/udf-president-20260921-8d2a5868/external-http-checks.json`; server `http-checks.json` and `starter-pack-check.log` in staging (the latter is the initial root check; the subsequent service-user PASS is recorded above).
- Final health: udf-api, nginx, php8.1-fpm, postfix, dovecot and opendkim active; API NRestarts=0; no API error-priority journal entries since cutover. Mail APK unchanged at its existing hash. Requested publication complete. Remaining limits: browser visual/responsive acceptance and real recipient email-client rendering are unverified; no existing members were re-emailed, no native device upgrade test or Play upload performed. Do not redeploy or resend on a generic continuation.

## Requested local rebuild of both release APKs — 2026-09-21
- User requested the full UDF Party release APK and UDF Party Mail APK build. Both `apk:release` pipelines completed successfully using Java 17 with `NODE_ENV` unset. Application source is unchanged from the existing release; retained Party 1.0.5 (6), Mail 1.0.0 (1), both application IDs and the approved upload key. HEAD is `825ad2b`; this request made no commit, push, production deployment or Play upload.
- Pre-build local APK hashes matched the published release receipts below. Fresh Party build: `frontend/android/app/build/outputs/apk/release/app-release.apk`, 30,106,658 bytes, SHA-256 `127f9b07b03831e8854e1819f2cb97091e9e5c00c30988634b8cfb77226c52b1`. This is a local rebuild, not the previously published Party binary.
- Mail build: `mailapp/android/app/build/outputs/apk/release/app-release.apk`, 19,543,248 bytes, SHA-256 `9d1e77b49fe62db4118bad835055a23b6abb46f16e809d34cb4d216916976cd1`. Unchanged native inputs allowed Gradle to reuse the existing valid artifact; it remains byte-identical to the published Mail receipt.
- Added the read-only `apks` mode to `verify-apk.py` for APK-only builds, independent of the web deployment archives. `python3 deploy/release-progress/verify-apk.py apks` passed both identities, approved v2 signatures, non-debuggable builds, target API 36, disabled backup, zip alignment, archive integrity, secret-like filename screening, expected production endpoints, Mail permission allowlist, Party feature markers and no legacy/local API target. All 185 Party export files and the Mail fallback file byte-match both synced assets and their APK entries. Both R8 mappings exist; the prior debug APK is preserved. Existing web/archive verification mode remains unchanged.
- Party pipeline also built and bundletool-validated `frontend/android/app/build/outputs/bundle/release/app-release.aab`, SHA-256 `efd398981b7de6769c74b4eecac0249affd17ccecb2e30c47d29a8abace6dff0`. JAR signature verification passed with no unsigned entries, certificate matches the approved upload key, and the dumped manifest is com.udf.party 1.0.5 (6), target API 36. No AAB publication.
- Local frontend dev server was paused to prevent shared `.next` writes, then restored on port 3000; `/login` returned HTTP 200. The current `frontend/out` is the APK-targeted production-HTTPS export, not the `/api` web export represented by the earlier deployment archive; do not compare those distinct exports as identical inputs.
- Verification limits remain: no new device installation, authenticated native relaunch or visual acceptance in this request. Existing production downloads and services were not changed or rechecked. Prior deployment/runtime evidence and unresolved concerns below remain historical facts, not new acceptance claims. `git diff --check` passed; language-server diagnostics were unavailable, but the Python verifier executed successfully.

## Full website, CRM, webmail and Android release — production handoff before local rebuild
- Scope authorized: full production deployment of website/CRM/backend, mail.udf-party.co.za branding and session policy, Party APK and Mail APK direct downloads. User explicitly approved a local release commit after validation. No Git push or Play upload is authorized.
- Initial production preflight: authenticated SSH succeeded; udf-api, nginx, php8.1-fpm, postfix, dovecot and opendkim active; API NRestarts=0; 14 GB free. Migration head 051 and Roundcube 1.6.19 verified. Subsequent deployment outcomes are recorded below.
- Build completed: backend TypeScript, 51 bundled SQL migrations, frontend static export 45/45 pages with six existing lint warnings; Mail Gradle release build successful. Web export uses `/api`; Party bundles the production HTTPS API. No Next server is part of the production release.
- Verified artifact hashes (SHA-256), before upload:
  - Party `frontend/android/app/build/outputs/apk/release/app-release.apk`: `dca3e1fb78d8722675751f08c401bb898b8d04915231c0c6a7fe6c9abdb070e1`, 30,106,695 bytes, com.udf.party 1.0.5 (6).
  - Mail `mailapp/android/app/build/outputs/apk/release/app-release.apk`: `9d1e77b49fe62db4118bad835055a23b6abb46f16e809d34cb4d216916976cd1`, 19,543,248 bytes, com.udf.mail 1.0.0 (1), display name Mail - UDF Party (Secure).
  - Backend `deploy/.artifacts/udf-backend.tar.gz`: `4b0cbd7872e45f97d3e55c8904e5ab5b916f9c80f0112da34d1f232f82317636`.
  - Frontend `deploy/.artifacts/udf-frontend.tar.gz`: `40d07c56b2b51f97a11d1eb8eec20e4e3cb387d4b79b48006a165d15e4db6e11`.
- `python3 deploy/release-progress/verify-apk.py current` passed: both approved upload signatures, non-debuggable, target API 36, backup disabled, zip alignment, expected app identities/configuration; Party release content markers present; frontend archive byte-matches export. Mail requests INTERNET plus the AndroidX signature-level receiver permission, not camera/microphone/location/storage.
- Mail installed and cold-launched on the existing Android 16 emulator; window dumpsys confirms SECURE and visible/drawn, with no AndroidRuntime/Chromium/Capacitor error output. This is not proof of authenticated mail or 30-day persistence. Party install on that emulator was refused due to its existing debug-signed 1.0.1 installation; that app/data is preserved. A separate clean Android 16 release emulator now installs Party 1.0.5 successfully; the initial activity wait timed out, but a subsequent start returned OK and dumpsys confirmed a visible/drawn SECURE window. Chromium reported first-run cache/tile-memory warnings; no AndroidRuntime crash was reported. UIAutomator did not provide a usable content tree, so rendered screen content and authenticated device workflows remain unverified.
- Mail policy replaced the inadequate login-only deletion design: atomic one-row-per-user UPSERT, hash of regenerated session ID, authorization checked before every authenticated request, absolute 30-day expiry, fail-closed database handling, and logout that cannot revoke another client's winning row. A response-header callback persists BOTH Roundcube cookies to the fixed deadline; native CookieManager flushes on pause and third-party cookies are disabled.
- `php deploy/webmail/single-session/test.php`: 26 checks passed, including revocation, expiry, storage failures, simulated overlapping logins, consistent SQLite backup, unchanged active config, preserved existing plugins, owner-only backup/candidate permissions, byte-idempotent config and refusal of unsupported cookie domains. This does NOT replace real Roundcube HTTP/cookie/IMAP or truly parallel-client tests.
- Dependency assessment: initial root audit flagged Express/qs, Next and PostCSS. Backend standalone artifact already resolved Express 4.22.3 and qs 6.16.0; local root lock/dependencies now match those patches. Backend production-dependency audit reports zero vulnerabilities; updated-dependency regression tests pass 50/50 and analytics 15/15. Next 14.2.35/PostCSS 8.4.31 advisories remain in the build toolchain. Critical Next findings target Windows-hosted Next servers or the image optimizer, neither deployed here (Linux nginx static export, unoptimized images). No beforeInteractive/nonce/server-action source was found. PostCSS consumes repository CSS, not user-uploaded CSS, in this release build. This is an applicability assessment, not a blanket security-clean claim or framework upgrade.
- Credential hygiene: removed a plaintext historical CRM credential from this record before staging. Its previous presence means the shared credential must be treated as exposed; no account password was changed by this cleanup. Secrets, generated Android projects, uploads and scratch media must stay out of the release commit.
- Source handoff: user-approved local commit `77fb391` created (163 files); no Git push. Scratch PDF/video work and generated artifacts are excluded. User selected L3 deep security review after the commit; it completed with no security issues reported. Known build-toolchain dependency advisories above remain separate from that result. Release staging directory `/root/udf-releases/udf-20260921-77fb391` created mode 0700; uploads completed and web cutover succeeded. This original review does not cover the later mail integration fixes below.
- Prepared `deploy/release-progress/deploy-full-release.sh` (bash syntax passed): unique release paths and lock, pinned artifact hashes, native runtime install/audit and exact migration-state preflight before downtime, database/config/application backups, upload preservation, rollback-capable backend/CRM/website cutover and public byte/health checks. It does not deploy mail, publish APKs, apply migrations or seed accounts; those remain separate ordered operations.
- Web deployment completed: `/root/udf-releases/udf-20260921-77fb391/WEB_DEPLOYED`; verified backups at `/var/backups/udf/udf-20260921-77fb391`. Runtime audit zero vulnerabilities; exact migration set 051 confirmed without migrations. Public CRM, wards, homepage and mobile landing byte checks passed; API active with NRestarts=0. Existing backend uploads and previous content-addressed assets retained; mail/nginx services remained active. Harmless Apple provenance archive-header warnings occurred during extraction.
- Councillor links completed after guarded dry run: 25 accounts, 116 ward slots, 25 users and public leaders linked. Account set/password hashes unchanged; no fabricated consents/email/public IDs. Root-only dry/apply logs are in the release staging directory.
- Branding deployed and public logo/favicon/styles/brand-strip checks passed; full previous webmail tree and FPM config backed up as `webmail-before.tar.gz` under the release backup. Signed-in visual acceptance remains pending.
- Resolved session-policy blocker during release: initial activation passed its installer checks, but real HTTPS/IMAP login failed. Roundcube 1.6.19 exposes `get_dbh()`, not the plugin/test stub's invented `get_db_instance()`. Fail-closed handling rejected the login. Immediately restored the branded pre-policy config from `/root/backups/premailsession-20260921T163620Z-fCK4Mq/config.inc.php.bak` and gracefully reloaded FPM; all services remain active. At that rollback the policy plugin/table were retained inactive; corrected activation subsequently passed as recorded below. A second integration defect was found on the isolated instance: Roundcube `fetch_array()` is numeric, not associative; the plugin now uses `get_dbh()->fetch_assoc()` and the stub matches those real contracts.
- User approved an isolated temporary test mailbox. First test failed as above; its authentication was disabled and authorization revoked in cleanup; original mailbox records unchanged. No existing mailbox content accessed or messages sent. Root-only failure receipt: release staging `mail-acceptance-release-check-9a6e69f3dd4e.json`. Browser automation unavailable, so no live visual/native persistence claim.
- Isolated real integration completed: loopback-only nginx TLS port 18443 against a separate Roundcube tree/database, using the production FPM/IMAP runtime. No public policy reactivation during tests. The corrected plugin and branding passed 22 checks: first login, both host-only Secure/HttpOnly/SameSite=Lax persistent cookies with identical fixed 30-day deadlines, signed-in mailbox/settings logo and stylesheet references, fresh HTTP-client cookie reuse, non-sliding expiry, second-client revocation, loser cleanup safety, logout replay denial, forced expiry and genuinely concurrent logins with exactly one winner. Native app relaunch and visual rendering are NOT proven. Success receipt: release staging `mail-acceptance-release-check-9a2e1b663499.json`; all temporary IMAP-only accounts disabled and authorization removed after each run, unrelated mailbox records unchanged. The loopback test listener was removed from nginx's active configuration after acceptance; `ss` confirms port 18443 closed. The hidden candidate tree is retained for evidence and blocked by the public dotfile rule.
- Signed-in branding corrections: Roundcube requires `elastic:*`, not `elastic`, as the skin-wide logo key. CSS now targets verified Elastic `#layout-menu`, `#taskmenu`, `#layout-sidebar` and panel-header structure. These corrections passed isolated and public HTTPS integration checks and are live. Local 26-check policy/installer suite remains green.
- User-approved follow-up LOCAL commit `825ad2b` contains mail API/branding/test corrections and the progress snapshot. Staged from that exact commit at `/root/udf-releases/udf-mail-825ad2b`; the deployed policy byte-matched the isolated tested file. No Git push. The initial L3 result applies to `77fb391`, not a claim of a new review of these later corrections.
- APK publication completed: `/root/udf-releases/udf-20260921-77fb391/APKS_PUBLISHED`. Both APKs were atomically installed, download config tested and nginx gracefully reloaded. Previous binaries/config retained at `/var/backups/udf/udf-20260921-77fb391/apk-publication`. Public HTTPS downloads verified from both the production host and the external local machine: Party `/downloads/udf.apk` = 30,106,695 bytes / SHA-256 `dca3e1fb78d8722675751f08c401bb898b8d04915231c0c6a7fe6c9abdb070e1`; Mail `/downloads/udf-mail.apk` = 19,543,248 bytes / SHA-256 `9d1e77b49fe62db4118bad835055a23b6abb46f16e809d34cb4d216916976cd1`. Correct Android content type, attachment filenames and no-cache headers; nonexistent APK returns 404. Initial source/Mail uploads were interrupted and successfully retried before activation.
- Corrected PUBLIC mail activation passed: backup `/root/backups/premailsession-20260921T164839Z-kFJGKg`; 22 real HTTPS/IMAP checks passed on mail.udf-party.co.za, including both fixed-expiry persistent secure cookies, mailbox/settings logo+stylesheet, fresh HTTP-client resume, logout, second-client revocation, expiry and concurrent login. Receipt: original release staging `mail-acceptance-release-check-873d8c510719.json`. All temporary test-account logins disabled and authorization revoked; original mailbox records unchanged; no messages sent or existing mailbox contents read. Existing users need one fresh login because of policy activation. Policy scope is webmail sessions, not physical-device enrollment, stolen-cookie prevention or independent IMAP clients; already-authorized in-flight requests may finish.
- Final data acceptance passed read-only against production: public W061/W066 contested=false and no councillor; W079/W092 contested=true with linked councillor; all 25 linked without invented email/membership identity; a real existing member is eligible through the live scorecard service (no rating submitted). Scheduled analytics snapshot was 13 seconds old, jsonb object/array types correct and visitor count matched source; live download report total 10 matched raw rows and daily sum at verification. Realtime read succeeded; unauthenticated analytics/scorecard requests returned 401.
- Final service health: udf-api active with NRestarts=0; nginx, php8.1-fpm, postfix, dovecot, opendkim and password-helper service active; no API error-priority journal entries since deployment. Database, uploads, signing identity and existing passwords preserved.
- Remaining acceptance limitations: browser automation unavailable, so signed-in visual appearance, interactive map UI and authenticated native Mail force-stop/relaunch persistence remain unverified. Native install/launch/security-flag observations above and real HTTP cookie reuse do not prove native authenticated persistence. The server-side 30-day policy is verified by deadline/expiry tests, not by waiting 30 days. Credential rotation concern above remains unresolved; no passwords were silently rotated.

## Historical status before the current handoff
Recorded earlier on 2026-09-21. Overall status: **FIVE THINGS FIXED, BUILT AND PROVEN LOCALLY — NOTHING DEPLOYED, BECAUSE PRODUCTION SSH IS STILL CLOSED (port 22 refuses connections; HTTPS prod is healthy): (1) members can now RATE their councillor — the 25 councillors had no `members` row, and the whole accountability model is keyed on `members.id`, so `deploy/scripts/seed-councillors.mjs` now creates email-less `candidate`-tier rows and links `users.member_id` + `leaders.member_id`; (2) wards UDF never contested are described honestly instead of as a 'vacant seat' / 'Unassigned', and a primary-ward-only read bug that made 93 wards look unstaffed is fixed; (3) the map's readout and case bars now FOLLOW the ◎ location button instead of describing the ward you registered in; (4) ◎ now zooms to the ward / subcouncil / region of the layer in play; (5) the map frame got a bigger screen and its locate/reset buttons are no longer scrolled off-page — plus the `analytics.realtime_refresh` jsonb/json failure is fixed. See the top section for the deploy recipe that is ready to run the moment SSH reopens**. Prior top item: **THE STARTER-PACK EMAIL NO LONGER LINKS TO `crm.udf-party.co.za/manifesto` — the dead "Read the full manifesto →" anchor is removed from both the HTML and the plaintext part, backend redeployed and the rendered email proven to contain zero hyperlinks**. Prior top item: **COUNCILLORS CAN NOW CHANGE THEIR OWN WEBMAIL PASSWORD — Roundcube `password` plugin enabled against a root-only unix-socket helper that never gives PHP the mailbox store — DEPLOYED & LIVE-VERIFIED END TO END (see top section)**. Prior top item: **the webmail login logo is now genuinely UN-CUT (the source asset was the crop, not the CSS) and the login card carries the party slogan + all seven manifesto pillars — DEPLOYED & MEASURED LIVE**. Before that: **CPT-W092 now resolves to the certified candidate ROY STANLEY PRINSLOO instead of Brendon Lakay, and the seeder no longer re-asserts incumbents — LIVE-VERIFIED**. Before that: **25 UDF councillors SEEDED AS PRODUCTION CRM `ward_councillor` ACCOUNTS with their live `@udf-party.co.za` emails, every IEC ward linked and the new bio set in-app AND on the homepage roster — DEPLOYED & LIVE-VERIFIED**. Prior item: **councillor webmail login now FULL-FIT branded — UDF logo at native aspect, UDF favicon, UDF red/gold/black palette via a Roundcube plugin — DEPLOYED & LIVE-VERIFIED**. Before that: **25 UDF councillor mailboxes (`initialsurname@udf-party.co.za`) PROVISIONED on the live mail host (see the mailbox section below; no CRM/APK/nginx change)**. Prior top item: **Live Map card copy on both `/mobile` landing surfaces trimmed to remove "members, volunteers" (now "See events across every region on an interactive map.") — DEPLOYED & LIVE-VERIFIED (see top section; no APK rebuild)**. Prior top item: **councillors can now be LINKED TO MULTIPLE WARDS from CRM Users & Roles without any APK rebuild (migration 051 `ward_codes[]` + CRM comma-separated ward list + app read paths widened) — DEPLOYED & LIVE-VERIFIED**. Before that: `/mobile` app landing copied into the marketing site as native `udf-party.co.za/mobile` + "App · Status" footer links removed — both DEPLOYED & LIVE-VERIFIED. Before that: app download page fixed to deliver the APK. Before that: public `/councillors` page HIDDEN on `udf-party.co.za`** (26-card roster retained on the homepage; all inbound links repointed to `/#counselors`) **and a signed release APK rebuilt to versionCode 5 / versionName 1.0.4 with the map (layer colours + zoom-growing labels) VERIFIED byte-identical to the deployed web build** — both DEPLOYED & BUILT. Earlier baseline: COUNCILLOR TRIM (migrations 048-050) + APK 1.0.3 (now superseded by 1.0.4) DEPLOYED & VERIFIED. Production backend migration head is now **051** (051 applied on top of 050). Frontend static export + apex `website/` redeployed; the public councillor roster now shows name-only cards and the trimmed section lede. Signed release APK rebuilt at **versionCode 4 / versionName 1.0.3** (superseded by 1.0.4). Mail (postfix/dovecot/opendkim) + nginx untouched and active; udf-api restarted with 0 crash-restarts. The prior permission-label frontend release (`udf-20260919T023411Z`) remains live. The marketing site now uses clean URLs (`/`, `/councillors`, extensionless pillar links). `carlmarks397@gmail.com` was promoted to the platform's first `superadmin`. A production **CORS** misconfiguration that silently blocked ALL apex-site (`udf-party.co.za`) hydration of `/api/public/*` — so uploaded councillor photos never appeared — was found and fixed (apex + www added to `CORS_ORIGINS`). A SECOND, distinct cause — helmet's global `Cross-Origin-Resource-Policy: same-site` blocking the **mobile app** WebView (origin `http://localhost`) from loading councillor photos cross-site — was also found and fixed (the public image endpoints now send `cross-origin`). Separately: the map's ward/subcouncil NUMBER labels were fixed to GROW when zooming in, the region/subcouncil/ward layers were given DISTINCT COLOURS, and BOTH are now built + DEPLOYED to the web CRM (byte-identical chunk + live DOM verified); the edge hardening from the read-only **URL leak test** (HSTS + CSP on static pages, dotfile-deny 404) was APPROVED ("do a and b") and APPLIED to nginx, verified live with the app unbroken and dotfiles now 404. The URL leak test itself found NO sensitive data leak via URL.

## VERIFICATION + HARDENING PASS (LOCAL — SECURITY CLEAN, DATA-REPORT PROVEN, WEBMAIL INSIDE-BRANDING ADDED; STILL NOT DEPLOYED, SSH CLOSED) — (2026-09-21)
- Request (user): as an expert engineer, confirm the data report is correct and live, run detailed debugging on all the changes and confirm the workflow; an expert cyber-security review of the platform; and brand the mail.udf-party.co.za pages INSIDE the login (not just the sign-in screen).
- **LIVE STATUS UNCHANGED: NOTHING DEPLOYED.** `nc -z -G 5 102.68.98.129 22` → SSH-PORT-CLOSED again this session, while `https://mail.udf-party.co.za/` → HTTP 200. "Live" below therefore means the local stack + the running dev backend, not production.
- **CYBER-SECURITY REVIEW — CLEAN.** Qoder L3 deep review ran to completion with **0 findings**. Independent engineer pass on top of it: (a) SQL-injection — every `${}` fragment in the query layer interpolates a CODE-CONTROLLED identifier (analytics `col` from a fixed array, `SCORECARD_COLS` const, geo `keyCol`) or a scope/WHERE builder that emits `$n` placeholders; no request value is ever concatenated; (b) response headers observed in the workflow run show `cross-origin-opener-policy: same-origin`, `cross-origin-resource-policy: same-site`, `x-frame-options: SAMEORIGIN`, `x-content-type-options: nosniff`, `referrer-policy: no-referrer`, `x-xss-protection: 0`, rate-limit `300;w=60`, and authz behaviour `401 anon → 403 lower role → 200 superadmin → 403 when the module toggle is off`; (c) the analytics reads are superadmin-gated (`Permission.ANALYTICS_READ`), cookieless, and carry no member PII by design.
- **DATA-REPORT CORRECTNESS — PROVEN.** `.tmp-verify/verify-data-report.mjs` seeds a KNOWN event/download set and drives the exact functions the scheduler + API use, then deletes its probes: **10/10** (realtime singleton written with `by_site` jsonb + `by_path` jsonb and the marker at visitors=2; `getRealtime`/`getTraffic`/`getOverview`/`getDownloads` all equal the seed — marker views=3/visitors=2, downloads total matches raw COUNT, version breakdown=2). Confirmed the scheduler mounts `analytics.realtime_refresh → refreshRealtime` every 60s. Regression suites re-run green: `test:analytics` 15/15, `test:workflows` **50/50** (fixtures rolled back). `proof-m4-m5.sh` re-run live on the dev backend: W061/W066 `vacant=True contested=False`; W079 `contested=True` + `eligible: true` with the linked `candidate`-tier member (`has_email=f`); the uncontested ward returns the honest 403 sentence. CAVEAT RECONFIRMED: the local `NORTH-W09` probe councillor has `users.member_id` linked but a BLANK `leaders.member_id` — the documented "`linkCouncillorMembers` only updates an EXISTING leaders row" edge; watch `member_linked` in the prod plan read.
- **WEBMAIL INSIDE-BRANDING — CODED, LINTED, NOT DEPLOYED.** Was login-only: `skin_logo` had just `elastic:login` and the stylesheet scoped its identity to `.task-login`, so every signed-in page reverted to stock teal Elastic. Now: `brand-webmail.sh` sets `skin_logo['elastic']` (all tasks) + keeps `elastic:login`; `udf-branding.css` gained a post-login block painting the header band (`#header`) ink→black with a 3px red base rule, light header/taskbar text with gold hover, a sized header `#logo`, red folder-rail hover/selected with an inset red marker, and deep-red unread/list-header text — all on the Elastic skin's own structural ids, no skin file edited, so a Roundcube upgrade does not lose it (re-run the script). `bash -n brand-webmail.sh` OK; `php -l` clean on `udf_branding.php` and the password driver. The inside page is a config + stylesheet change on the mail host — it needs **no** Next/APK rebuild. Visual confirmation of the post-login selectors still requires the live browser once SSH/deploy reopens (the selectors are standard Elastic but were not observable from this sandbox).
- **DEPLOY ADDITION to the ordered recipe below:** after the CRM ships, run `scp -r deploy/scripts/brand-webmail.sh deploy/branding deploy/webmail root@HOST:/root/udf-deploy/ && ssh root@HOST 'bash /root/udf-deploy/brand-webmail.sh'` to apply the inside-branding (it backs up config.inc.php first and is idempotent), then browser-check a signed-in mailbox/settings page.

## RELEASE (LOCAL — BUILT, TESTED AND MEASURED; NOT DEPLOYED, SSH TO PRODUCTION STILL CLOSED): councillor rating unblocked, uncontested wards told honestly, map follows ◎ and gets a bigger screen, analytics job fixed — (2026-09-21)
- Request (user, verbatim): *"Fix it and also the uassigned councilors assign them. Why not able to rate councilor in the app. When the location buttion is press in the app the map move to the location but the data below stay - table below needs to be interactive . The map need a littitl bigger screen and zoom to the ward if location is press clear ward or sub or region depend on the selected type in map."* Five asks: the analytics job flagged in the section below ("Fix it"), the "unassigned" councillors, rating being impossible in the app, the readout under the map not following the location button, and a bigger map that zooms to whichever layer is selected.
- SCOPE DECIDED BY QUESTION BEFORE WRITING CODE: W61/W66 → **honest vacancy copy, no invented names** (the certified IEC list fields no UDF candidate there, so "vacant seat" is a claim the party cannot make); the rating blocker → **create member rows for the councillors and link them, with no email**; delivery → **web CRM plus a new signed APK**.
- **NOTHING WAS DEPLOYED.** `nc -z -G 5 102.68.98.129 22` → `SSH-PORT-CLOSED` at the start of the session and again at the end, while HTTPS prod answers normally — consistent with fail2ban after the rapid reconnects during the mailbox/password work, and there is no client-side remedy. Everything below is staged and re-testable locally.

### 1. `analytics.realtime_refresh` — the pre-existing 42846 failure ("Fix it")
- ROOT CAUSE: `json_object_agg`/`json_agg` return `json`, the target columns are `jsonb`, and `COALESCE(<json>, '{}'::jsonb)` has no common type → `42846 COALESCE could not convert type jsonb to json`, thrown every minute, killing the refresh behind the live-visitor surface (the page reads a stale singleton rather than failing, which is why nobody noticed).
- FIX: cast the aggregates to `jsonb` at the source in `backend/src/modules/analytics/service.ts::refreshRealtime`, so operands and columns agree; the reasoning is recorded in the function docblock.
- PROOF (fresh, this session, `.tmp-verify/analytics-jsonb.sql` against the local Postgres, both inside rolled-back transactions): the NEW statement → `UPDATE 1`, returning `visitors_now=0 by_site={} by_path=[]`; the OLD statement, in the same file, → `ERROR: COALESCE could not convert type jsonb to json` on the `json_agg` COALESCE line — the exact production message. `npm run test:analytics` 15/15.

### 2. "Why not able to rate councilor in the app" — the councillors had no member row
- THE MODEL: `councillor_scorecards`, `councillor_patrols` and `councillor_service_requests` all key the councillor on `members.id` (`… councillor_member_id … REFERENCES members(id)`), and `users.member_id` is mirrored into `leaders.member_id`. The 25 councillors were seeded as CRM USERS only, so every ward reported `vacant_seat` and "Rate your councillor" could never open — for all 116 covered wards, not just the 2 uncontested ones.
- WHAT WAS ADDED: `linkCouncillorMembers({apply})` in `deploy/scripts/seed-councillors.mjs` — one `members` row per councillor account with **`tier='candidate'`, `status='active'`**, the name opened from sealed `sealed_pii` and re-sealed through production crypto (never copied in the clear), `ward` + `region_code` from the account, `tags=['councillor','ward:<CODE>']`, and deliberately **no `email`/`email_bidx`, no `membership_no`, no `public_code`, no `member_consents` row and no T&C stamp**. Rationale in the function docblock: a councillor is not a paying member, must never be OTP-provisioned or emailed through the member paths, must not appear on a public `/v/` ID page, and must not acquire an invented consent record.
- WHY THAT IS SAFE: `loadMemberFacts()` returns null for an email-less row so `provisionMemberNo`/starter-pack mail skip it (`skipped:'no_member_or_email'`); `member_consents` is always LEFT JOINed; the bind path in `provisionMemberUser()` only writes `WHERE member_id IS NULL` and inserts `ON CONFLICT DO NOTHING`, so a councillor who later registers for themselves cannot 500 on the UNIQUE column. No public marketing counter reads `members` (the public module has exactly one gated `FROM members`), so +25 candidate rows cannot inflate a published number.
- THE LINK ITSELF: `INSERT members` → `UPDATE users SET member_id=$2 WHERE id=$1 AND member_id IS NULL` (throws if the row was linked by someone else mid-run) → `UPDATE leaders SET member_id=… WHERE user_id=$1`, all inside `withTransaction`. Verification queries now report `member_linked`. Dry-run by default; `SEED_APPLY=yes` to write.
- PROOF ON THE LOCAL STACK (this session's re-run of `.tmp-verify/proof-m4-m5.sh`, plus the before/after break-and-restore from the previous session): a `ward_councillor` account with its member row linked → `GET /api/scorecards/current` → **`eligible: true`** with `councillor.memberId = a6556975-7c31-4aba-988c-bd97a2985def`; clearing `leaders.member_id` on the same session flips the same ward to `vacant_seat` / "there is no active councillor to rate right now" — i.e. the blocker was reproduced and removed in one place. `PUT /api/scorecards/` then 200s a real card against that member row (100-word rule 400s correctly, and a same-month re-submit updates the same row id).
- DRY-RUN REHEARSED: `BACKEND_DIST=… CRM_GENERIC_PASSWORD=… node deploy/scripts/seed-councillors.mjs` → plan printed for all 25 accounts / 116 ward slots and **"councillor member rows: 0 councillor account(s) have no members row"** against the local DB, where the probe councillors are already linked — the skip path works, so the prod run creates exactly the 25 unlinked ones.
- CAVEAT FOR THE PROD RUN: the script updates an EXISTING `leaders` row and never creates one. All 25 have one (created by `syncCouncillorLeader()` at seeding), but if any ward still reports `member_linked` false, that is the reason.

### 3. "The uassigned councilors" — wards UDF did not contest now say so, and the CRM stopped lying
- NEW SIGNAL `contested` ("did UDF stand a candidate in this ward at LGE2026"), derived from the existence of a `ward_councillor` user row for that ward — active or not, because those accounts came off the certified list: `councillorWasFielded(wardCode)` and the batch `wardsWereFielded(wardCodes)` in `backend/src/modules/transparency/service.ts`, which unnests `ward_codes` with the primary appended so a multi-ward councillor counts on every ward they hold. `getOverview()` returns `contested = councillor ? true : await councillorWasFielded(wardCode)`.
- WIRED THROUGH EVERY SURFACE THAT WAS MAKING THE WRONG CLAIM: `/api/transparency/wards/councillor` (top-level `contested`), `geo/service.ts` `RegionChild.contested` (non-ward children carry `true` so they can never be labelled vacant), `types.ts` (three interfaces; the app reads it with `=== false` so an OLDER server degrades to the neutral copy instead of asserting "we didn't stand anyone"), `WardTransparency.tsx` + `RegionSheet.tsx` banners, the CRM ward list, the **starter-pack welcome email** (HTML + plaintext now distinguish never-contested from vacant) and the **scorecard eligibility message** (`Eligibility.message`, so 403s carry the honest sentence: "UDF has no ward councillor in this ward — no UDF candidate was fielded here at the 2026 local elections").
- A SEPARATE, REAL BUG IN THE CRM SCREEN: `crm/wards/page.tsx` built `councillorByWard` from `u.wardCode` ONLY, so a multi-ward councillor was credited with their lowest-numbered ward and **93 wards displayed 'Unassigned'**. It now maps every code in `wardCodes ?? [wardCode]`, prefers `fullName` over the email, and marks disabled accounts "(account disabled)"; the warn tile became `Wards UDF Did Not Contest` (neutral, not red) and the cell reads `No UDF candidate at LGE2026`.
- PROOF (local API, fresh this session): `CPT-W061` and `CPT-W066` → `vacant=True contested=False councillor=None`; `CPT-W079` → `vacant=False contested=True` with a linked councillor. (The 2 uncontested wards are the certified list's own gap, recorded in the seeding release below.)

### 4. "The table below needs to be interactive" — the map readout now follows the location
- WHAT WAS WRONG: `focus = selected ? shapes.find(selected) : here`. ◎ moved the viewport but never set `selected`, and on the member map `selected` is ALWAYS the ward you registered in (the `wardCode` effect selects it on load) — so the map flew to where you stand and the place table + case bars underneath went on describing your home ward. Exactly the reported symptom.
- FIX (`UdfStaticMap.tsx`): a `followMe` mode armed by ◎ and disarmed by a deliberate tap or a reset; `frameLocation(fix)` selects the containing shape **at the layer currently on screen** and flies to `fitBox(boxOf(shape), LOCATE_FIT[layer])`, where `LOCATE_FIT = { ward: 1.35, subcouncil: 1.25, region: 1.1 }` — the layer-dependent zoom that was asked for. `framedRef` remembers what was last framed so ~1 Hz GPS drift does not re-yank the viewport out from under a pan, and a repeat ◎ re-frames from the last fix without a new GPS read. When nothing at that layer contains the fix, `selected` is CLEARED rather than left describing somewhere else. The old fixed 22%-width jump is gone (its comment says why).
- MEASURED IN A REAL BROWSER (member session, own ward CPT-W079, local stack; the MCP tab is a HIDDEN page, which suspends `requestAnimationFrame` AND throttles `setTimeout` to ~1/min — so the fly-to tween needed a microtask-driven synthetic clock before it could be observed at all; the first two runs of this test were invalid for that reason, and this is worth knowing before anyone re-runs it). With a fix planted inside `CPT-W065`: ward layer → `viewBox 256.7 914.9 70.1 106.9`, table `Ward 65 · Subcouncil 18 · Lotus River · Southern Region`, status "You are in Ward 65 · following your location"; switch to **Region** → `0.0 852.3 440.7 671.5` + "Southern Region"; **Subcouncil** → `207.0 861.3 165.9 252.7`; back to **Ward** → the ward framing returns. Every one of those four boxes equals the independently computed `fitBox(boxOf(shape), LOCATE_FIT[layer])` (`.tmp-verify/mapfit.cjs`, which reproduces the projection/clamp) to the decimal, so the zoom is proven layer-correct, not just visibly different. The layer buttons' own `setView(FULL_VIEW)` reset does NOT clobber the follow re-frame (the follow effect wins in the same commit).
- ALSO MEASURED: a tap on Ward 1 while following → viewport `323.0 566.7 122.6 186.8` and the table + bars scope move to Ward 1 (so the table is interactive both ways); a subsequent drift tick does NOT pull the map or the table back; a fix at lon 17.0 → "You are outside the mapped area."; a second ◎ re-arms and re-frames to `411.6 965.1 57.1 87.0`. `aria-pressed` on the shape tracks the located area at every layer.

### 5. "The map need a little bigger screen"
- THE GEOGRAPHY IS PORTRAIT (~0.656 wide:tall) AND THE PANE IS NOT: squeezed into the flex column, the `preserveAspectRatio="…meet"` SVG shrank the MAP to fit the reduced height and padded the sides — the map drew at roughly three quarters of the available width inside a full-width white box, which is what read as "too small". The frame now takes the width its height affords (inline `aspectRatio: 1000 / 1523.8` + `--udf-map-aspect`), with `flex: none` so the column cannot crush it.
- DEFECT FOUND WHILE MEASURING THIS, AND FIXED: capping the frame at a flat `80dvh` ignored the 54px layer row sitting above it, so at 618×766 the frame was **612.8px tall inside a 600.5px pane that had already spent 54px** — the bottom 66px of the map, containing the ◎ locate and ⤢ reset buttons, was scrolled out of view at rest. The budget is now the space the map actually has: `width: clamp(240px, calc((100dvh - 220px) * var(--udf-map-aspect)), 100%)` (166px chrome + 54px layer row), with `vh` first for older engines. Re-measured in the same tab: frame **358.3×546**, `frameOverflowsPane = -0.5px`, both tool buttons `fullyVisible: true`, and `svg/frame = 0.994` (the 0.6% is the border) — the SVG still fills the frame, so there is no letterboxing either way. Geography renders **356px wide against ~286px before** = +24% and no white side gutters.
- NOT A REGRESSION FOR THE CRM: `/crm/heatmaps` mounts the same component inside a content-height `CrmCard`, where nothing was being clipped and nothing is now — the clamp only changes the size by a few percent either way, and the `240px…100%` bounds keep it sane at both ends.

### BUILD, TYPECHECK, LINT — ALL GREEN (and a trap worth recording)
- `backend`: `tsc --noEmit` clean, `npm run build` clean, `test:analytics` 15/15, `test:workflows` **50/50** (fixtures rolled back). `frontend`: `tsc --noEmit` clean, `next lint` = the same **6 pre-existing warnings** (none in a file touched here), `caps:check` all pins hold, and the deploy-path export `NEXT_EXPORT=1 next build` → **✓ Generating static pages (45/45)**, exit 0.
- **TRAP: `NODE_ENV=development` in this shell makes EVERY `next build` fail.** Both `npm run build` and `npm run build:mobile` initially died with `TypeError: Cannot read properties of null (reading 'useContext')` on **all 45 routes including `/404`**, from inside Next's own error boundary (`usePathname`) — the server chunks are compiled in DEV mode against the PROD prerender runtime, so the React dispatcher is null and the boundary crash masks the real render. It is NOT a code defect and NOT the shared `.next` with the dev server (deleting `.next` and killing `next dev` changed nothing). Build with `env -u NODE_ENV …` — which is now how the pending commands below are written.
- HEAD IS NOT A BASELINE: the working tree is ~124 files ahead of the last commit, so reverting any subset of it to check a theory does not compile (`types.ts` at HEAD lacks `WardCandidate`, which `crm/candidates` needs). Revert-and-build bisection is not available on this repo; back up and restore by hand instead (that was done here, and the 9 files were verified byte-identical to their backups afterwards).

### APK 1.0.5 / versionCode 6 — BUILT AND VERIFIED LOCALLY, NOT PUBLISHED
- Bumped `frontend/android-overrides/app-build.gradle` (the source of truth; `android:assets` regenerates `android/app/build.gradle` from it) 5/1.0.4 → **6/1.0.5**, then `env -u NODE_ENV JAVA_HOME=/opt/homebrew/opt/openjdk@17/… npm run apk:release` → BUILD SUCCESSFUL in 1m33s (48 tasks executed).
- FACTS: `app-release.apk` 30,106,695 bytes, sha256 **`dca3e1fb78d8722675751f08c401bb898b8d04915231c0c6a7fe6c9abdb070e1`**; `aapt dump badging` → `com.udf.party versionCode='6' versionName='1.0.5'`, minSdk 22, targetSdk 36, `application-debuggable` absent; `apksigner verify` → v1+v2 true, cert SHA-256 `0744136b…` = the approved `CN=UDF Party Upload` key (unchanged from 1.0.2-1.0.4).
- CONTENT PROVEN IN THE PACKAGED ASSETS (`android/app/src/main/assets/public`): production base `https://crm.udf-party.co.za/api` present; `following your location` in the map chunk; `did not field a candidate` in the app chunk; `No UDF candidate at LGE2026` in the CRM wards chunk; the new CSS rule `100dvh - 220px` in `6c15baa3910191b6.css` — the SAME filename the local export produced, so app and web ship the same stylesheet; zero occurrences of the stale `80dvh`.

### NEXT, IN ORDER, WHEN SSH REOPENS (nothing here is optional except the geo snapshot)
1. `env -u NODE_ENV API_BASE=/api bash deploy/scripts/build-offbox.sh` (rebuilds `backend/dist` + the export into `deploy/.artifacts`) → `SERVER=root@102.68.98.129 bash deploy/scripts/ship.sh`.
2. On-box, in this order: back up DB + `dist` + `frontend`; swap `udf-api.service` ExecStart to the new candidate and `systemctl restart udf-api`; **no migration is needed** (head stays 051); stage the web export; then `cd /opt/udf/backend && set -a && . /etc/udf/udf-api.env && . /etc/udf/secrets.env && set +a && CRM_GENERIC_PASSWORD='…' node /root/udf-deploy/seed-councillors.mjs` — READ THE PLAN (expect 25 accounts already present, 25 councillors reported as having no members row) — and only then the same command with `SEED_APPLY=yes`.
3. LIVE VERIFY: `GET /api/transparency/wards/CPT-W061/overview` → `vacant:true, contested:false`; a member in a covered ward → `/api/scorecards/current` `eligible:true` (today all 116 say "no active councillor to rate right now"); journald shows `realtime_refresh` no longer erroring; the CRM wards screen shows named councillors in the 93 wards that used to read 'Unassigned'; the app map follows ◎.
4. Replace `/var/www/udf/downloads/udf.apk` with the 1.0.5 build (keep the old one), and re-point the download counter. Play/AAB upload remains gated on explicit approval.

## RELEASE: starter-pack email's "Read the full manifesto →" link removed — it pointed at a page that does not exist — DEPLOYED & LIVE-VERIFIED (2026-09-21)
- Request (user): pasted the "The mini-manifesto" block from the starter-pack email and said *"Read the full manifesto → this link in here starter pack must be remove no crm.upd-party.co.za"*.
- WHAT THE LINK ACTUALLY DID (worth recording, because it looked healthy): `starterPack.ts` built it as `appUrl('/manifesto')` = `${PUBLIC_BASE_URL}/manifesto`, and production `PUBLIC_BASE_URL` is `https://crm.udf-party.co.za`. The Next static export deployed there has **no `/manifesto` route**, but nginx `location / { try_files $uri $uri.html $uri/ /index.html; }` answers the miss with a **200 carrying the CRM app shell** (`<title>UDF Party</title>`, no manifesto copy). So a `curl` status check passes while the member lands on the sign-in app: a silent-fallback 404. Confirmed live: `crm.udf-party.co.za/manifesto` → 200 with 0 manifesto strings, and there is no `website/manifesto.html` and no apex `/manifesto` either (404) — the party's manifesto copy lives on the apex homepage and the three pillar pages, and `/api/public/manifesto` is a **JSON API**, not a page.
- FIX (narrow, no repointing invented): removed the anchor `<div>` after `miniManifestoHtml()` and the `Full manifesto: …` line from the plaintext part in `backend/src/modules/onboarding/starterPack.ts`; deleted the now-orphaned `appUrl()` and its `env` import so nobody re-derives a member-facing URL from the CRM origin, leaving a comment stating why and what would make a link valid again. **`PUBLIC_BASE_URL` itself was left alone and is still correct for the surfaces that do exist on that origin** — `joinUrl()`'s `/register?ref=` and `issueToken()`'s `/confirm/<token>` (memberships service) are real CRM routes.
- THE MINI-MANIFESTO CONTENT IS UNTOUCHED and still renders from the system of record (`MANIFESTO` in `backend/src/modules/public/content.ts`: mission + the seven pillar headlines), so nothing else in the email changed.
- DEPLOY: `npm run typecheck` and `npm run build` clean; backed up the incumbent file to `/root/backups/pre-manifesto-link-20260921T040012Z/`, swapped **only** `dist/modules/onboarding/starterPack.js` into `/opt/udf/backend/dist/...` (md5 `d64af1fe…` identical both sides, `udf:udf 644` preserved), `systemctl restart udf-api`. No migration, no rebuild of the frontend or APK, no npm ci, nothing else touched.
- VERIFY (against the deployed dist and the real DB, read-only — an ad-hoc node call to `buildStarterPackEmail()` for ward CPT-W092): `href=` anchors in the HTML **0**, `crm.udf-party` in HTML **false**, in plaintext **false**, URLs in the plaintext part **[]**, and the mini-manifesto heading + pillar copy still present (`Service delivery` true). That was the only hyperlink in the whole starter-pack email; the OTP-resend email has none and was unaffected. Post-restart: `udf-api` `active`, `NRestarts=0`, `/healthz` 200.
- PRE-EXISTING DEFECT FOUND ON THE WAY (NOT caused by this change, NOT fixed): the `analytics.realtime_refresh` scheduled job fails every minute — `DatabaseError: COALESCE could not convert type jsonb to json` at `backend/src/modules/analytics/service.ts` `refreshRealtime` (~line 367). 1805 log lines since 2026-09-20 00:00, so it long predates this deploy and it still recurs; the surface that consumes that stat is stale rather than broken. Flagged for a separate decision.

## RELEASE: councillors can change their own webmail password — Roundcube `password` plugin + root-only socket helper — DEPLOYED & LIVE-VERIFIED (2026-09-21)
- Request (user): "add that plugin" — closing the caveat left by the seeding release, which noted that Roundcube had no password plugin installed, so a councillor who rotated away from the shared mailbox password had no way to do it themselves.
- WHY THE OBVIOUS IMPLEMENTATION WAS REJECTED: the `password` plugin already ships complete at `/var/www/udf/webmail/plugins/password/` and is merely absent from `$config['plugins']`, but **no shipped driver can work on this host as configured**. The `roundcube` FPM pool sets `disable_functions = exec,passthru,shell_exec,system,proc_open,popen,show_source`; `dovecot_passwdfile` writes `/etc/dovecot/users` straight from PHP and the `dovecot` hash algorithm needs `proc_open`, `chpasswd` needs `popen`. And `/etc/dovecot/users` is root:root 0600 and outside `open_basedir`. The easy fix — chmod the store and widen `open_basedir`/`disable_functions` — would mean one PHP or Roundcube compromise rewrites the hash of any of the 25 councillor mailboxes, so it was not done.
- WHAT WAS BUILT INSTEAD (3 new tracked files + 1 script): `deploy/webmail/mail-passwd/udf-mail-passwd.py` → `/usr/local/sbin/udf-mail-passwd`, a root-only listener on the unix socket `/run/udf-mail-passwd/mail.sock` (root:www-data 0660); `deploy/webmail/mail-passwd/udf-mail-passwd.service` (enabled, `Restart=always`, `ProtectSystem=strict` with `ReadWritePaths=/etc/dovecot`, `NoNewPrivileges`, `RestrictAddressFamilies=AF_UNIX`, `MemoryMax=64M`); `deploy/webmail/password-drivers/udf_mailbox.php` → installed into the vendor plugin's `drivers/` (that is where `_load_driver()` looks) as `rcube_udf_mailbox_password::save($cur,$new,$user)`, which does **no shell and no file access** — one three-line socket call; and `deploy/scripts/enable-webmail-password.sh` to install/enable/verify all of it idempotently. Nothing in the web tier can now read the credential store; it can only ask, "set U's password to P, given you can already authenticate as U".
- HELPER SAFETY PROPERTIES (each one exercised, see VERIFY): the three fields arrive on **stdin**, never argv or the environment, so no password is ever visible in `ps` or journald; the **current** password is re-verified in-process with `crypt.crypt()` against the stored `$6$` SHA512-crypt and compared with `hmac.compare_digest` before anything is written (proven byte-identical to the real stored hash first); the new hash is produced by `doveadm pw -s SHA512-CRYPT` fed on stdin and then **re-verified against itself** before it is allowed into the file; only `@udf-party.co.za` localparts that exist exactly once in the file may change; minimum 10 characters (matching `$config['password_minimum_length']`); the file is replaced with `O_EXCL` temp + `fsync` + `os.replace` + dir `fsync`, staying 0600 root:root, every other line copied through verbatim; a wrong current password costs a 1s sleep and writes nothing. Peer uid is also checked against `SO_PEERCRED` (read via `getsockopt`, because `socket.getpeercred()` is not exposed on this build) — belt-and-braces over the 0660 group check, which the kernel already enforces.
- CONFIG: `password` appended to `$config['plugins']` (now `archive, zipdownload, udf_branding, password`) and a `# BEGIN/END UDF-MAIL-PASSWORD` block setting `password_driver='udf_mailbox'`, `password_confirm_current=true`, `password_minimum_length=10`, `password_log=false` (the helper already journals every change). `php8.1-fpm` reloaded for opcache; **no** dovecot or postfix reload needed (`auth_cache_size=0`, and Postfix does not read this file). No change to `disable_functions` or `open_basedir`.
- BACKUPS: `/root/backups/premailpasswd-20260921T032536Z/` and `...T032705Z/` (Roundcube config + `/etc/dovecot/users` each time).
- VERIFY (end to end, on a THROWAWAY mailbox so no councillor's shared password was consumed or exposed): created `udf-webtest@udf-party.co.za` with its own test password, then drove the real UI in a browser — Settings now shows a **Password** tile (`li#settingstabpluginpassword`, `?_task=settings&_action=plugin.password`) with exactly `Current Password` / `New Password` / `Confirm New Password`; `POST ?_task=settings&_action=plugin.password-save` → 200 and the green bar **`Successfully saved.`**; logout → **login with the NEW password succeeded**; logout → **login with the OLD password rejected** with `Login failed.`; three separate successful changes are in the helper journal. Negative paths measured too: wrong current → `ERR BAD_CURRENT`, unknown user → `ERR UNKNOWN_USER`, new password too short → `ERR WEAK`, foreign domain → `ERR MALFORMED`, overlong → `ERR WEAK`, and a connection from uid `nobody` could not even reach the socket. A `sudo -u www-data php` probe drives the actual driver class as the actual web user and gets `rejected`. Then the scratch mailbox was removed from `/etc/dovecot/users`, from the Postfix map (+`postmap`) and its Maildir deleted — **`md5sum /etc/dovecot/users` is now byte-identical to the pre-test backup (`07f6fda1…`), 33 lines, root:root 0600, so not one councillor mailbox moved.**
- POST-CHECK: `udf-mail-passwd`, postfix, dovecot, nginx, udf-api, php8.1-fpm all `active`; `/healthz` 200; `https://mail.udf-party.co.za/` 200; `/api/transparency/wards/CPT-W092/overview` still 200 → ROY STANLEY PRINSLOO.
- NOT DONE / FOLLOW-UP: the 25 councillors are still on the shared mailbox password (`/root/udf-councillor-mailbox-password.txt`) — the point of this release is that each one can now rotate it themselves in Settings → Password, so the shared value should be handed out once and treated as burned after they do. The `drivers/` file lives inside the vendor plugin, so a Roundcube upgrade removes it; re-running `deploy/scripts/enable-webmail-password.sh` restores driver, unit and config in one pass.

## RELEASE: webmail login logo truly un-cut + slogan and all seven pillars on the login card — DEPLOYED & MEASURED LIVE (2026-09-21)
- Request (user): "mail landing page logo is cut full logo. add short pilliars and slogon: one flag one movement service to self. check it first"
- "check it first" paid off immediately: the real wording is **`One flag · One movement`** (tagline) and **`Service before self`** (slogan), not "service to self". Both were taken from the system of record rather than invented — `PARTY_PROFILE` in `backend/src/modules/public/content.ts`, seeded by migration 037, published live as the `shared.brand` content block and joined the same way `MoreTab.tsx` joins them. The pillars are the manifesto's **seven** (not three): Housing · Public infrastructure · Crime & safety · Economic empowerment · Accountable councillors · Fair municipal finance · Women & youth.
- ROOT CAUSE OF "LOGO IS CUT" WAS THE ASSET, NOT THE CSS: `frontend/public/brand/logo.png` is a **1600×640 crop** of the artwork (flag band plus severed fists), so the previous "full fit" pass could only ever scale a cropped picture. Regenerated `deploy/branding/udf-webmail-logo.png` at **480×480 from the uncropped 1050×1050 `website/assets/img/udf-flag.png`** (the full crowd-and-flag composition) and installed it in place of the band.
- AND A SECOND, REAL CAUSE FOUND BY MEASURING: Elastic offsets `#logo` (`top:16vh`) and `#login-form` (`top:20vh`) with `position:relative` — which does **not** remove them from the flow — inside `#layout{overflow:hidden}` with `#layout-content{height:100%}`. A taller logo therefore slid behind the card and was sliced. Fixed in `deploy/webmail/udf_branding/skins/elastic/udf-branding.css`: both reset to `position:static`, and `#layout-content` becomes a centred scrolling column sized from the viewport height (`min(200px, 26vh, 62vw)`) so it cannot overflow.
- THE FIRST FIX OF THAT WAS ITSELF DEFECTIVE AND WAS CAUGHT BY MEASUREMENT: the initial version used `justify-content:center`, which is the flex centring/overflow trap — when content is taller than the box it splits the excess **above and below**, and overflow above a scroll container is unreachable. Measured live at a 419px-tall viewport it cut 53.73px off the TOP of the logo and no scroll position could bring it back (scrolling down made it worse). Now `justify-content:center; justify-content:safe center;` (declared twice: supporting browsers centre safely, others discard the unknown value and fall back to start alignment, which is never clipped).
- HOW THE COPY GETS INTO THE DOM: the plugin gained an `add_hook('template_container', ...)` handler filling Elastic's own `loginfooter` container (which sits *inside* `#login-form`, so the strip reads as part of the sign-in card) with `.udf-brand-strip` > `.udf-brand-line` + `.udf-brand-pillars`. Verified against the deployed source that `template_container` really returns `$hook['content']` (`program/include/rcmail_output_html.php:1456`), that `rcube_plugin` provides `add_hook`/`include_stylesheet`/`local_skin_path` (`program/lib/Roundcube/rcube_plugin.php:187/327/391`), and — after an early draft used it — that **`html::show()` is not a static API in 1.6.19**, so the markup is built as escaped strings via `htmlspecialchars(..., ENT_QUOTES, 'UTF-8')` and the task check uses `rcmail::get_instance()->task` (there is no `$this->rcmail` on `rcube_plugin`).
- DEPLOY: `deploy/scripts/brand-webmail.sh` extended with 4 more assertions (brand strip present in the served HTML, plus the literal copy "Service before self", "Housing", "Women &amp; youth"). Re-run twice; config backed up each time to `/root/backups/prewebmailbrand-*`. No Roundcube core or skin file edited, so an upgrade does not silently keep any of this — re-running the script restores it.
- VERIFY (measured in the browser, cache-busted; screenshots still unavailable in this IDE — `NATIVE_BROWSER_VIEWPORT_UNAVAILABLE` — so the evidence is geometry): at 618×766 `#logo` renders **199.16×199.16** from natural **480×480** (aspect 1.0, undistorted) at y 86.95–286.10, fully inside the viewport; `#login-form` bottom 671.40 and the brand strip bottom 651.40 are both inside 766; the last pill ("Women & youth") ends at 637.40, visible without scrolling; `#layout-content` computes `justify-content: safe center`, `display:flex`, `overflow-y:auto`, scrollHeight == clientHeight (**0px overflow**); computed logo width 199.156px equals 26vh exactly (0.00 delta), proving the new stylesheet is the one in effect. All 7 pillars and the slogan line are present in the served HTML and rendered.

## DATA: CPT-W092 now publishes the certified candidate ROY STANLEY PRINSLOO — LIVE-VERIFIED (2026-09-21)
- Request (user): "Ward 92 still shows Brendon Lakay fix member now" — overriding the conservative incumbent-preservation recorded in the seeding release below. Decision: **the IEC certified list wins**.
- HOW IT WAS FIXED (by the user, in the CRM, while asking): `Brendon Lakay`'s account was moved from `ward_councillor` to **`member`**, which is the correct app-level lever — `syncCouncillorLeader()` then sets `is_public = FALSE` on his `leaders` row, so `councillorForWard('CPT-W092')` no longer sees him at all and Prinsloo's user-linked public row wins on the first ORDER BY key (`user_id IS NOT NULL`). Verified in the DB: `role=member`, `is_public=f`, councillor count 26 → **25**. No row was deleted and no raw SQL write was needed.
- WHY THAT MATTERS MORE THAN A TIMESTAMP: the earlier note below assumed the conflict was between two live councillors, but the predicate is `u.ward_code = l.ward_code`, so a multi-ward councillor's user-linked row only qualifies *directly* on their primary ward; on their other wards they win because the competing rows are user-less demo rows. Prinsloo (primary CPT-W071) now holds W071, W092, W115 and his other slots.
- MADE PERMANENT: `deploy/scripts/seed-councillors.mjs` no longer re-asserts incumbents — the query, the `UPDATE leaders SET updated_at = now() + interval '1 minute'` and the "incumbent public councillor rows kept" report are removed, replaced by a comment stating that the certified list is authoritative and that a ward must be fixed by changing the *account's role* in the CRM (the exact move just performed). Without this a re-run of the seeder would have flipped W092 straight back. `node --check` clean; not re-run against production (nothing to seed — all 25 accounts already exist).
- VERIFY: `GET /api/transparency/wards/CPT-W092/overview` → 200 **ROY STANLEY PRINSLOO** with the new bio; W071 and W115 also Prinsloo; W081 STEPHEN NOBLE; W061 still no councillor (the certified list has no UDF candidate there).
- CONSEQUENCE TO REPORT: CPT-W092 was Lakay's **only** ward, so his public councillor card is no longer the winner anywhere and he now signs in as an ordinary member. His account, history and `leaders` row are untouched; restoring `ward_councillor` would republish him immediately.

## RELEASE: 25 LGE2026 councillors seeded into the production CRM (accounts + ward links + bio) — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "seed these councilors into the backend with their full name, udf email and generic accounts, add all the wards link to each of them, add bio: Evidence-based. Practical. Community-driven." Scope confirmed by question: **production** CRM, a **different** generic password from the mailbox one, **create 25 new accounts and leave the existing accounts alone**, bio in **both** the CRM/app (`users.bio` → `leaders.bio`) **and** the homepage roster (`ward_candidates.bio`).
- New tracked file: `deploy/scripts/seed-councillors.mjs` — imports the DEPLOYED `createUser` from `/opt/udf/backend/dist` so every write goes through production code (argon2 hash, envelope-sealed `sealed_pii`, HMAC `email_bidx`, `ward_code` FK, `syncCouncillorLeader` mirror, `recordAudit`). Dry-run by default; writes only with `SEED_APPLY=yes`. Idempotent: it resolves each email by blind index and skips an existing account instead of re-hashing or overwriting. Reuses `deploy/scripts/councillor-mailboxes.txt` as the single source of names/emails/wards, and derives each primary ward's macro region from `regions` (ward → subcouncil → region) to match the existing staff convention.
- WHAT WAS WRITTEN: 25 `ward_councillor` users (`users` now 39 rows, councillors 1 → **26**), each with `ward_code` = lowest-numbered ward, `ward_codes` = **all** IEC ward slots (**116 slots** across the 25, matching the certified list exactly), `region_codes` = that ward's macro region, `bio` = the new copy, then `must_change_password = TRUE` (the shared password is therefore temporary) and `tc_version` pre-accepted so first login lands on the password gate, not a T&C wall. 25 public `leaders` rows mirrored by `syncCouncillorLeader`. `ward_candidates.bio` set on **25 of 26** homepage roster rows — `MICHAEL JACK POGGENPOEL` deliberately left blank (he is on the party list and stands in no ward, so he is not one of the seeded councillors); say the word to give him the line too.
- Generic CRM credential: [REDACTED]. The root-only handoff file is `/root/udf-councillor-crm-password.txt` (600, root). Correction: a previous working-tree version of this record contained the credential, contrary to the original storage-only claim. Treat it as exposed; first login requires each account to set a private password. No credential value belongs in this record.
- INCUMBENT PRESERVED: `councillorForWard()` returns ONE row per ward, newest `leaders.updated_at` first, and Roy Stanley Prinsloo's certified ward list also contains **CPT-W092**, already held publicly by the pre-existing councillor **Brendon Lakay**. Rather than silently replace a sitting councillor's published profile ("leave the old ones"), the script re-asserts pre-existing public councillor rows (matched by "public leader on an active `ward_councillor` whose email is NOT one of the plan's") so the incumbent keeps priority. W092 still resolves to Brendon Lakay; Prinsloo covers his other 11 wards. Two bugs were found and fixed in-run: (a) the first version defined "incumbent" as "any public councillor row", so a re-run flattened all 26 timestamps onto one value and made the W092 winner depend on the uuid tie-break; (b) the verification query said `bio = $1` with `leaders`+`users` joined (ambiguous column) and aborted the run AFTER all writes had succeeded. Both fixed in the committed script; the re-run was then a clean no-op (25 × "already exists, untouched").
- WEBSITE STATIC FALLBACK: `website/index.html` councillor cards gained `<p class="council-bio">Evidence-based. Practical. Community-driven.</p>` on exactly the 25 councillor cards (0 other lines changed — verified by diffing against the pre-deploy copy pulled from the box: 25 additions, 0 deletions). The live JS roster already rendered `c.bio`; this keeps no-JS / CMS-down markup equal to the API. `assets/css/style.css` already had `.council-bio` and was untouched (md5-identical local vs prod).
- DEPLOY: `pg_dump -Fc udf` → `/root/backups/udf-preseed-councillors-2026-09-20-211003.dump` (4.7 MB) BEFORE any write; website copy backed up to `/root/backups/pre-councillor-bio-20260920T211554Z/`. Only `website/index.html` was rsynced to `/var/www/udf/manifesto` (md5 `29d216c0…` matches local). **No backend or frontend rebuild, no migration, no `systemctl restart`** — the seeding used the already-deployed dist; nginx/mail/DB config untouched. Nothing committed to git.
- VERIFY: script summary `users {accounts: 26, must_change: 25, ward_slots: 117}` / `leaders {public_leaders: 26, bioed: 25}` (117 = 116 new + Lakay's 1; the 25/26 must_change and 26/25 bioed gaps are both Lakay, untouched by design). 116 of the 118 wards now resolve to a public councillor with the new bio; the 2 with none are **CPT-W061 and CPT-W066** — the two wards the certified list shows no UDF candidate for. 3 stale demo leader rows ("Carl" W001, "Test Me" W081/W092) are no longer the ward winner (real councillors outrank user-less rows by design); no row was deleted. Live HTTP: `POST /api/auth/login` → **200** for `snefdt`, `rprinsloo`, `agmarks`, `ddaniels` with `role=ward_councillor`, `mustChangePassword=true`, `tcAccepted=true`, 25 permissions, 12 modules; `GET /api/transparency/wards/{CPT-W065,W064,W001,W002,W071,W115,W118}/overview` returns the right name + bio; **W092 → Brendon Lakay** (bio null, as before); `GET /api/public/candidates` → 26 items, bio on 25; apex home page serves 25 `council-bio` lines; `audit_log` has 25 `role.change`/`op:create` rows; `/healthz` 200 and postfix/dovecot/nginx/udf-api all active.
- NOT TESTED (and why): the first-login **password-change submit** was not exercised, because doing so would permanently consume one real councillor's shared password and clear their `must_change_password` flag. The path was verified by code read instead: `ChangePasswordGate` renders for any authenticated `mustChangePassword` session and only hides the "current password" field for `role === 'member'`, so a councillor signs in with the generic password and rotates it — and `changePassword` accepts that value as `currentPassword` (the OTP waiver is member-only). Backend DB facts and every login above are live-verified.

## RELEASE: webmail login fully branded — logo full-fit, UDF favicon, UDF palette — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "the logo is not fit correc best full fit and change the favacr correct. mail box and the coulors." Three defects in the first branding pass: Elastic caps `.task-login #logo` at `max-height:100px` inside a 320px card (small, floating, off-balance), the favicon was still Roundcube's own, and only the logo — not the party colours — changed.
- Logo: regenerated `deploy/branding/udf-webmail-logo.png` at **960×384** (2.5:1, 2× the card width, from `frontend/public/brand/logo.png` via `sips`) so it stays crisp when displayed larger.
- Favicon: `deploy/branding/udf-webmail-favicon.ico` (copy of `frontend/public/favicon.ico`, 64×64) installed as `skins/elastic/images/udf-favicon.ico` and pointed at with `$config['favicon']`.
- Colours + fit: new tracked Roundcube plugin `deploy/webmail/udf_branding/` (`udf_branding.php`, `$task = '.*'`, includes `skins/elastic/udf-branding.css` — 11 rules). Palette is the party's own, lifted from `website/assets/css/style.css` `:root` (red `#d8261c`, deep red `#a3170e`, gold `#f7c600`, black `#101010`, ink `#1b1815`, paper `#fbf7f0`) — the login page gets a red-on-black wash, a paper card with a red top rule, red primary buttons, gold focus rings, red selected states, and the logo released to `min(320px, 88vw)` at natural aspect inside a gold frame. A plugin (not an edited skin file) means a Roundcube upgrade no longer silently wipes the branding.
- `deploy/scripts/brand-webmail.sh` rewritten to install logo + favicon + plugin, keep the idempotent `# BEGIN/END UDF-BRANDING` config block (`skin_logo`, `favicon`) and `sed` `'udf_branding'` into `$config['plugins']`, then run 6 verify checks. Existing config keys and the 33 mailboxes untouched; `php -l` clean; ownership restored.
- VERIFY (measured in the browser): `#logo` renders **320×128** from natural 960×384 — aspect 2.5 == 2.5, centred, inside the viewport; body carries the red-on-black gradient; `#login-form` computes `rgb(251,247,240)`; `#rcmloginsubmit` computes `rgb(216,38,28)` on border `rgb(163,23,14)`; the favicon `<link>` resolves to `udf-favicon.ico`; the plugin stylesheet loads with `cssRules.length = 11`. Screenshots still unavailable in this IDE (`NATIVE_BROWSER_VIEWPORT_UNAVAILABLE`), so evidence is computed-style/geometry based.

## RELEASE: 25 councillor mailboxes created + UDF logo on the webmail login — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "for the councilors create me email mail accounts. initials and surnames. proffessional look … ***@udf-party.co.za. Create the mail.udf-party.co.za in the logo". Scope confirmed by question: create the mailboxes on the live mail host now, ONE shared temporary password, and "in the logo" = put the UDF logo on the `mail.udf-party.co.za` login screen.
- Address scheme: first initial + surname, lowercased (`snefdt@`, `fswartz@`); multi-word surnames joined (`cvandervent@`). Only the three MARKS candidates needed a second initial — `agmarks@` (Andhor Grey, 39 ward slots), `akmarks@` (Andhor Karl, 11), `jmarks@` (Jacqueline Weaver). 25 distinct people hold the 116 City of Cape Town ward rows; `MICHAEL JACK POGGENPOEL` is on the 25-name council list but stands in no ward, so he got NO mailbox (not in the user's list). Wards 61/66 have no UDF row at all.
- New tracked files: `deploy/scripts/councillor-mailboxes.txt` (localpart + candidate + wards), `deploy/scripts/provision-mailboxes.sh`, `deploy/scripts/brand-webmail.sh`, `deploy/branding/udf-webmail-logo.png` (480×192 PNG from `frontend/public/brand/logo.png`).
- MAIL HOST (`102.68.98.129`): appended 25 entries to `/etc/dovecot/users` (`{SHA512-CRYPT}` via `doveadm pw`, uid/gid 5000, home `/var/mail/vhosts/udf-party.co.za/<localpart>/`) and to `/etc/postfix/vmail/mailboxes`, `postmap`, created each Maildir (`cur/new/tmp`, 2700 / 700 `vmail:vmail`), then `postfix reload`. **No service was restarted**; Dovecot needs no reload either (passwd-file is read per auth, `auth_cache_size=0`). `/etc/dovecot/users` restored to root:root 600. Backups: `/root/backups/premailboxes-20260920T202644Z/`, `/root/backups/prewebmailbrand-20260920T202656Z/`. Shared password lives only in `/root/udf-councillor-mailbox-password.txt` (600, root) — hand out once, then delete; it is NOT in git, in any repo file, or in the CSV deliverables.
- WEBMAIL BRANDING: logo installed at `/var/www/udf/webmail/skins/elastic/images/udf-logo.png`; `config.inc.php` gained an idempotent `# BEGIN/END UDF-BRANDING` block with `$config['skin_logo'] = ['elastic:login' => '/images/udf-logo.png'];`; `php -l` clean, ownership restored to root:www-data 640. Existing mailboxes (`info@`, `admin@`, `support@`, `membership@`, `no-reply@`) and the 3 `vm478jzwg…` ones were not modified.
- VERIFY: 25/25 `doveadm auth test` OK **and** `postmap -q` returns the mailbox path **and** the Maildir exists (`added=25 skipped=0 failed=0`); a real inbound probe to `snefdt@` was accepted and `saved mail to INBOX` over `dovecot-lmtp`, then deleted so the mailbox is empty again; authenticated OUTBOUND send was proven too (SMTP submission :587 STARTTLS, `login` + one message as `snefdt@`, delivered to its own INBOX, then deleted — loopback-only, so the probe disabled cert verification for that connection and the script was removed afterwards); `/etc/dovecot/users` now holds 33 entries (8 existing + 25 new); `postfix`/`dovecot`/`nginx` all active; CRM `/healthz` still `{"status":"ok","db":true}`. Login page 200 references `udf-logo.png` (asset 200 `image/png`), rendered 250×100 from natural 480×192 (no distortion, no overflow); a real webmail login as `snefdt@udf-party.co.za` reached an empty INBOX and logout returned to the branded screen, 0 console errors. NOTE: only the LOGIN screen is branded — the logged-in taskbar still uses the skin's `logo.svg` (one extra `elastic:*` line in `brand-webmail.sh` would cover it). Screenshots were not captured (`NATIVE_BROWSER_VIEWPORT_UNAVAILABLE`, hidden browser view); verification is DOM/geometry + mail-log based.
- Fix found during the run: the password generator `tr -dc … </dev/urandom | head -c 20` killed the first attempt under `set -o pipefail` (SIGPIPE in `tr`); replaced with `head -c 512 | tr` + a bash substring. The aborted run wrote NOTHING (both maps still had 8 lines afterwards), so no cleanup was needed before re-running.

## RELEASE: remove "members, volunteers" from the Live Map card on both `/mobile` surfaces — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "See members, volunteers and remove these text from Live Map website". The Live Map card copy read "See members, volunteers and events across every region on an interactive map."; dropped the "members, volunteers and" so it now reads "See events across every region on an interactive map." (members/volunteers are no longer advertised as visible on the public map).
- Edited BOTH copies of the landing: `website/mobile.html` (apex marketing `/mobile`) and `frontend/src/app/mobile/page.tsx` (CRM `/mobile`).
- DEPLOY: backups `/root/backups/manifesto-20260920T150005Z.tar.gz` + `frontend-20260920T150005Z.tar.gz`; rsync `website/mobile.html` → `/var/www/udf/manifesto/mobile.html`; rebuilt `NEXT_PUBLIC_API_BASE=/api npm run build:mobile` + `rsync --delete frontend/out/` → `/var/www/udf/frontend` (chown root:udf 755/644). No nginx/API/mail/DB change or reload; APK untouched. Nothing committed to git.
- VERIFY: local `out/` has 0 occurrences of the old string; live `udf-party.co.za/mobile` and `crm.udf-party.co.za/mobile/` both show "See events across every region on an interactive map." with 0 old-text matches.

## RELEASE: multi-ward councillors — link a councillor to MORE THAN ONE WARD from the CRM, no APK rebuild — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "from the crm select i shouldd be able to link councilors to more then 1 ward from the back with out apk rebuild". Confirmed scope via question: target = the APP ward→councillor system (`users`/`leaders`, drives map + ward pages + rate-your-councillor), picker = free-text comma-separated ward-code list in CRM Users & Roles.
- DB: migration `051_leader_multi_ward.sql` adds `users.ward_codes TEXT[]` + `leaders.ward_codes TEXT[]` (backfilled from the single `ward_code`, GIN indexes). The legacy `ward_code` stays the PRIMARY ward (FK + first entry of the list), so territory scope, login and all existing guards are unchanged — a councillor is DISPLAYED for every linked ward but still OPERATES (patrols, member visibility) only in the primary; ratings target memberId so they work from any linked ward.
- Backend: `crm/userService.ts` — `normalizeWards` (trim/dedupe/order), create/update accept `wardCodes[]` and write `ward_code = list[0]` + `ward_codes = list`; `syncCouncillorLeader` mirrors the list into `leaders.ward_codes`. `crm/routes.ts` — create/patch accept `wardCodes` and validate every code against `regions` (`level='ward'`), 400 listing unknown codes (the TEXT[] columns have no FK). App reads: `transparency councillorForWard` matches `ward_code = $1 OR $1 = ANY(ward_codes)`; `geo region-summary` emits one row per covered ward (`CROSS JOIN LATERAL unnest(ward_codes…)`) so the map keys councillors by secondary wards too, and the ward_profiles branch accepts `wp.ward_code = ANY(l.ward_codes)`. Public leader-photo gate needed no change (compares primaries).
- CRM UI (`frontend/src/app/crm/users/page.tsx` + `lib/api.ts`): Ward field in create/edit modals is now "Ward Codes (comma-separated)" (first = primary), pre-filled from the full list; users table Ward column + CSV export show all linked wards.
- DEPLOY: backups `/root/backups/backend-20260920T122402Z-pre-051.tar.gz` + `frontend-20260920T122402Z.tar.gz`; rsync `backend/dist` (incl. 051.sql) → `/opt/udf/backend/dist` (chown udf:udf); `node dist/db/migrate.js` applied 051 (head now **051**); `systemctl restart udf-api`; rebuilt `NEXT_PUBLIC_API_BASE=/api npm run build:mobile` + `rsync --delete frontend/out/` → `/var/www/udf/frontend` (chown root:udf 755/644). nginx/mail/apk untouched; **no APK rebuild** (all app reads are live API). Nothing committed to git.
- VERIFY: typechecks clean (backend + frontend); healthz 200, udf-api active, journalctl 0 errors; backfill complete (users 13/0 missing, public leaders 10/0 missing). Live end-to-end via public API: set Aubrey Charles Robinson (CPT-W110) `ward_codes={CPT-W110,CPT-W065}` — `/api/transparency/wards/CPT-W065/overview` flipped from vacant to showing him, primary W110 unaffected — then reverted (W065 vacant again). The exact deployed geo query verified in psql emits him for BOTH wards. `PATCH /api/crm/users/:id` → 401 unauthenticated (guard intact); live CRM users chunk (new "first is primary" label) 200.

## RELEASE: `/mobile` landing COPIED into the marketing site as a native `udf-party.co.za/mobile` page (no CRM link) — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "Copy the https://crm.udf-party.co.za/mobile/ site directly into the udf-party.co.za website. no link to https://crm.udf-party.co.za/mobile/". The CRM `/mobile` Next page was hand-ported to a self-contained static page on the apex marketing site; every marketing link to the CRM `/mobile` was removed.
- New files: `website/mobile.html` (faithful static reproduction — nav brand→`/`, hero+phone splash, whole-image poster, feature grid, brand-screens row, accountability tools+banner+notif preview, final CTA, copyright footer; downloads render as `<button data-apk>` whose URL lives only in an inline script, so the APK source URL is never in an `href`). `website/assets/css/mobile.css` (copy of the frontend landing stylesheet + added `.udf-brand-mark`/`.udf-fill` static-copy helpers). Copied brand images into `website/assets/img/brand/` (icon-32, splash, app-icon, notification, udf-illustration); the poster reuses the existing `assets/img/udf-flag.png` (md5-identical to `udf-illustration.png`).
- Repointed the "Mobile App" nav tab + footer link on all 4 marketing pages (`index`, `housing-infrastructure`, `safety-communities`, `economy-governance`) from `https://crm.udf-party.co.za/mobile` → local `/mobile` (2 per page; 0 CRM links remain).
- Download target on the apex page is the absolute counted endpoint `https://crm.udf-party.co.za/api/public/download/apk` (apex vhost has no local `/api`/`/downloads`; `window.location.assign` navigation is not CORS-bound) → 302 → `/downloads/udf.apk`. This is the APK source, NOT a link to the CRM `/mobile` page.
- No nginx change (the manifesto `try_files $uri $uri.html $uri/` already serves `mobile.html` at clean URL `/mobile`). DEPLOY: backup `/root/backups/manifesto-20260920T120206Z.tar.gz`; `rsync -az website/` → `/var/www/udf/manifesto` (no --delete; chown root:udf 755/644). No reload; CRM frontend, APK, nginx/API/mail/DB untouched. Nothing committed to git.
- VERIFY: live `/mobile` → 200 text/html 9810B; `mobile.css` 200; brand pngs 200; page has 3 `<button data-apk>`, the APK URL appears once (in JS only), 0 `crm.../mobile` links, brand→`/`; home nav shows 2 `href="/mobile"` + 0 CRM links; `GET /api/public/download/apk` → 302 → `/downloads/udf.apk`. Same-origin 390px DOM probe confirmed no horizontal overflow (scrollWidth==clientWidth==390).

## RELEASE: remove the "App · Status" footer links from `/mobile` — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "remove these bottom right : App · Status". Deleted the footer right-hand `<span>` (`<a href="/mobile">App</a> · <a href="/healthz">Status</a>`) in `frontend/src/app/mobile/page.tsx`; the left copyright (`© <year> UDF Party. All rights reserved.`) is retained.
- DEPLOY: backup `/root/backups/frontend-20260920T114927Z.tar.gz`; rebuilt `NEXT_PUBLIC_API_BASE=/api npm run build:mobile`; `rsync --delete frontend/out/` → `/var/www/udf/frontend` (chown root:udf 755/644). No nginx/API/mail/DB change, no reload; APK untouched. Nothing committed to git.
- VERIFY: live `/mobile/` → 200; `href="/healthz"` / `>Status<` / `>App<` count = **0**; copyright present.

## RELEASE: `/mobile` poster fit + download filename "UDF-Party App" + marketing nav alignment — DEPLOYED & VERIFIED (2026-09-20)
- Request (user, 4 parts): (1) what version does the download button serve — ensure latest; (2) download APK name should be "UDF-Party App" with the logo; (3) the "Built for the people, by the people" background must fit, not zoom; (4) the button on the (marketing) nav Tabs needs better alignment.
- VERSION (verified, no change needed): the served `/var/www/udf/downloads/udf.apk` is **versionName 1.0.4 / versionCode 5** (`com.udf.party`, targetSdk 36), sha256 `929d7585…`, byte-identical to the only release build on disk (`frontend/android/app/build/outputs/apk/release/UDF-Party V3.apk`) and to `android-overrides/app-build.gradle` (vc5/1.0.4). It IS the latest; no APK rebuild required.
- DOWNLOAD FILENAME: `deploy/nginx/udf.conf` `/downloads/` `Content-Disposition` changed `filename=UDF-Party.apk` → `filename="UDF-Party App.apk"; filename*=UTF-8''UDF-Party%20App.apk` (quoted + RFC5987 for the space). The launcher logo/icon is already the branded UDF mark. Also added the app logo icon (via `ApkDownloadButton` children + `icon-32.png`) and relabelled the /mobile hero/final buttons to "Download UDF Party App" / "Download for Android".
- POSTER FIT (`frontend/src/app/mobile/page.tsx` + `mobile.css`): the brand band used `<Image fill objectFit:cover objectPosition:'center 30%'>` on a `min(46vw,420px)` box — a 1050×1050 square illustration, so cover scaled/cropped (the "zoom"). Replaced with a `.udf-poster` band that renders the WHOLE illustration proportionally (`width/height` 1050, CSS `width:auto; height:min(46vw,340px); max-width:86vw`) on a red gradient with the caption beneath — no crop/zoom.
- MARKETING NAV (`website/assets/css/style.css`): the added "Mobile App" tab crowded the header — tabs wrapped to 2 ragged lines and the "Vote UDF" button wrapped + overflowed the bar (seen via local Chrome screenshots at 1280/1024). Set `.site-nav a { font-size:.9rem; line-height:1; white-space:nowrap; padding:9px }`, `ul gap:2px`, and a `.site-nav .nav-cta { margin-left:8px; padding:9px 15px; font-size:.82rem; line-height:1; box-shadow:3px 3px 0 var(--red); align-self:center }` → single vertically-centred row at 1280 and 1024.
- DEPLOY: backups `/root/backups/{frontend,manifesto}-20260920T114604Z.tar.gz` + nginx `/root/udf-rollback/udf-nginx-20260920T114604Z.conf`. Rebuilt `NEXT_PUBLIC_API_BASE=/api npm run build:mobile`; `rsync --delete frontend/out/` → `/var/www/udf/frontend` (chown root:udf 755/644); `rsync website/` → `/var/www/udf/manifesto`. nginx: staged conf diff vs live = ONLY the Content-Disposition line; `nginx -t` OK; `systemctl reload nginx` (no restart). `/var/www/udf/downloads/udf.apk` untouched (30,105,639 B). No API/mail/DB change.
- VERIFY: local Chrome screenshots confirmed poster (whole image, no crop) + button logo + nav single-row alignment; DOM probe confirmed `/mobile` has NO horizontal overflow (scrollWidth==viewport, no element past edge). Live: `/downloads/udf.apk` → 200 `content-disposition: attachment; filename="UDF-Party App.apk"`; `/mobile/` → 200 with `udf-poster-img` + "Download UDF Party App" (old "Download APK" 0); live `style.css` carries the `.site-nav .nav-cta` rule. Nothing committed/pushed to git; Play/AAB still gated.

## RELEASE: `/mobile` made mobile-app-only (removed web-app buttons) + top-button alignment — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "no webapp only mobile app. remove the mobile web app button. better align the buttons on top".
- `frontend/src/app/mobile/page.tsx`: deleted the hero "Open web app →" and final-CTA "Sign in on web" ghost buttons, and the footer "Sign in" link (the last `/login` references on the page); rewrote the final-CTA copy to app-only. Each download area now holds a single app-download button (nav "Get the app", hero "⬇ Download APK", final "Download for Android") — 0 remaining `/login`/web-app links. `mobile.css`: `.udf-nav-cta` now `inline-flex` centered with `white-space:nowrap` + `line-height:1`, and `.udf-cta-row` given `align-items:center` so the top buttons sit on a clean baseline.
- BUILD/DEPLOY: `NEXT_PUBLIC_API_BASE=/api npm run build:mobile` (45 pages); backup `/root/backups/frontend-20260920T112820Z.tar.gz`; `rsync -az --delete frontend/out/` → `/var/www/udf/frontend` (chown root:udf, 755/644). Marketing site (`website/`) NOT touched this round (no changes). `/var/www/udf/downloads/udf.apk` untouched (30,105,639 B). No nginx/API/mail/DB change or reload. No APK rebuild (app bundles its own separate export).
- VERIFY (external, mobile UA): `/mobile/` → 200 size 26509; web-app refs (Open web app / Sign in / href="/login") = **0**; 3 app-download buttons present; download-`<a>`-to-file count = 0 (still buttons); chain `/api/public/download/apk` → 302 → `/downloads/udf.apk`. Nothing committed/pushed to git; Play/AAB still gated.

## RELEASE: `/mobile` landing redesigned + "Mobile App" tab on the marketing site — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): improve the app-download landing with better mobile imagery/logo, explain member benefits (short highlights of patrols & engagements; tools for the community to judge true progress and accountability; rate your councillor + see progress), surface it as a "Mobile App" tab on `udf-party.co.za`, REMOVE the "Privacy & security / Protected by 3-layer encryption" section, and hide the source URL behind the download action. Clarifications chosen: (a) marketing **tab links to the existing `crm /mobile`** page (no separate marketing page), (b) member-benefit summary lives **only on the Mobile App page** (home-page copy untouched), (c) **hide the file URL behind a button**, (d) **polish existing brand assets** (no new artwork).
- MARKETING SITE (`website/`, static): added a `Mobile App` tab to the header nav (after `Councillors`) and to the footer link list (after `Ward councillors`) on all 4 pages — `index`, `housing-infrastructure`, `safety-communities`, `economy-governance` — each `<a href="https://crm.udf-party.co.za/mobile">Mobile App</a>` (2 tabs/page). Home-page copy untouched.
- APP LANDING (`frontend/src/app/mobile/`): `page.tsx` — deleted the whole Security section ("Protected by 3-layer encryption", In transit / At rest / Field-level E2EE); replaced it with an **"Accountability at your fingertips"** section (lead + 3 tool cards: Patrols & engagements, Rate your councillor, See true progress; a red banner; and the notification preview relabelled to a patrol/rating alert). Feature grid: `Members`/`Encrypted` cards replaced by `Patrols` 🚑 and `Engagements` 🤝. Hero sub + hero-meta reframed to map/patrols/rate-your-councillor (dropped the 3-layer + 100%-member-first chips). Metadata description rewritten. `ApkDownloadButton.tsx` (new `'use client'`) renders the 3 APK actions as `<button onClick→window.location.assign(APK_URL)>` instead of `<a href>` so the download URL is not exposed in the DOM/status bar; still hits the counted `/api/public/download/apk`. `mobile.css` — removed the security-band rules, added `.udf-tools/.udf-tool/.udf-tool-num/.udf-banner`, centred `.udf-notif`, added `button.udf-btn` font inherit. Existing brand images/logo reused (splash/app-icon/notification/illustration).
- BUILD: `NEXT_PUBLIC_API_BASE=/api npm run build:mobile` (static export, matches how the live CRM is built) — 45 pages, `/mobile` 26,867 B. Exported HTML confirmed: new heading present, 0 security markup, 0 `<a …download/apk>` anchors. tsc clean. NO APK rebuild (the app's bundled web code is separate; the served `/downloads/udf.apk` file was NOT touched).
- DEPLOY: timestamped backups `/root/backups/frontend-20260920T111949Z.tar.gz` + `manifesto-20260920T111949Z.tar.gz`. `rsync -az --delete` `frontend/out/` → `/var/www/udf/frontend` (chown root:udf, dirs 755 / files 644); `rsync -az` `website/` → `/var/www/udf/manifesto`. `/var/www/udf/downloads/udf.apk` untouched (30,105,639 B). No nginx/API/mail/DB change, no reload (static only).
- VERIFY (external, mobile UA): all 4 marketing pages → 200 with `Mobile App` tabs=2 each; `crm /mobile/` → 200 size 26,867 with "Accountability at your fingertips" + "Rate your councillor", security-section grep 0, download-anchor count 0; download chain `/api/public/download/apk` → 302 → `/downloads/udf.apk` → 200 `application/vnd.android.package-archive` 30,105,639 B sha256 `929d7585…` (signed 1.0.4).
- GATED / NOT DONE: nothing committed/pushed to git; Play/AAB upload still gated. The download buttons still don't pass `?v=<version>` (per-version analytics stays empty — optional). Served APK remains 1.0.4 (unchanged this release).

## FIX: app download page (`/mobile`) now delivers the APK — was serving the HTML shell — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "mobile website" / "app download page". The app-download landing ALREADY exists at `crm.udf-party.co.za/mobile` (`frontend/src/app/mobile/page.tsx`); its buttons hit `GET /api/public/download/apk` (`backend/.../analytics/publicRoutes.ts`), which counts the hit then 302-redirects to `${PUBLIC_BASE_URL}/downloads/udf.apk` (= https://crm.udf-party.co.za/downloads/udf.apk).
- ROOT CAUSE: there was NO APK on the box and NO `/downloads` nginx location, so `/downloads/udf.apk` fell through the static catch-all `try_files … /index.html` and returned the **8780-byte SPA index.html shell** — every download button silently handed back HTML instead of the app. (`apk` is also absent from nginx `mime.types`.)
- FIX: (1) added a dedicated `location /downloads/ { alias /var/www/udf/downloads/; default_type application/vnd.android.package-archive; add_header Content-Disposition "attachment; filename=UDF-Party.apk" always; … try_files $uri =404; }` to the crm vhost — served from a dir OUTSIDE the frontend export so a future frontend re-deploy never wipes it, and a MISSING file now 404s instead of silently serving the shell. (2) Placed the signed **1.0.4** APK at `/var/www/udf/downloads/udf.apk` (root:udf, 644). Repo `deploy/nginx/udf.conf` updated to match live.
- NOTE: the local build artifact `app-release.apk` had been renamed to `frontend/android/app/build/outputs/apk/release/UDF-Party V3.apk`; verified byte-identical to the 1.0.4 build (sha256 `929d7585…`, versionCode 5, signed CN=UDF Party Upload) before deploying.
- DEPLOY: nginx backup `/root/udf-rollback/udf-nginx-20260920T064931Z.conf`; staged-conf diff vs live showed ONLY the `/downloads` block added (hardening intact); `nginx -t` OK; graceful `systemctl reload nginx` (never restart — mail co-exists), nginx active, postfix/dovecot untouched. No backend/frontend/DB change.
- VERIFY (external, mobile UA): `GET /downloads/udf.apk` → **200** `application/vnd.android.package-archive`, `content-length 30105639`, `Content-Disposition: attachment; filename=UDF-Party.apk`, first bytes `PK\x03\x04` (real APK), streamed sha256 == `929d7585…` (== the 1.0.4 build). Counter endpoint `GET /api/public/download/apk?v=1.0.4` → 302 → followed to **200 / 30105639 bytes** of the APK type. `/mobile/` → 200 with 6 buttons all pointing at `/api/public/download/apk`. `GET /downloads/nope.apk` → **404** (no shell fallback).
- GATED: the APK is served for DIRECT sideload only; Play/AAB upload remains gated on explicit approval. The `/mobile` buttons don't pass `?v=<version>` (the per-version analytics breakdown stays empty until they do — optional follow-up). Nothing committed/pushed to git.

## RELEASE: website contact details updated — DEPLOYED & VERIFIED (2026-09-20)
- Request (user): "A website contact details Andy@udfp.co.za / 0642522689". Replaced the public marketing-site contact on all 4 pages (`index`, `housing-infrastructure`, `safety-communities`, `economy-governance`) — footer + Vote/contact blocks.
- CHANGES: email `jnefdt7@gmail.com` → `Andy@udfp.co.za`; phone display `062 442 6465` → `064 252 2689`; `tel:+27624426465` → `tel:+27642522689` (SA intl format, leading 0 dropped). 6 email + 6 phone occurrences (12 total), all replaced; 0 leftovers. Static HTML only — nginx/mail/certbot config and the API untouched.
- DEPLOY: timestamped backup `/root/udf-rollback/manifesto-20260920T061801Z.tar.gz`; rsync `website/` → `/var/www/udf/manifesto`. No reload needed (HTML served uncached).
- VERIFY: all 4 live pages byte-identical to local (md5 match); external curl of each page shows the new `mailto:Andy@udfp.co.za` + `tel:+27642522689` and 0 stale `jnefdt7`/`062 442 6465`/`+27624426465`. Nothing committed/pushed to git.

## RELEASE: hide public `/councillors` page + rebuild signed APK 1.0.4 with map verified — DEPLOYED & BUILT (2026-09-20)
- Trigger: user "disable the counsilor page from live udf-party.co.za. upadet apk and build ensure the [map] is correct", then narrowed with "only hide https://udf-party.co.za/councillors". Chosen scope: take ONLY the standalone `/counselors` page offline, KEEP the 26-card roster on the homepage, and repoint every inbound link to the homepage roster anchor so nothing dead-links. The `frontend/` APK is independent of the marketing site (site edits do not enter the app).
- SOURCE (`website/`): deleted `website/councillors.html`; in the 4 remaining pages replaced all `href="/counselors"` → `href="/#counselors"` (10 links: index 3, housing 2, safety 2, economy 3). Homepage inline `#counselors` section (26 `council-card`) + its `data-cms-candidates` hydration hook left untouched. `/api/public/candidates` and CRM `/crm/candidates` intentionally unchanged (admin still needs the feed).
- DEPLOY (static marketing site): timestamped backup `/root/udf-rollback/manifesto-20260920T060011Z.tar.gz`; rsync `website/` → `/var/www/udf/manifesto`, then removed the orphaned live `councillors.html`. No nginx change/reload — `try_files … =404` returns 404 once the file is gone. Backend/mail/nginx services untouched; migration head stays **050**.
- VERIFY (external curl + on-box md5): `/counselors` → **404**; `/` + 3 pillar pages → 200; leftover `href="/counselors"` = 0 on every page, repointed `href="/#counselors"` counts match source (3/2/2/3); homepage roster = 26 `council-card` + `id="counselors"` intact; all four deployed pages byte-identical to local (md5 match). Rollback = restore that tarball.
- APK 1.0.4 (closes the top map section's "no APK rebuild" gap): bumped `frontend/android-overrides/app-build.gradle` versionCode 4→5 / versionName 1.0.3→1.0.4 (the override is the source of truth; `android:assets` regenerates `android/app/build.gradle`), then `npm run apk:release` (check-caps → `build:mobile` with `NEXT_PUBLIC_API_BASE=https://crm.udf-party.co.za/api` → `cap:sync` → `android:assets` → Gradle `:app:assembleRelease :app:dumpReleaseManifest`) under `JAVA_HOME=…/openjdk@17`. BUILD SUCCESSFUL.
- APK FACTS: `frontend/android/app/build/outputs/apk/release/app-release.apk`, 30,105,639 bytes, sha256 `929d7585f7f8d233b8fa1c67efc84de7f870ed9ca80415156791967875485c4c`; badging versionCode 5 / versionName 1.0.4, minSdk 22, targetSdk 36, label "UDF Party", NOT debuggable; apksigner verified signed by **CN=UDF Party Upload, O=UDF Party, C=ZA** (cert SHA-256 `0744136b…`); production API base `https://crm.udf-party.co.za/api` present in the bundled assets.
- MAP-CORRECT VERIFY ("ensure the map is correct"): the WebView serves its OWN bundled export (`android/…/assets/public`), so confirmed the shipped map code equals the live, visually-verified web map. Two map chunks are BYTE-IDENTICAL mobile-vs-web: palette `9042-83a3559a3df49ff7.js` md5 `5bf04a8a…` (holds SUBCOUNCIL/ward hex palette `#0f766e`/`#be185d`/`#7d3c98`/`#1f77b4`) and component `5369-8c6b2740ef991eb6.js` md5 `3740ef2e…` (holds `#7f1020` stroke / zoom-label code). Same source as the web build (only the API-base env differs, absent from these chunks); the web render was already confirmed for distinct region/subcouncil/ward colours (4/12/20) and labels growing +131% on zoom — so the mobile map is correct.
- GATED / NOT DONE: APK NOT uploaded to Play (no AAB/`bundleRelease`) — remains gated on explicit approval; nothing committed/pushed to git. No on-device/emulator install smoke run performed — verification is APK-code identity + the prior live web render.

## DEPLOY: map layer colours + zoom-label fix to web, and edge hardening (HSTS + CSP + dotfile-deny) — DEPLOYED & VERIFIED (2026-09-20)
- Trigger: user said "add the colours to the region, subc, ward. do a and b." — (1) give the map's region/subcouncil/ward layers distinct colours, (a) build+deploy the map fix to web, (b) apply the hardening items from the URL-leak-test section below. This SUPERSEDES the "NOT yet deployed" status of the map zoom-label section and the "recommendations only" status of hardening items (1)-(3) below — all are now LIVE on `crm.udf-party.co.za`.
- COLOURS (code, `frontend/src/components/UdfStaticMap.tsx` `fillOf`): region = `mixWhite(regionColor(code),0.25)` (hashed → 4 distinct); subcouncil = `mixWhite(SUBCOUNCIL_COLORS[index-by-sorted-code],0.35)` (new `SUBCOUNCIL_INDEX` map → 12 distinct); ward = `mixWhite(wardColorForCode(code),0.25)` (hashed → 20 distinct; an index-based scheme was rejected — only 8 distinct citywide). White label halo retained for legibility. Palette source of truth: `frontend/src/lib/taxonomy.ts`. typecheck clean.
- FRONTEND DEPLOY (a): rebuilt the static export off-box (`NEXT_PUBLIC_API_BASE=/api npm run build:mobile`), re-tarred ONLY `udf-frontend.tar.gz` (backend tarball preserved unchanged), shipped via `ship.sh`, on-box extracted into `/var/www/udf/frontend` via a staged swap (rollback backup `/root/udf-rollback/frontend-20260920T051615Z.tar.gz`; ownership `root:udf`, world-readable). Backend/uploads/nginx-service/mail untouched; migration head stays **050**; no APK rebuild.
- DEPLOY VERIFY (byte-identity): deployed `index.html` md5 `4cf74926f2be9b9a7ad90de37cfc71d4` and app chunk `page-f0de41db3774eaef.js` both match the local build exactly (prior live was `2dd8e724…` / `page-ae2f7c4f62373f1e.js`); 35 crm route dirs present.
- LIVE MAP VERIFY (read-only DOM, authenticated session, `/crm/heatmaps`): map SVG renders, toggle buttons Region/Subcouncil/Ward, NO console errors. Distinct path fills = region **4** (5 paths), subcouncil **12** (20 paths), ward **20** (116 paths) — matches local counts. Zoom test (dispatched wheel): viewBox `w` shrank 1000 → 432.3 → 186.9 while RENDERED label size grew 8.13px → 12.37px → 18.81px (+131%); the `font-size` ATTRIBUTE (19 → 12.49 → 8.21, SVG user-units) matches `labelZoom = clamp(√(VIEW_W/view.w),1,3.2)` exactly, confirming numbers now GROW on zoom-in.
- HARDENING (b) — `deploy/nginx/udf.conf` crm vhost (installed to `/etc/nginx/sites-available/udf`, `nginx -t` OK, graceful reload; backup `/root/udf-rollback/udf-nginx-20260920T051719Z.conf`): (1) **HSTS** `Strict-Transport-Security: max-age=31536000; includeSubDomains` added at nginx as the single source for the whole vhost (backend `ENABLE_HSTS` stays false so no duplicate; helmet only covered `/api`). (2) **CSP** at nginx for the static export: `default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'; form-action 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; font-src 'self' data:; media-src 'self' blob:; connect-src 'self'; upgrade-insecure-requests`. `'unsafe-inline'` for script/style is REQUIRED — the Next.js App Router injects inline RSC scripts (`self.__next_f.push`) and React emits inline `style=` attrs; without it hydration breaks. `proxy_hide_header` strips helmet's CSP/HSTS on `/api`+`/healthz` so clients see exactly one copy. (3) **dotfile-deny** `location ~ /\. { return 404; }` before the SPA catch-all so `/.env`, `/.git/*` etc. get a clean 404 instead of the 200 index.html shell.
- HARDENING VERIFY (live, forced-SNI curl + browser): static `GET /` returns HSTS + CSP (single copy each) with the existing nosniff/XFO DENY/Referrer-Policy; `/.env` and `/.git/config` → **404**; `/`, `/login/`, `/healthz`, `/api/posts`, and the new chunk all → 200; `/api` CSP header count = 1 (no dupe). Browser: hydration succeeds on `/` and `/login`, all 21 JS chunks + 2 CSS + brand image → 200, **zero CSP violations**; a synthetic probe confirmed cross-origin fetch/script are BLOCKED while inline scripts still run. udf-api/nginx/postfix/dovecot all still active (mail untouched).
- NOTE: `/login` → 301 → `/login/` (200) is pre-existing nginx directory-redirect behaviour, unchanged. The doubled `X-Frame-Options`/`Referrer-Policy` on `/api` JSON (helmet SAMEORIGIN/no-referrer + nginx DENY/strict-origin) are pre-existing and harmless on non-rendered JSON; left as-is. A verification curl run microseconds after `nginx reload` briefly showed the OLD headers (worker reload race) — a delayed re-check confirmed the new ones; do not treat an immediate post-reload curl as authoritative.
- NOT DONE / gated (at the time of writing): no APK rebuild — the installed app serves the map from its OWN bundled export, so it needs a rebuild+release to pick up colours/zoom (Play/AAB upload remains gated on explicit approval). Apex `website/` + webmail vhosts unchanged. Nothing committed/pushed to git. UPDATE 2026-09-20: the APK HAS since been rebuilt to versionCode 5 / 1.0.4 with the map verified byte-identical to web — see the top section.

## Fix: map ward/subcouncil numbers must get BIGGER when zooming in — code done & verified locally, DEPLOYED to web 2026-09-20 (see the top section)
- SYMPTOM (user): "when on the map and zoom in the ward and subcoincil numbers must get biger not smaller." Zooming into the UDF map left the ward/subcouncil number labels at a constant on-screen size, so as the geography magnified the numbers read as shrinking (tiny specks once zoomed to a ward).
- ROOT CAUSE: `frontend/src/components/UdfStaticMap.tsx` set the label `fontSize={view.w * 0.019}`. The SVG `viewBox` maps `view.w` view-units onto the frame's FIXED pixel width, so a font proportional to `view.w` renders at a CONSTANT pixel size at every zoom (the same trick as `vector-effect:non-scaling-stroke`). Constant-on-screen = shrinking relative to the enlarging map.
- FIX: multiply the label font (and its white halo stroke) by `labelZoom = clamp(sqrt(VIEW_W / view.w), 1, 3.2)`, so the numbers GROW up to ~3.2× as the reader zooms in, the fully-zoomed-out map (zoom 1) is unchanged, and a label can never balloon past its shape. `MapTab`/`FullMapTab`, `MemberMapTab` and `/crm/heatmaps` all mount this single renderer, so one change covers every map surface.
- VERIFIED: `npm run typecheck` clean; `npm run lint` shows only the 6 pre-existing warnings (none in UdfStaticMap). A standalone render harness (`.tmp-verify/zoom-label-render.mjs` → `.html`, screenshot `.tmp-verify/zoom-label-render-fullpage.png`) using the REAL `udfMapData.json` geometry shows OLD = 5.3px at 1×/3×/8× (specks at 8×) vs NEW = 5.3px → 9.2px → 15.0px, labels sitting inside their magnified wards with no severe overflow.
- STATUS: code change only at the time of writing. UPDATE 2026-09-20: DEPLOYED to the web CRM (static export rebuilt + shipped; byte-identical chunk and live DOM verified) together with the new layer colours — see the top section. The installed APK still needs a rebuild to pick it up. Nothing committed/pushed to git.

## Read-only URL leak test against production — NO sensitive leak found; hardening items noted (2026-09-19)
- REQUEST (user): "run a web url test to see if there is any leak via url." Ran `.tmp-verify/url-leak-test.sh` — an external, READ-ONLY probe (GET/HEAD/OPTIONS/TRACE; no auth, no brute force, no exploitation) of `crm.udf-party.co.za`, `udf-party.co.za` and the raw IP.
- NO LEAK — protected surface: every member/CRM/geo/analytics/moderation/audit endpoint returned **401 "Authentication required"** unauthenticated (`/api/members`, `/api/members/me`, `/api/geo/*`, `/api/audit`, `/api/crm/*`, `/api/crm/superadmin/*`, `/api/moderation`, `/api/analytics`, `/api/id-card`, `/api/scorecards`, `/api/recruitment`, …). Guards fail closed (`middleware/authorize.ts`: `if (!req.principal) return unauthorized`). The two non-401 results are by design and verified in code: `GET /api/posts` is the public newsroom (published-only; the D41 clamp forces anonymous `?status=draft|taken_down` to zero rows and `?status=all` to published — confirmed live, all `total:0`), and `GET /api/attachments` is `optionalAuthenticate` (anonymous for published parents only; the 400 was a missing required `parentType/parentId`).
- NO LEAK — public feeds carry no PII: `/api/public/candidates` (the roster) has no `email`/`phone`/`mobile`/`idNumber`/`identity`/`token`/`password`/`msisdn` keys. `/healthz` returns only `{status,db}`.
- NO LEAK — files/config: `/.env`, `/.env.*`, `/.git/*`, `/secrets/*`, `/package.json`, `/*.js.map`, `/uploads/`, `/backups/`, `/.ssh/*` all return the SAME 8780-byte SPA `index.html` shell (nginx `try_files … /index.html`), i.e. the files are absent from the webroot and nothing real is served; no source maps exposed. The apex site 404s these correctly. No directory listing anywhere.
- NO LEAK — CORS/headers/errors: evil + `null` Origins get NO `access-control-allow-origin` (allowlist works; the apex origin is echoed as intended); error/404 bodies contain no stack traces or internal paths; `x-powered-by`/`server` absent; `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy` present; unknown `Host` dropped (444); plain HTTP 301→HTTPS; `TRACE` 405 (no XST); path traversal normalises and stays auth-gated.
- HARDENING (UPDATE 2026-09-20: items (1)-(3) below were APPROVED by the user ("do a and b") and APPLIED to the nginx crm vhost + verified live — see the top section. Original findings/recommendations retained for history): (1) **HSTS absent** (`strict-transport-security` missing) although a valid Let's Encrypt cert now exists — enable `ENABLE_HSTS=true` AND add the header at nginx, because helmet only covers `/api`+`/healthz`, not the static pages. (2) **CSP absent** on the static CRM + apex pages (helmet's CSP only reaches API responses). (3) The CRM catch-all returns **200** for dotfiles/sensitive paths — add `location ~ /\. { deny all; }` so scanners get 404/403 and a stray sensitive file could never be served. (4) Minor: malformed JSON to `/api/auth/login` yields a generic 500 (should be 400); body is clean, no leak.
- Nothing committed/pushed to git; no production change was made by this test.

## Fix: ward-councillor photo not loading in the MOBILE APP (CORP same-site) — DONE on production (2026-09-19)
- SYMPTOM (user): "in the mobile app the Ward Councilor image is not pull through. the bio text is pull correctly." In the Capacitor Android app a ward councillor's bio (JSON text) rendered but the photo did not.
- ROOT CAUSE (distinct from the apex CORS fix below): the member-facing views `frontend/src/components/RegionSheet.tsx` (~line 252) and `WardTransparency.tsx`'s `LeaderPhoto` (~line 70) render the photo as a DIRECT cross-site `<img src="${API_BASE}/public/media/${photoId}">`. The app's WebView origin is `http://localhost` (capacitor.config.ts `server.androidScheme:'http'`), which is cross-SITE with `crm.udf-party.co.za`. Helmet (`backend/src/app.ts`) sets a global `Cross-Origin-Resource-Policy: same-site`, so the browser BLOCKED the cross-site `<img>`. The bio worked because it arrives via the JSON fetch and `http://localhost` IS in `CORS_ORIGINS` (CORS governs fetch; CORP governs `<img>` embedding — different mechanisms). The CRM admin preview worked because `CandidatePhotoEditor`/`CoverEditor` use the authenticated `coverBlob()` fetch→blob-URL pattern, which bypasses CORP.
- FIX (backend code, NO APK rebuild): every PUBLIC no-auth image handler now overrides the header for its own response only — `res.setHeader('Cross-Origin-Resource-Policy','cross-origin')` in `backend/src/modules/public/routes.ts` (`GET /public/media/:id`) and `backend/src/modules/candidates/publicRoutes.ts` (`GET /public/candidates/:id/photo`); and, because the identical bug class affects the app's other direct cross-site `<img>` loads (`EventSheet` cover, homepage `Slider` / CMS thumbs via `cmsMediaUrl`), also `backend/src/modules/events/routes.ts` (`GET /events/:id/cover`) and `backend/src/modules/content/routes.ts` (`GET /public/content-media/:id`). Semantically correct: these images are public and meant to be embeddable anywhere. Because the app loads images from the live API at runtime, the INSTALLED app picks this up with no rebuild/update. (Blob-pattern thumbs — `MediaThumb`, `CandidatePhotoEditor`, `CoverEditor`, CRM avatars, `AttachmentManager` — fetch via CORS then render a same-origin object URL, so they were never affected.)
- DEPLOY: rebuilt the backend off-box (tsc clean, 50 migrations bundled, CORP fix confirmed compiled into both dist files), shipped via `ship.sh`, and on-box swapped ONLY `dist/` + package files — deliberately preserving `/opt/udf/backend/uploads/` (2 user media files incl. Ashley's photo) and `node_modules/` (deps unchanged → no npm ci, no migration; head stays **050**). env gate OK, `chown -R udf:udf`, `systemctl restart udf-api` → active, NRestarts=0, healthz ok.
- VERIFIED (live through nginx, simulating `Origin: http://localhost`): `GET /api/public/media/7eb84910-…` (a published leader photo) → HTTP 200 image/jpeg with `cross-origin-resource-policy: cross-origin` (+ `access-control-allow-origin: http://localhost`); `GET /api/public/candidates/<ashley>/photo` → 200 image/jpeg, `cross-origin-resource-policy: cross-origin`, 61970 bytes (uploads preserved). Pre-fix both were `same-site`. All four handlers were confirmed compiled with the override (1 hit each) and redeployed via the same dist-only swap (uploads 2→2 preserved, head stays **050**, NRestarts=0, healthz ok); `/events/:id/cover` and `/public/content-media/:id` had no published rows on the box to live-test but carry the identical change.
- NOTE for the user: a previously CORP-blocked load is not cached, so reopening the councillor screen (or restarting the app) fetches the photo fresh with the new header; the photo endpoint is `max-age=3600`. Nothing committed/pushed to git.

## Fix: councillor photos not appearing on udf-party.co.za (apex CORS) — DONE on production (2026-09-19)
- SYMPTOM (user): "i add a photo of a councilor and it is not pull to the static udf-party.co.za". A headshot uploaded in `/crm/candidates` for ASHLEY CRAIG CLOETE (id `c0000000-0000-4000-8000-000000000007`) did not render on the public marketing site.
- DIAGNOSIS: backend was correct — `GET /api/public/candidates` returned `hasPhoto:true` for him and `GET /api/public/candidates/<id>/photo` served HTTP 200 `image/jpeg` (62 KB, 960×1280). A fresh-browser DOM inspection of `https://udf-party.co.za/councillors` showed ALL 26 cards were still the static empty `<div class="council-photo">` fallback (0 `<img>`), and the console showed `net::ERR_FAILED` **CORS** blocks on `/api/public/candidates`, `/api/public/content/marketing.candidates` and `/api/public/collect`. ROOT CAUSE: the apex origin `https://udf-party.co.za` was missing from `CORS_ORIGINS`, so `udf-content.js`'s cross-origin fetch of the roster was blocked and hydration never ran — the public cards silently stayed name-only with no photos. (This had been masking ALL CMS-driven apex content, not just photos; the trimmed lede only appeared because it is also baked into the static HTML.)
- FIX (server config, no code change): appended the apex + www origins to line 28 of `/etc/udf/udf-api.env` → `CORS_ORIGINS=https://crm.udf-party.co.za,https://udf-party.co.za,https://www.udf-party.co.za,https://102.68.98.129,http://localhost`; preserved `root:udf 640`; `systemctl restart udf-api` (active). Env backed up first to `/root/backups/udf-api.env.pre-cors.2026-09-19-190328`.
- VERIFIED (live): `curl -H 'Origin: https://udf-party.co.za' …/api/public/candidates` now returns `access-control-allow-origin: https://udf-party.co.za` (+ `vary: Origin`, credentials true). Fresh-browser re-check of `/councillors` and the homepage `#councillors`: Ashley's card is now an `<img class="council-photo">` (src = the photo endpoint, `naturalWidth×Height` 960×1280, `complete=true`, rendered 92×92 circle); tally 1 `<img>` / 25 empty `<div>`; all three `/api/public/*` requests HTTP 200; zero console/CORS errors.
- DURABILITY: `deploy/production.env.example` + `deploy/RUNBOOK.md` (domain env section) updated to include the apex/www origins with an explanatory note + a verify one-liner, so a re-provision won't regress it. NOTE: the 7-day `expires` on `assets/js/*.js` means a browser that cached the OLD `udf-content.js` still works (photo logic unchanged); the candidates list is `max-age=60`, so new photos appear on the apex within ~1 min + a refresh. Nothing committed/pushed to git.

## Promote carlmarks397@gmail.com to Superadmin — DONE on production (2026-09-19)
- User request: "Make carlmarks397@gmail.com Superadmin" (a password was supplied in-chat; it is deliberately NOT recorded here, per the no-credentials-in-progress-record rule).
- PRE-STATE (read-only lookup by blind index): the account already existed — id `44651db6-e978-4551-921b-d4e59e2d2b69`, role `national_admin`, scope `region_codes=["EAST"]`, active, `must_change_password=false`. There was NO superadmin on the box before this (role counts: member 7, national_admin 2, ward_councillor 5).
- ACTION (server-side node script under `/opt/udf/backend`, env sourced from `/etc/udf/*.env`, password passed via an env var — never hardcoded): promoted to `role=superadmin`; set `password_hash` to an argon2id hash of the operator-supplied password via the app's own `hashPassword`; `region_codes=[]` (national scope), `ward_code=NULL`, `is_active=TRUE`, `moderation_status='active'`, `suspended_until=NULL`, `must_change_password=FALSE`, `tc_version=TERMS_VERSION` (pre-accepted), `token_version+1`. Revoked 7 live refresh tokens (password-change hygiene). This mirrors `seed:admin`'s field set but also resets the password (seed:admin leaves an existing account's password unchanged).
- VERIFIED: in-script `verifyPassword(hash, pw)` = true; `SUPERADMIN_COUNT_NOW=1`. Real end-to-end `POST /api/auth/login` → LOGIN OK, `role=superadmin`, `mustChangePassword=false`, `tcAccepted=true`, 45 permissions, `enabledModules` includes `superadmin`, ops perms present (`platform:read`, `analytics:read`, `content:manage`). The CMS editor (`/crm/website`) and Ward Councillors admin (`/crm/candidates`) are now reachable by this account.
- SECURITY NOTE: because the password transited chat, the user should rotate it after first sign-in (CRM → change password). No credential stored in this record; nothing committed/pushed to git.

## Clean URLs for the marketing site: dedicated /councillors page + drop index.html — DEPLOYED & VERIFIED (2026-09-19)
- User request: `https://udf-party.co.za/index.html#councillors` should be `https://udf-party.co.za/councillors`, and `.../index.html` should be `https://udf-party.co.za`. Also asked why the CMS/CRM changes were "not showing".
- Created `website/councillors.html` — a standalone page (same header/nav/footer + the 26-card roster section extracted verbatim from the homepage, keeping the `data-cms-candidates` hydration hook). nginx already serves it at `/councillors` via `try_files $uri $uri.html $uri/ =404` — NO nginx change/reload needed.
- Normalised EVERY internal link across all 5 marketing pages to clean, extensionless, root-absolute URLs: `index.html`→`/`; `index.html#councillors` / `#councillors` / `economy-governance.html#councillors`→`/councillors`; `index.html#vote`→`/#vote`; pillar cross-links `housing-infrastructure.html`→`/housing-infrastructure` (and safety/economy), including their `#section` fragments. Applied by `.tmp-verify/fix-councillors-url.mjs` (generates the page + ordered regex rules; idempotent).
- Deployed via `rsync -az website/ → /var/www/udf/manifesto` (new `councillors.html` + 4 modified pages).
- VERIFIED (live): `/councillors` HTTP 200 (26 name-only cards, title "Ward Councillors …", `aria-current` on the nav item, `data-cms-candidates` hook, 0 council-role / 0 council-wards); `/`, `/housing-infrastructure`, `/safety-communities`, `/economy-governance` all HTTP 200; 0 stale `href="index.html…"` anywhere; even the OLD `/index.html` URL now serves the updated name-only cards (26 cards, 0 council-role).
- "CMS/CRM not seeing changes" — RESOLVED as browser cache: server-side is fully current. CRM `/crm/candidates` serves the freshly-built chunk `page-56d03d6dd5e2890e.js` (byte-identical name to the local build), the public API returns 26 items with 0 roleLabel / 0 wardsLabel, and apex HTML is uncached (ETag/Last-Modified only, assets 7d). A hard refresh (Cmd+Shift+R) or the new `/councillors` URL shows the trim. NOTE: the CRM admin TABLE still shows the "Role"/"Wards" COLUMNS (rendering `—` for the now-blank values) — only the DATA was blanked (migrations 048/049), the columns/fields were intentionally kept so editors can re-add labels; removing the columns entirely is a separate change if wanted.

## PRODUCTION DEPLOY — councillor trim (migrations 048-050) + frontend + apex website + APK 1.0.3 — DEPLOYED & VERIFIED (2026-09-19)
- Trigger: user said "Deploy and update the Apk release" (explicit approval). This SUPERSEDES the "LOCAL, NOT DEPLOYED" status of the three sections immediately below (048 / 049 / 050) — those changes are now LIVE in production.
- PRE-DEPLOY STATE (re-verified over SSH before touching anything): prod migration head was **047** (NOT 030 — that earlier line-3 claim was stale; the 045→047 roster work was already live), `ward_candidates`=26, postfix/dovecot/opendkim/nginx/udf-api all active, healthz `{status:ok,db:true}`, 15G free. So 048/049/050 were the exact delta.
- BACKUP: `sudo -u postgres pg_dump -Fc udf` → `/root/backups/udf-pre050-2026-09-19-174537.dump` (4.2 MB, valid `pg_restore -l` header) taken before migrating.
- ARTIFACTS: rebuilt off-box via `API_BASE=/api build-offbox.sh` (backend tarball carries all 50 migrations incl. 048/049/050, 0 AppleDouble sidecars; frontend export includes `crm/candidates/`), shipped with `ship.sh` to `/root/udf-deploy`.
- BACKEND DEPLOY (RUNBOOK §13): clean extract to `/opt/udf/backend` (no `uploads/` existed to preserve), `npm ci --omit=dev` (0 vulns), env dry-run OK, `node dist/db/migrate.js` applied 048→049→050 ("Migrations complete"), `chown -R udf:udf`, `systemctl restart udf-api`. Post: head **050**, udf-api active, healthz ok.
- FRONTEND + WEBSITE: frontend static export re-extracted to `/var/www/udf/frontend` (185 files, `crm/candidates/index.html` present, root:udf 755/644); `rsync -az website/` pushed exactly the 2 changed files (`index.html`, `assets/js/udf-content.js`) to the apex manifesto webroot `/var/www/udf/manifesto`.
- POST-DEPLOY VERIFY (live): `GET https://crm.udf-party.co.za/api/public/candidates` → 26 items, **0** non-empty roleLabel, **0** non-empty wardsLabel (sample `ANDHOR GREY MARKS {roleLabel:"",wardsLabel:"",hasPhoto:false}`); `GET /api/public/content/marketing.candidates` → lede = trimmed text, publishedAt 2026-09-19T15:47:04Z; apex `https://udf-party.co.za/` HTTP 200 with **0** party-list / **0** accountable-for / **0** council-role / **0** council-wards across 26 name-only `council-card`s; postfix/dovecot/opendkim/nginx/udf-api all active, udf-api NRestarts=0, no priority≤3 journal errors; CRM index + `/crm/candidates/` both HTTP 200; legacy no-SNI IP cert intact (`CN=102.68.98.129`).
- APK REBUILD: bumped `android-overrides/app-build.gradle` (the source-of-truth template that `prepare-android-assets.mjs` copies over `android/app/build.gradle`) from vc3/1.0.2 → **vc4 / 1.0.3**; built via `apk:release` (needed `JAVA_HOME=/opt/homebrew/opt/openjdk@17/...` — AGP requires Java 17, default shell was Java 11). Artifact `frontend/android/app/build/outputs/apk/release/app-release.apk`: applicationId `com.udf.party`, **versionCode 4 / versionName 1.0.3**, NON-debuggable, targetSdk 36, 30,105,546 bytes, sha256 `f37c6a307bdf81657a3d2c885480f3ffc336cf6084852b063dd9fbc099ce464a`, signed `CN=UDF Party Upload, O=UDF Party, C=ZA`, apksigner v1+v2 verified (exit 0), API base `https://crm.udf-party.co.za/api` baked in.
- GOTCHA (recorded): editing `android/app/build.gradle` directly does NOT stick — `cap:sync`/`android:assets` regenerates it from `android-overrides/app-build.gradle`; the first APK came out as vc3, fixed by bumping the override and re-running `android:assets` + gradle.
- NOT DONE / gated: the APK was built + signed locally and is NOT uploaded to Play (no `:app:bundleRelease`/AAB, no Play Console upload) — that remains deploy-gated on explicit approval. 26 bios/headshots still empty (CRM content task). Nothing committed/pushed to git this session.

## Remove remaining "Party list #" refs + trim the councillor-section lede — LOCAL, NOT DEPLOYED (2026-09-19)
- User request: "remove Party list # from all and in the text: — listed in party-list order, with the wards each is accountable for." Card `role_label` values were already blanked by 048, so this targets (a) the section lede clause and (b) the leftover "Party list #" example strings in code.
- Migration `050_marketing_candidates_lede.sql` (head now **050**): rewrites the `marketing.candidates` CMS block's `sectionLede` (draft + published) via `jsonb_set` — removes "— listed in party-list order, with the wards each is accountable for", leaving "The candidates standing for the United Democratic Front Party across Cape Town wards at Local Government 2026." Bumps version 1→2 and snapshots into `content_block_versions` (v1 old text preserved, v2 new). Idempotent (guarded by a text equality check + ON CONFLICT). kicker/title untouched.
- `website/index.html`: static `sectionLede` fallback updated to the same trimmed wording.
- Code refs neutralised: `frontend/src/app/crm/candidates/page.tsx` edit-form label "Role label (e.g. Party list #1)" → "Role label (optional)"; `frontend/src/types.ts` roleLabel doc comment no longer cites "Party list #1". Migration-file comments (045/047/048/049) left as historical record.
- VERIFIED (local): `GET /api/public/content/marketing.candidates` → lede = trimmed text; `website/index.html` has 0 `party-list`/`accountable for` matches; frontend `tsc --noEmit` exit 0; DB head `050`.
- NOT DONE / gated: no commit/ship/deploy; same production-state caveat as the 048 section below.

## Strip councillor cards to name-only: remove ward numbers, "Regional list" + initials — LOCAL, NOT DEPLOYED (2026-09-19)
- User request: "Also remove the ward numbers and Regional list and initials" (follows the role-label removal below). Result: each public councillor card now shows ONLY the name plus an empty photo slot. Kept the photo circle itself as the headshot slot; only its initials text was removed. WEBSITE-ONLY roster; nothing committed/pushed/shipped/deployed.
- Migration `049_ward_candidate_remove_wards_label.sql` (head now **049**): idempotent `UPDATE ward_candidates SET wards_label='' WHERE wards_label<>''`, which also clears POGGENPOEL's "Regional list". Column kept; only seeded values blanked. Applied via `npm run migrate` → "Migrations complete".
- `website/index.html`: removed all 26 `<p class="council-wards">…</p>` lines and emptied the 26 `<div class="council-photo" aria-hidden="true">XX</div>` placeholders to `…></div>` (no initials). Cards are now `photo-slot + <h3>name</h3>`.
- `website/assets/js/udf-content.js`: deleted the now-unused `initials()` helper and the `ph.textContent = initials(...)` line so hydrated (live-API) cards also render an empty photo slot. The existing `if (c.wardsLabel)`/`if (c.roleLabel)` guards already suppress those lines when blank. `node --check` passes.
- VERIFIED (local): public `GET /api/public/candidates` (:4000) → 26 items, 0 non-empty roleLabel, 0 non-empty wardsLabel, POGGENPOEL `{roleLabel:"", wardsLabel:""}`; `website/index.html` has 0 `council-wards`, 0 `council-role`, 0 non-empty initials placeholders; only remaining "initials" token in the JS is a comment.
- NOT DONE / gated: no commit/ship/deploy; same production-state caveat as the 048 section below (reconfirm prod migration head before shipping 048/049).

## Remove "Party list #N" role label from councillor roster — LOCAL, NOT DEPLOYED (2026-09-19)
- User request: "remove the party list from councillor". Clarified via question → option "Remove role label on all cards": keep all 26 councillor cards but drop the `Party list #N` / `UDF candidate` role-label line from every card, leaving name + ward numbers only (POGGENPOEL keeps `Regional list` on its wards line). WEBSITE-ONLY roster, as before; nothing committed/pushed/shipped/deployed.
- Migration `048_ward_candidate_remove_role_label.sql` (head now **048**): single idempotent `UPDATE ward_candidates SET role_label='' WHERE role_label<>''`. The `role_label` column is kept (editors can still set one from `/crm/candidates`); only the seeded values are blanked. Applied locally via `npm run migrate` → "Migrations complete"; DB now 26 rows, 0 non-empty role_label, head `048`.
- Static fallback: removed all 26 `<p class="council-role">…</p>` lines from `website/index.html` so the no-JS / pre-publish / CMS-down markup matches (cards = photo/initials + name + wards). JS hydration unchanged — `udf-content.js` already guards `if (c.roleLabel)`, so an empty label renders no role line; CRM table already shows `—` for empty role.
- VERIFIED (local): public `GET /api/public/candidates` (:4000) returns 26 items with `roleLabel:""` for all (0 non-empty), `wardsLabel` ranges intact, POGGENPOEL `{role:"", wards:"Regional list"}`; `website/index.html` has 0 `council-role` lines.
- NOT DONE / gated: no commit/ship/deploy. Deploy of this change to production depends on the prior 045–047 roster work already being live on the box; production deploy state was NOT re-verified this session (a "deploy now" was in flight when the request pivoted) — reconfirm prod migration head before shipping 048.

## Ward-councillor roster refresh (real IEC ward numbers) + local release APK rebuild — LOCAL, NOT DEPLOYED (2026-09-19)
- User request: pasted the authoritative IEC LGE2026 Western Cape per-ward table (118 rows: IEC 8-digit order number / local ward / subcouncil / UDF candidate) and asked that each councillor card keep 1 image with the ACTUAL ward numbers listed below it, and to "update the APK to be live with the changes". Clarified via questions: roster stays WEBSITE-ONLY (no new in-app view), ward numbers shown as COMPACT RANGES, and BUILD LOCALLY — do not deploy. Stacks on the 045/046 roster feature and the pre-deployment audit below; nothing committed, pushed, shipped or deployed; production still at migration 030.
- Migration `047_ward_candidate_ward_numbers.sql` (head now **047**): 25 idempotent `UPDATE ward_candidates SET wards_label=…` (fixed seed ids, safe to re-run) rewriting the generic "NN wards" counts into the real compact ward ranges decoded from the IEC extract (order − 19100000 = ward 1..118). 118 rows → 116 ward slots → 25 distinct candidates; wards **61 & 66** have no UDF candidate (skipped); **MICHAEL JACK POGGENPOEL** (party-list #13) is list-only → deliberately left unchanged ("Regional list"). Range format: run ≥3 → "a–b" (en-dash), pair → "a, b", lone → "Ward n". Longest label **101 chars < 160** schema cap — no widening needed. Generated mechanically from the source CSV (`.tmp-verify/build-ward-ranges.mjs`), not hand-transcribed.
- Static fallback cards regenerated in `website/index.html` (all 26 `.council-card` inside `data-cms-candidates`, via `.tmp-verify/regen-council-cards.mjs`) so the no-JS / pre-publish / CMS-down markup equals the live API. Card structure preserved (photo/initials + h3 + role + wards) and the `reveal` class kept on these STATIC cards (hydrated cards still omit it).
- VERIFIED (all local): DB + public `GET /api/public/candidates` return the new ranges — 26 items, max `wardsLabel` 101, POGGENPOEL still "Regional list", ZERO legacy count-style labels (e.g. ANDHOR GREY MARKS → "Wards 2, 3, 5–13, 15–19, 23, 31, 33, 35, 39–41, 50, 52, 54, 55, 59, 91, 93–97, 99, 101, 103, 117, 118"; ROGER STEVE ROMAN → "Ward 72"); served marketing HTML correct; browser DOM 8/8 (ranges render + wrap, scrollWidth==clientWidth, 0 `.reveal` on hydrated cards, initials present); fresh scratch DB `001→047` applies clean (47 migrations); `check-cms-hooks` still lossless; candidates CRUD smoke unaffected.
- RELEASE APK REBUILT LOCALLY (no deploy): `npm run apk:release` → caps:check ✓ + `NEXT_EXPORT=1 next build` ✓ + cap sync + `gradlew :app:assembleRelease` → **BUILD SUCCESSFUL in 1m 6s**. Artifact `frontend/android/app/build/outputs/apk/release/app-release.apk`: applicationId `com.udf.party`, **versionCode 3 / versionName 1.0.2**, NON-debuggable, targetSdk 36, 30,105,523 bytes, sha256 `73497dc2b4e5f3469d2e28959e749924bc16d4a70742a4f7b79e6a8dacd26771`. Signed with the APPROVED UPLOAD cert (`CN=UDF Party Upload, O=UDF Party, C=ZA`; cert sha256 `0744136ba322327792b70495877024a9238e08e218418ea939577a4adb66d797` == `udf-upload-certificate.pem` fingerprint); apksigner v1+v2 verified. Production API base `https://crm.udf-party.co.za/api` baked into the JS chunks (187 packaged web assets); `/crm/candidates` admin screen present. Identity EXACTLY matches `verify-apk.py release` mode's expectation (non-debuggable `com.udf.party 1.0.2 (3)`, API 36, upload signer).
- KEY NUANCE (must-read): the ward-councillor roster is WEBSITE-ONLY (marketing homepage `#councillors`) — it is NOT in the app bundle (`council-card` markup does not appear in `out/`), and NO frontend/app CODE changed in this task (only a SQL migration + static HTML). This APK is therefore functionally identical to the prior build; the app's "Ward Councillors" admin screen (`/crm/candidates`) shows the new ward ranges at RUNTIME by fetching the API — which reflects the updated numbers only once the BACKEND (migration 047) is deployed to production. The APK was still rebuilt as requested and to keep the release artifact current.
- NOT DONE / gated (per "build locally, don't deploy"): nothing committed/pushed/shipped/deployed. The Play/AAB pipeline was NOT run — `apk:release` builds the APK only (an AAB needs `:app:bundleRelease`); `verify-apk.py release` + Play Console upload remain deploy-gated on explicit approval. `apk-build.json` was deliberately NOT modified: it is the DEBUG-build baseline that `verify-apk.py release` reads as "previous" (it asserts the release signer DIFFERS from it and that the debug APK at that path is preserved — confirmed intact, sha `487f87b4…` still matches). The Sep-18 debug APK remains in place.
- Dev-server note: the `NEXT_EXPORT=1` build overwrites `frontend/.next`, so :3000 was stopped for the build and restarted afterward (`/login` 200). Marketing :4100, API :4000 and DB :5433 stayed up throughout.

## PRE-DEPLOYMENT AUDIT — full feature/workflow/button verification + build readiness — LOCAL (2026-09-19)
- User request: before deploying to production, audit and confirm every feature, workflow and button works correctly and that everything asked for is created and ready for final deployment. This is a VERIFICATION-ONLY pass — nothing was committed, pushed, shipped or deployed; no production change. Scope = the entire stacked uncommitted release (migrations 031-046 + attachments/covers/RSVP, CMS pages/blocks/slider, SA report gate, patrol GPS, notification bell, analytics, marketing CMS hooks, ward-candidates roster). Production remains at migration 030.
- VERDICT: GREEN — functionally verified and build-ready. Every audited button/workflow passes; all three production builds succeed; deploy artifacts rebuilt from current source. Two non-code items must be handled AT COMMIT/DEPLOY time (see FINDINGS). Go to commit → ship.sh → RUNBOOK only on explicit approval.
- MIGRATION LEDGER (deploy-critical): the full 001→046 chain applied cleanly on a FRESH scratch DB (`party_audit`, exit 0, 46 migrations). Definitive live-vs-fresh schema diff: **86 tables, 842 columns, 254 indexes, 359 constraints — ALL IDENTICAL**, so production's 031→046 path lands in exactly the right state. The migration-042 `cms_media` hand-edit concern is RESOLVED: `cms_media` is a TABLE (not a column) and exists identically on live and fresh DBs; 045 seeds 26 fixed-id candidates; 046 `marketing.candidates` block present. Scratch DB dropped afterward (no residue).
- STATIC: backend `tsc --noEmit` exit 0; frontend `tsc --noEmit` exit 0; `caps:check` exit 0 (35 capability pins, 34 nav gates, 31 nav pins, 16 modules, 55 seeded gates); `next lint` 0 errors / the same 6 known pre-existing warnings (none in new files).
- REGRESSION: `workflow-regression` 50 passed / 0 failed (fixtures rolled back); `analytics-unit` 15 / 0; client workflow regression 14 passed.
- LIVE SMOKES (against :4000): candidates — public list 26 → unauth CRM 401 → create → photo set `{hasPhoto:true}` → public photo 200 image bytes → deactivate (drops from public feed) → delete 204 → final 26; `check-cms-hooks` 16/16 lossless, 0 issues; `audit-cms-http` 6 passed / 0 failed (rows rolled back, files removed); `smoke-rsvp-covers` 43 / 0; `smoke-cms-geo` 9 / 0.
- BROWSER DOM AUDIT (both origins; pixel screenshots still blocked by the detached IDE browser panel — `NATIVE_BROWSER_VIEWPORT_UNAVAILABLE` — so verified at DOM level): CRM `/crm/candidates` — header "Ward Councillors", stats 26 on-roster / 26 public / 0 hidden / 26 missing-photo, 26 rows in order, all six row actions present (Edit · Photo · ↑ · ↓ · Hide · Delete; ↑ correctly disabled on row 1), Edit modal opens with every field (name/role/wards/bio/order/"Show on the public homepage") and cancels without saving, sidebar nav item present. Marketing `:4100/#councillors` — heading "Meet our ward councillors", exactly 26 `.council-card`s hydrated in CRM order, initials placeholders (no photos authored yet), **0 cards carry `.reveal`** (all computed opacity 1 / visible), nav "Councillors" → `#councillors`. Only console noise: self-healing JWT-refresh 401s and a local-only CORS block on the production analytics beacon — neither is a defect.
- PRODUCTION BUILDS (all three deploy paths): backend `tsc` emit exit 0 (`dist/index.js`); frontend `next build` exit 0 (`/crm/candidates` in the route manifest, static); frontend `NEXT_EXPORT=1 next build` (APK path) exit 0 (`out/crm/candidates/index.html` emitted).
- DEPLOY ARTIFACTS REBUILT from current source (were STALE — dated Sep 17, predating this release's Sep 18-19 work; `ship.sh` ships the tarballs as-is and does NOT rebuild, so this was required). `build-offbox.sh` exit 0 → `deploy/.artifacts/udf-backend.tar.gz` (Sep 19 16:36) contains all 46 migrations incl. 044/045/046 + the compiled `dist/modules/candidates/` (routes + publicRoutes); `udf-frontend.tar.gz` contains `crm/candidates/` page + JS chunk. nginx (4 confs) + `systemd/udf-api.service` present; `production.env.example` diff adds NO secret values; `secrets/` and `backend/.env` gitignored.
- NO RESIDUE: 26 ward_candidates / 0 non-empty bios / 0 photos / 0 `qa.*` blocks / 0 `qa-` pages; scratch DB dropped; `backend/uploads/` gitignored (local test media will not ship).
- FINDINGS / required at commit-deploy time (none are functional defects):
  1. `.tmp-pdfjob/` (~8.5 MB scratch Python PDF scripts) is NOT gitignored and WOULD be committed. Add `.tmp-pdfjob/` to `.gitignore` (mirrors the existing `.tmp-verify/` entry) before committing. The other two untracked non-source files — `website/assets/js/udf-content.js` and `udf-analytics.js` — ARE legitimate marketing source and SHOULD be committed.
  2. Deploy precondition: production DB is at migration 030. Before applying 031-046, confirm production `schema_migrations` head is exactly 030 with no hand-patches (the fresh-DB proof assumes a clean 030 baseline), then follow `deploy/RUNBOOK.md`.
  3. Content not entered: the 26 bios and headshots are still empty — a CRM content task on `/crm/candidates`, not a code blocker.
- Dev-server note: running the production builds overwrote `frontend/.next`, transiently 500-ing the :3000 dev server; it was restarted and is healthy (`/login` 200, `/crm/candidates` 200). Marketing :4100 and API :4000 stayed up throughout.

## "Meet our ward councillors" — 26-candidate roster: DB + CRM screen + homepage grid — LOCAL (2026-09-19)
- User request: after the sample CMS edit above, "need a page to have all the ward candidates on", clarified to: below the party leader on the public homepage, 26 ward-councillor cards (photo + name + bio), maintained as CMS/CRM-editable content. Built as a vertical slice mirroring events CRUD. NOTHING committed, pushed or deployed; local dev DB only.
- Source data: IEC LGE2026 Western Cape candidate extract (`~/Downloads/LGE2026-UDF-candidates-Western-Cape/udf-wc-candidate-roster.csv`) — 26 distinct candidates, party-list order 1-26 with their ward slots. No candidates table existed before this (only `councillor_scorecards`).
- Migration `045_ward_candidates.sql` (head now 046): `ward_candidates (full_name, role_label, wards_label, bio ≤2000, photo_media_id → media_assets ON DELETE SET NULL, sort_order, is_active, timestamps)` + active/order index, seeded with the 26 rows under fixed ids `c0000000-…-0000000000NN` (`ON CONFLICT (id) DO NOTHING`, so re-running never duplicates). Bios intentionally seeded empty — copy is the editor's to write.
- Migration `046_marketing_candidates_section.sql`: new published block `marketing.candidates` (`sectionKicker`/`sectionTitle`/`sectionLede`) holding EXACTLY the homepage section's static wording, so the section heading is editable in `/crm/website` without touching the roster. Lossless like 044.
- Backend `modules/candidates/`: `routes.ts` = CRM CRUD (`GET/POST/PATCH/DELETE /api/crm/candidates` + `POST/DELETE /:id/photo` through `transparency.uploadMedia(… purpose 'profile')`, every write `recordAudit`ed as `candidate.*`) gated `authenticate + requirePermission(CONTENT_MANAGE)` — NO new permission, no territory scope (it is published content, not a ward record); `publicRoutes.ts` = `GET /api/public/candidates` (active only, editorial order, `max-age=60`) + `GET /api/public/candidates/:id/photo` bytes. Both wired in `app.ts` before the `/api/crm` gate. Added `GET /api/crm/candidates/:id/photo` (`no-store`) so the CRM can preview an INACTIVE candidate's photo, which the public path hides — avoids the known bulletin-cover "settable but never previewable" asymmetry.
- Frontend: `types.ts` + `api.ts` candidate methods (`listCandidates`/`create`/`update`/`delete`, `set`/`deleteCandidatePhoto`, `candidatePhotoUrl`); new `components/crm/CandidatePhotoEditor.tsx` (Round preview via authenticated blob, upload/replace/remove, shared 8 MB data-url cap); new CRM screen `app/crm/candidates/page.tsx` (stat grid, search + public/hidden filter, CSV export, table with reorder ↑/↓, Hide/Publish, Edit modal, Photo dialog); `CrmShell` Platform nav entry "Ward Councillors" gated `Perm.CONTENT_MANAGE`, plus two `check-caps.mjs` nav pins (superadmin true / national_admin false — fires if `content:manage` is ever widened downward).
- Public homepage `website/index.html`: new `#councillors` section (`section-alt`) directly BELOW `#leader` and above the vote strip, with 26 static `.council-card`s shipped in the HTML (name / list label / wards, initials avatar) as the no-JS, pre-publish and CMS-down fallback, inside a `data-cms-candidates` container. `udf-content.js` gained a roster hydrator: `GET /public/candidates`, rebuilds the grid with photo (when set) + name + role + wards + bio, all via `textContent`/safe `img.src`, leaving the static cards untouched on fetch failure or an empty roster. Built cards deliberately omit `.reveal` (main.js binds its observer at load, so late-added reveal elements would stay at opacity 0). Grid styles added to `website/assets/css/style.css`.
- Reachability: "Councillors" added to the header nav and "Ward councillors" to the footer Explore list on ALL FOUR pages (`index.html` uses the `#councillors` fragment, the pillar pages link `index.html#councillors`), so the roster is one click away from any page.
- VERIFIED (all local): backend + frontend `tsc --noEmit` exit 0; `caps:check` ✓ (34 nav gates, 31 nav pins, 35 capability pins — +1 gate/+2 pins from this feature); `next lint` = only the 6 known pre-existing warnings, none from new files; `.tmp-verify/check-cms-hooks.mjs` now 16/16 hooks equal to published, 0 mismatches. `.tmp-verify/smoke-candidates.mjs`: public list 26 → unauth CRM 401 → create 201 → photo set `{hasPhoto:true}` → public photo 200 image bytes → update `isActive:false` (drops out of the public feed) → delete 204 → back to 26. `.tmp-verify/candidates-loop.mjs` proved the content loop on a REAL seeded row (ANDHOR GREY MARKS): CRM PATCH bio + POST photo → public feed reflected both → `GET /public/candidates/:id/photo` 200 image/jpeg 284,631 bytes → `--revert` restored the seeded state (verified: 26 rows, 0 non-empty bios, 0 photos).
- Browser verification (DOM-level, both origins): on `:4100/index.html` the hydrated grid exposes exactly 26 `.council-card`s in CRM order, the first card's `<img>` resolves to `/api/public/candidates/<id>/photo` (loaded, naturalWidth 1024) and shows the sample bio, and zero cards carry `.reveal`; console clean. On `:3000/crm/candidates` (signed-in superadmin session) stats read 26 on roster / 26 public / 0 hidden / 25 missing photo, 26 table rows, Platform sidebar shows "Ward Councillors", Edit modal prefills the bio with "Public" ticked (cancelled, nothing saved) and the Photo dialog shows the round preview with Replace/Remove.
- KNOWN LOCAL RESIDUE (dev only, nothing sensitive): the media tests left ~6 orphan `media_assets` rows + files under `backend/uploads/` (a row keeps at most one photo, so re-sets orphan the previous asset; the reverted sample left 2 unreferenced uploads). One truncated smoke run left a `QA SMOKE CANDIDATE` row behind — deleted through `DELETE /api/crm/candidates/:id` (204); roster verified back to exactly 26 seeded rows, 0 non-empty bios, 0 photos. `.tmp-verify/*.mjs` helpers hold the throwaway local dev admin credentials; `frontend/.env`-free dev DB only.
- NOT DONE / next: the 26 bios and headshots are still empty — content entry is a CRM task (`/crm/candidates`), and photos need real files; nothing is deployed, so production `udf-party.co.za` still shows the manifesto pages without this section until migrations 045-046 ship and the site is redeployed.

## Marketing-site CMS wiring (closes the web-editing reach gap) — LOCAL (2026-09-19)
- User approved the follow-on from the audit below: wire the public `udf-party.co.za` marketing pages so non-devs can edit them from `/crm/website` (was 1 hook; the rest was static-HTML-only). Progresses, not deployed, not pushed; local dev DB only.
- Migration `044_marketing_hero_hooks.sql` (head now 044): aligns `marketing.home` published hero/CTA (`heroTitle`/`heroSubtitle`/`ctaLabel`/`ctaHref`) to the real homepage copy and seeds three new blocks — `marketing.housing`, `marketing.safety`, `marketing.economy` (`heroKicker`/`heroTitle`/`heroLede`) — each PUBLISHED = the exact current page text. `marketing.home.mission`/`vision` deliberately untouched (they also feed `GET /api/public/manifesto`). Version snapshots written so editor rollback history is complete.
- HTML hooks added (static copy kept as the no-JS/SEO/CMS-down fallback): `website/index.html` headline + already-present lede + primary CTA (label via inner span, link via `data-cms-attr="href"`); the three policy-page heroes in `housing-infrastructure.html`/`safety-communities.html`/`economy-governance.html`. The homepage headline's decorative gold `<span class="hl">` was removed so static == published (single editable plain-text field). Coverage 1 → 13 editable fields across all 4 pages.
- VERIFIED: `.tmp-verify/check-cms-hooks.mjs` parses every `data-cms` hook out of the HTML (attr-aware) and diffs it against the published API value — 13/13 lossless, 0 mismatches (hydration is a no-op until an edit is published). Live admin round-trip on `marketing.housing`: editor lists all 4 marketing blocks → save draft → publish (public reflected the edit) → rollback to v1 (public restored to seed). Dev session revoked; no content or auth residue; `marketing.home` still serves `mission`/`vision`.
- Carried-over note: `GET /api/public/content/*` is `cache-control: public, max-age=60`, so a just-published marketing edit can take up to 60s to appear in a warm cache.

## Pre-deploy button/workflow debug + web-editing reach audit — LOCAL (2026-09-19)
- User request: debug every button/workflow, confirm behaviour is correct, locate the web editing for udf-party.co.za, and reconcile what was requested/planned vs built. No code changed — this is a verification/audit pass. Nothing committed/pushed/deployed.
- Full battery GREEN on current working tree: backend `tsc` 0, frontend `tsc` 0, `caps:check` all ✓ (35 pins / 33 nav gates / 16 modules), `next lint` = only the 6 known pre-existing warnings, `workflow-regression` 50/0, `analytics-unit` 15/0, client regression 14/0, live smokes `smoke-cms-geo` 9/0 + `audit-cms-http` 6/0 + `smoke-rsvp-covers` 43/0 (≈137 behavior assertions).
- CMS editor (`/crm/website`, superadmin `content:manage`) verified END-TO-END in a live browser session via the dev-session helper: create block → save draft → publish → roll back → delete ALL buttons work (correct endpoints, 200s, `confirm()` gating on destructive actions, version history accurate, public read reflects publish/rollback, zero residue). All 16 editor actions + `cmsMediaUrl` resolve to real `api.ts` methods — no dead buttons.
- FINDING (deploy-relevant gap, NOT a code defect): the web editor is real and works, but its reach into the PUBLIC marketing site `udf-party.co.za` is minimal. That site is a static export served by nginx from `/var/www/udf/manifesto` (per `deploy/nginx/udf-manifesto.conf`), hydrated by `website/assets/js/udf-content.js`, and carries exactly ONE `data-cms` hook — `website/index.html:45` bound to `marketing.home:heroSubtitle`. The 3 policy pages have zero hooks, and the other published `marketing.home` fields (heroTitle/ctaLabel/ctaHref/mission/vision) are NOT bound in the HTML. The only other consumer is server-side: `GET /api/public/manifesto` overlays mission/vision from `marketing.home`. The CMS Pages/`CmsPage` path is unused (0 pages). So "non-devs edit all website content" is currently satisfied for the in-app surfaces + one hero line; editing the rest of the public site still means editing static HTML. Closing this = add `data-cms`/`data-cms-slider` hooks to the marketing pages and/or author them as CMS pages via `render-marketing-pages.ts` (plan Assumption A1) — an explicitly separate, not-yet-requested step.
- Minor UX note: `GET /api/public/content/*` is served `cache-control: public, max-age=60`, so a just-published edit can take up to 60s to appear in a warm browser cache.

## Pre-deploy snapshot verification (bell + patrol + covers) — LOCAL (2026-09-19)
- User request: "confirm with snapshot that everything asked before deploy is there and working" for website/CRM/CMS/mobile. Verified the two most-recent features plus the cover-image surfaces with authenticated browser evidence. Nothing committed/pushed/deployed; this is a LOCAL verification pass only.
- Login blocker worked around WITHOUT passwords: added dev-only helper `backend/scripts/dev-session.mjs` — mints a genuine access token via `signAccessToken` + a real `refresh_tokens` row for an existing local user and prints the exact localStorage blob the frontend reads. Refuses to run outside a local DB. Injected a WARD_COUNCILLOR (`df86b280…`, ward NORTH-W09) and a SUPERADMIN (`e0acd856…`) session; both authenticated against :4000 API-side first (patrol wardEntries, notifications `?unread=true`).
- Staged fixtures (dev DB only) via `.tmp-verify/snapshot-seed.sql`; remove with `.tmp-verify/snapshot-teardown.sql`. Event cover set through the real `POST /api/events/:id/cover` (`.tmp-verify/set-event-cover.mjs`).
- RESULTS — screenshots under `.tmp-verify/`: `snapshot-crm-patrols.png` (green ● Live badge on the active row), `snapshot-crm-patrol-detail-wards.png` ("Wards walked (2)": green own-ward chip + amber "crossed in" chip + explainer), `snapshot-mobile-engages-live.png` (● LIVE on the mobile patrols row), `snapshot-crm-bell.png` (bell dropdown unread-only, 13 unread, staged notice on top), `snapshot-mobile-alerts.png` (mobile Alerts unread-only + Mark-all-read; Show-read toggle correctly hidden while nothing is read), `snapshot-crm-bulletins-cover.png` (Cover action on bulletin rows). Events "Cover" + Website CMS verified by DOM only (round-1 pixels caught the councillor's correct "NOT AUTHORISED" gate for `event:write`/`content:manage`; superadmin DOM confirmed both render — Cover dialog shows existing-cover Replace/Remove + 200 preview, `/crm/website` shows the Pages/Blocks manager). Pixel capture for those two was blocked by the IDE Browser panel being detached (`attached=false`), not an app fault.
- INCIDENT (local dev only, now fixed): `/crm` dashboard hit `ERR_TOO_MANY_REDIRECTS` in the browser while every other `/crm/*` route served 200. Cause was NOT app code (`crm/page.tsx` is a plain client component, no `redirects()` in `next.config.mjs`) — it was two `next dev` instances (:3000 fresh + a stale :3100) sharing one `frontend/.next`, corrupting the `/crm` index-route 308 manifest; a stale browser-cached 301 then persisted it. Fix: killed both servers, `rm -rf frontend/.next`, restarted a single :3000; `/crm`→200 (0 redirects), `/crm/`→308→`/crm` (1 hop). Anyone with a poisoned profile needs a one-time cache-bypass reload.

## CMS pages/blocks/slider + South-Africa report gate + events/bulletin CRUD UI — LOCAL feature work COMPLETE (2026-09-19)
- User request: full website editing (pages add/change), an image+text slider with per-slide durations, events + bulletin CRUD, and a South-Africa restriction on resident reporting; plus confirmation that reports route by location to the correct ward/councillor while the filer keeps updates, and National retains full cross-territory admin. Decisions clarified via questions: geo scope = Cape Town geometry unchanged + hard SA bounds gate only (no province/ward import, no cascading picker); CMS = manage pages + blocks publishing to BOTH marketing site and in-app surfaces; events/bulletin = verify backend + fill CRM UI gaps. Routing/confirmation answers verified from code and live dev (see below). NOT committed, NOT deployed, NOT pushed; migration 042 applied LOCAL dev DB only; nothing shipped to `102.68.98.129`.
- Confirmed on the user's questions (verified, no code change needed): a report's ward is resolved from its submitted lat/lng via PostGIS `resolveWardByPoint` (`ST_Intersects`), so a Ward-1 filer reporting a Ward-3 pothole routes to Ward 3 (owner assignment + councillor notification), while the filer still sees the report under `scope=mine`; superadmin (National) has full-admin permissions across all territories (strict superset of national_admin per regression).
- SA gate: `transparency/schemas.ts` `createResidentReportSchema` gained a lat/lng `.refine` rejecting any point outside the South-Africa bounding box (lat −34.90..−22.10, lng 16.40..32.95) with "Resident reports are only supported within South Africa"; the both-or-neither lat/lng rule and Cape Town ward resolution are unchanged (valid-but-unsupported SA points keep the existing 400 "No supported ward…"). `components/tabs/ReportToCouncillor.tsx` copy makes Region/Area/precise-location intent explicit and surfaces the out-of-country 400 as a friendly inline message.
- Migration `042_cms_pages.sql` (applied locally; `schema_migrations` head is now `042_cms_pages.sql`): `content_pages (slug unique, site marketing|app, title, sections jsonb ordered block-key refs, is_active)`, `content_blocks.kind` (`fields|slider|section`, default `fields` — existing rows untouched), `content_page_versions` (UNIQUE(slug,version) snapshot for publish/rollback), and `cms_media` (allow-list registry for CMS-uploaded media). NOTE: 042 was edited after first apply to add `cms_media`; the delta was applied directly via psql and the ledger row re-recorded — a fresh-environment `npm run db:migrate` applies the full file cleanly.
- Backend content module: block CRUD (`createBlock` seeds empty draft + kind; `deleteBlock` refuses with 409 while any page references it), full page CRUD (`listPages`/`getPage`/`createPage`/`renamePage`/`updatePageSections` validating referenced keys/`deletePage`) with draft→publish→snapshot semantics matching blocks (public never sees drafts), `publishPage`/`rollbackPage`/`listPageVersions`. New routes: admin `/api/crm/superadmin/content/pages*` (+ `POST /media`) all behind the existing `content:manage` superadmin permission — NO new permission introduced; public `GET /api/public/pages` + `/api/public/pages/:slug` (resolves ordered PUBLISHED blocks with kind) and `GET /api/public/content-media/:id` (serves only media registered in `cms_media` or present in a published block payload — leader-photo and report-media gates untouched). Slide item shape `{imageMediaId,title,text,durationSeconds}`; images reuse the existing `transparency.uploadMedia` pipeline.
- Frontend CMS editor: `app/crm/website/page.tsx` rewritten (~870 lines) as a Pages/Blocks manager — page list per site with add/rename/delete, ordered-section editor (add existing/new block, remove, reorder), block editor rendering `text`/`textarea`/`url` plus new `image` (upload → mediaId via `api.uploadCmsMedia`, preview via `cmsMediaUrl`) and `list` repeater controls, a dedicated slider editor (per-slide image+title+text+duration, add/remove/reorder), and Draft/Save/Publish/Version-history-rollback for both blocks and pages. `lib/api.ts`/`types.ts` extended with the CMS page/block/media types + methods. New dependency-free `components/Slider.tsx` (auto-advances by each slide's `durationSeconds` clamped 2..120, pauses on hover/focus, respects `prefers-reduced-motion`) and `components/CmsPage.tsx` (in-app renderer consuming `/api/public/pages/:slug`), wired into `components/public/PublicHome.tsx`.
- Marketing site (both surfaces): new `website/assets/js/udf-content.js` progressive hydration — `data-cms="key:field"` (+ optional `data-cms-attr`), `data-cms-slider="key"` fetch published payloads from `https://crm.udf-party.co.za/api` and fill text/images or build the slider (hardcoded HTML remains the no-JS/pre-publish fallback, so current output/SEO is unchanged until an admin publishes); script included in all four marketing pages, with a `data-cms` hook on the index hero. New `backend/scripts/render-marketing-pages.ts` (+ root/backend `npm run render:marketing`) emits `website/<slug>.html` for CMS-authored marketing pages from published data (currently a clean no-op: count 0). No nginx/deploy changes.
- Events/bulletin CRUD audit: backend already had full CRUD (events create/patch/delete/cover/rsvp/attendees; bulletins create/patch/cover with status `draft|published|taken_down` — no hard delete by design). Gap-fill UI only, all against existing endpoints: `crm/events/page.tsx` gained an Edit modal (reuses the create form via `editingId`) and a Delete confirmation; `crm/bulletins/page.tsx` gained an Edit modal, a status column/badge, and Publish/Take-down controls, and FIXED a real bug where the list requested `limit=200` against a `max(100)` schema — the 400 was silently swallowed into an empty list (now `limit=100`).
- Verification (LOCAL dev): backend `tsc --noEmit` exit 0; frontend `tsc --noEmit` exit 0; `next lint` exit 0 with the SAME 6 known pre-existing warnings, none from new files; `caps:check` exit 0, no new permissions. Live :4000 SA-gate proof: Rio coordinate → 400 "…South Africa", Cape Town coordinate → 201 routed to ward `CPT-W115`. New `backend/scripts/smoke-cms-geo.sh` passed 9/0 (SA gate + filer `scope=mine` visibility + public pages index/slug 200 + unknown-slug 404). `workflow-regression.mjs` 47 passed / 0 failed (was 44; +3: SA schema refine assertions, block create/draft/publish kind model, page CRUD publish-only + delete-guard + rollback; fixtures rolled back). Authenticated browser acceptance of the new CMS editor remains NOT performed (standing login blocker applies).
- NEXT if asked to ship: this stacks on the still-uncommitted SuperAdmin 031–038, attachments 036, and email/cover/RSVP 039–041 work; decide commit/deploy explicitly and separately. No production migration has been run for 042; marketing bundle redeploy is an explicitly separate, later, approved step.

### Principal-engineer double audit + fix/retest (same day, LOCAL)
- Ran a two-pass audit (static review of every changed surface + live-data runtime probes) over the whole workstream above, fixing then retesting each finding. Data accuracy verified against the live dev DB: migration head is `042_cms_pages.sql`; all CMS constraints/CHECKs/FKs present (`content_pages_slug_key`, `content_page_versions_slug_version_key`, `content_blocks_kind_check`, cascades); zero malformed sections, zero orphan block-references, zero version rows without a page, zero `cms_media` orphans.
- Defects found and FIXED (retested clean):
  1. **Events CRM was upcoming-only** (`GET /api/events` defaults `upcoming=true`), so a past/finished event silently vanished from the table and could never be marked done/cancelled/deleted — a real data-accuracy bug in the CRUD gap-fill. The CRM list now requests `upcoming=false`.
  2. **`z.coerce.boolean()` query trap**: the fix surfaced that `z.coerce.boolean().parse('false') === true` (non-empty string), so a naive `upcoming=false` would have kept the upcoming-only filter. Added a shared `backend/src/http/query.ts` `boolQuery()` (accepts 1/true/yes · 0/false/no, rejects garbage with 400) and applied it to BOTH `events.upcoming` and `notifications.unread` (the latter had the same latent bug — `?unread=false` was impossible).
  3. **Duplicate block reference**: `updatePageSections` accepted the same `blockKey` twice, which would double-render a section and make `deleteBlock` refuse a block the UI could not cleanly remove — now 400s and does not persist.
  4. **Non-atomic create race**: `createBlock`/`createPage` pre-checked existence then INSERTed; a concurrent create surfaced as a 500 `23505` — now translated to a clean 409.
  5. **Public CMS media over-serving**: `loadPublicCmsMedia` would serve any registered asset by its stored content-type; now guards to `image/*` only (defence-in-depth — the upload path already pins `captureMode:'photo'` so a PDF is refused at 400 before registration; verified both layers).
  6. **Editor lifecycle**: block `open()` had no error handling (stuck on a failed fetch); the delete flows reloaded the just-deleted row (spurious 404 notice); page `publish` renamed unconditionally (could clobber a concurrent title edit) — added try/catch, a `keepReload=false` path for deletes, and a `titleDirty` gate.
  7. **Slider robustness**: the auto-advance timer depended on the `slides` array identity, so any parent re-render restarted the countdown; a non-numeric/garbage `durationSeconds` could yield `NaN` ms; and removing slides could leave the cursor out of range — fixed via a ref-stable timer, `Number.isFinite` clamp, and an index bounds effect.
  8. **Marketing hydration hardening**: `udf-content.js` now allow-lists which attributes `data-cms-attr` may set (never `on*` handlers) and rejects `javascript:`/`data:`/`vbscript:`/`file:` URLs on href/src, plus a NaN duration guard and lazy-loaded slide images.
- Regression extended +4 assertions (page duplicate-block 400, `boolQuery` truth table, block/page 409 races covered by the model tests). New live-HTTP audit probe `backend/scripts/audit-cms-http.mjs` (in-transaction, fully rolled back, uploaded files deleted) exercises the REAL endpoints on an ephemeral server: auth/content:manage gating (member 403), image upload→public serve with `max-age=86400`, PDF-refused-at-400, unknown media 404, slider draft-invisible→publish→public per-slide durations, page create→sections(publish-only)→duplicate-400→delete-guard-409→rollback, bad-slug 400, public page index, and `?upcoming=` true/false/garbage(400). Left zero residual rows and zero stray files (verified in DB + on disk).
- Retest battery (LOCAL dev, all after fixes): backend `tsc` 0; frontend `tsc` 0; `next lint` 0 with the SAME 6 pre-existing warnings, none from touched files; `caps:check` 0 (no new permissions); `workflow-regression.mjs` 48 passed / 0 failed (was 44→47→48 across this feature+audit); `audit-cms-http.mjs` 6 passed / 0 failed; `smoke-cms-geo.sh` 9 passed / 0 failed against the live :4000. Nothing committed, pushed, or deployed.

## Notification bell clears read messages — LOCAL (2026-09-19)
- User request: "clear bell messages once read." Scoping questions answered: apply to BOTH the desktop CRM bell dropdown AND the mobile Engage→Alerts list; the read archive must be KEPT (nothing deleted) — the bell/inbox becomes a pure unread queue that clears as you read.
- Bug found + fixed: `notifications/routes.ts` parsed `?unread=` into `q.unread` and spread it into `service.list`, but the service reads `opts.unreadOnly` — so `?unread=true` was a silent no-op (the read filter never applied). The route now maps `unread → unreadOnly`.
- Desktop bell (`components/CrmShell.tsx`): the panel now fetches `unread:true`, `openAlert` removes a message from the list on read (was: flip a read flag in place), and `readAllAlerts` clears the panel; the durable archive stays the separate Notification Centre screen (`/crm/notifications`, which lists all — unchanged).
- Mobile Alerts (`components/tabs/EngageTab.tsx`): list defaults to unread-only and drops items as they are read; added a "Show read"/"Hide read" toggle (visible only when read items exist) so history remains reachable on the one mobile surface; "Mark all read" is disabled when nothing is unread; distinct empty states for "no notifications" vs "all caught up."
- Verification (LOCAL dev): backend `tsc` 0; frontend `tsc` 0; `caps:check` 0 (no new permissions); `next lint` shows only the SAME 6 pre-existing warnings, none from touched files; `workflow-regression.mjs` 49 passed / 0 failed (was 48; +1 new live-HTTP test asserting `?unread=true` returns only unread, a read message clears from the bell, and the unfiltered archive still carries it read-flagged; fixtures rolled back). Nothing committed, pushed, or deployed.

## Patrol GPS ward indication + live cross-ward detection — LOCAL (2026-09-19)
- User request: when a patrol starts, automatically indicate the ward of the patrol; if the patrol moves over into another ward, indicate the patrol is live there. Scoping answers: ward detection is DISPLAY-ONLY (the patrol stays filed to the councillor's registered ward — no re-filing, no scope change); crossings ARE PERSISTED on the patrol; the LIVE indicator shows on the mobile patrol sheet AND the CRM `/crm/patrols` list.
- Migration `043_patrol_ward_entries.sql` (applied LOCAL dev DB; head is now `043_patrol_ward_entries.sql`): `patrol_ward_entries (patrol_id FK cascade, ward_code FK regions/code SET NULL, first_seen_at, last_seen_at, point_count, UNIQUE(patrol_id, ward_code))` + patrol/ward indexes.
- Backend: `addTrackPoint` now resolves each saved fix against `regions.geom` via `ST_Intersects` once, upserts the ward row (own ward included; repeat fixes bump `last_seen_at`/`point_count`), and echoes `{ wardCode, wardName }` on the track-point response (no extra round trip for the mobile label; retried duplicate points also record their sighting; an unmapped fix records nothing but still saves the point). `getPatrol` hydrates `wardEntries[]` with a SQL-computed `crossing` flag (`ward_code IS DISTINCT FROM` the patrol's ward). Mobile live label reads the echoed ward; the persisted rows are the archive.
- Frontend: `PatrolSheet` active phase shows a `● LIVE · <ward>` badge (green in own ward, amber when the fix resolves elsewhere) plus an amber banner "Patrol is live in <ward>, outside your ward <home>. It stays filed to <home>."; view phase lists "Wards walked" with own-ward/crossed badges and first/last sighting + fix counts. `EngageTab` patrol rows show a green LIVE badge for `status=active`. `crm/patrols` table shows a ● Live badge instead of the plain badge for active rows, and the detail modal renders the persisted wards-walked chips with a crossed-in explainer. `types.ts`/`api.ts` extended (`PatrolWardEntry`, `TrackPointResult`).
- Verification (LOCAL dev): backend `tsc` 0; frontend `tsc` 0; `caps:check` 0 (no new permissions — patrol surfaces still ride `patrol:read`/`patrol:write`); `next lint` back to ONLY the 6 pre-existing warnings (one transient unescaped-entity error in the new CRM copy was fixed); `workflow-regression.mjs` 50 passed / 0 failed (was 49; +1 new test gives the two QA wards real Karoo geometry and asserts: home-ward fix echoes its ward, a moved-over fix echoes the neighbour, `getPatrol` records own-ward-not-crossing + neighbour-crossing with point counts, repeat fixes aggregate (no duplicate row), and an unmapped fix returns `wardCode:null` while still storing the point; fixtures rolled back, `patrol_ward_entries` residue 0 rows). Migration 043 applied to the LOCAL dev DB only. Nothing committed, pushed, or deployed. Authenticated browser acceptance of the live mobile tracking screen NOT performed (standing login blocker; real-device GPS required).

## Email-uniqueness + cover images + named RSVP — LOCAL feature work COMPLETE (2026-09-19)
- User request (three features): (A) remove the double-email-account security risk, (B) add a cover image to events and ward bulletins, (C) let the backoffice see WHO attends/interested, one entry per member. Clarified via questions into: DB-unique member email, a dedicated cover field, and named + login-required + one-per-member RSVP. NOT committed, NOT deployed, NOT pushed. All changes are local; nothing shipped to `102.68.98.129`.
- Migrations (applied locally; `schema_migrations` head is now `041_event_rsvps.sql`):
  - `039_member_email_unique.sql`: dedup-guarded partial UNIQUE index `members_email_bidx_live_unique` on `members(email_bidx) WHERE email_bidx IS NOT NULL AND deleted_at IS NULL`. RAISEs if any live duplicate exists at apply time (dev DB had 0). Closes the race window that the previous app-check-only guard left.
  - `040_cover_images.sql`: adds `cover_media_id UUID REFERENCES media_assets(id) ON DELETE SET NULL` to BOTH `events` and `ward_bulletins` (dedicated cover, independent of the 036 attachments gallery).
  - `041_event_rsvps.sql`: new `event_rsvps` table with `CONSTRAINT event_rsvps_one_per_person UNIQUE (event_id, user_id)` and `response CHECK (response IN ('going','interested'))` default `'going'`; plus event/user indexes. Resolved users/member linkage stored on `member_id` (nullable).
- Backend: `memberships/service.ts` dup pre-check now filters `deleted_at IS NULL` (matches the partial index) and `registerMember` wraps the transaction to translate a `23505` into a friendly `409 "That email is already registered with the party"`. `events/routes.ts`: RSVP rewritten to `authenticate` + `rsvpLimiter` + a single `FOR UPDATE` transaction (idempotent; `rsvp_count` only moves on going↔not-going transitions; `409 Event is full` on capacity), `DELETE /:id/rsvp` cancels, `GET /:id` is `optionalAuthenticate` and annotates caller `going`, new public `GET /:id/cover`, organiser-scoped + audited `GET /:id/attendees` (`event:write` + territory, reveals sealed name via `openRecord`, logs `event.attendees.read`), and `POST`/`DELETE /:id/cover` behind the `EVENT_WRITE` gate. `wardBulletins` service+routes: `hasCover` field + published-public/draft-gated `GET /:id/cover` and gated cover `POST`/`DELETE`.
- Frontend: `types.ts` (`PartyEvent` +`hasCover`/`going`, new `EventAttendee`, `WardBulletin` +`hasCover`); `api.ts` (rsvp with response, cancelRsvp, listEventAttendees, event/bulletin cover set/delete + public URL + authed `coverBlob`); new reusable `components/crm/CoverEditor.tsx` and `components/crm/EventAttendees.tsx`; wired Cover + Attendees actions into `crm/events` and `crm/bulletins`; `tabs/EventSheet.tsx` gained login-gated RSVP with going/cancel toggle, refreshed `going`/count for the signed-in caller, and cover display.
- Verification (LOCAL dev, live :4000): backend `tsc` exit 0; frontend `tsc` exit 0; `next lint` exit 0 (SAME 6 pre-existing warnings, none from new files); `caps:check` exit 0. New targeted runtime smoke `backend/scripts/smoke-rsvp-covers.sh` passed 43/0 — proves fresh signup 201 then same-email 409, a true concurrent same-email race yields exactly one 201 + one 409 with no live duplicate left in DB, anonymous RSVP 401, idempotent one-row-per-member RSVP with count stable, going↔interested capacity transitions, `going` annotation for the caller, organiser 200/403/401 attendee gating with the read audited, and event/bulletin cover upload → public/draft serve (`image/png`) → delete with `hasCover` flipping both ways, plus 401/403 cover-write gating and 404 for a missing cover. Backend `workflow-regression.mjs` 44 passed / 0 failed (fixtures rolled back) — no regressions.
- Note: seeded staff/member logins in the dev DB are not linked to a `members` row (`users.member_id` is NULL for all), so the attendee list surfaces names from `users.sealed_pii` and legitimately-null `membershipNo`/`ward`; the RSVP uniqueness guarantee is on `(event_id, user_id)` regardless of member linkage. NOT yet done: any production migration for 039–041, authenticated browser acceptance of the CRM cover/attendee UIs, and commit/deploy decisions.
- NEXT if asked to ship: decide commit/deploy explicitly (this stacks on the still-uncommitted SuperAdmin 031–038 and attachments 036 work). No production migration for 039–041 has been run; confirm no live duplicate member emails on the target DB before applying 039 (it intentionally aborts otherwise).


## SuperAdmin Operations Dashboard — LOCAL feature work COMPLETE (2026-09-19)
- Scope (per plan `SuperAdmin_Ops_Dashboard`): a new `superadmin` role + a CRM "Platform" section — first-party cookieless website/app analytics, counted APK downloads + device info, live-user presence over battery-safe adaptive SSE, truthful server/cron status, and a structured-content website editor. NOT committed, NOT deployed, NOT pushed (user: "don't deploy yet"). All changes are local; nothing was shipped to `102.68.98.129`.
- Backend: migrations `031_sessions_presence`–`035_content_blocks` + `037_content_blocks_seed`, and NEW `038_superadmin_module.sql` (registers the `superadmin` module in the `modules` registry + seeds an enabled gate so the ops trio can actually be toggled from CRM → Settings; without it the FK/list blocked the plan's "module toggling strips them" guarantee). Applied locally — `schema_migrations` head is now `038_superadmin_module.sql`.
- Bug fixes this session: analytics site-filter param-index collision in `analytics/service.ts` (getOverview→$3, getTraffic→$2, getDevices→$2 — was 500 "text = integer" when a site filter was set); non-destructive content hydration overlaying published `shared.brand`/`marketing.home` onto the existing `/meta` + `/manifesto` public routes (byte-identical to coded defaults until an admin publishes); `check-caps.mjs` parser taught to resolve named `readonly Permission[]` consts and `...NAME` spreads so the t1 `superadmin`/`national_admin` superset refactor passes the drift guard.
- Frontend: `api.ts` (15 interfaces + ops/analytics/content methods), `lib/liveStatus.ts` (visibility-gated fetch-SSE), `lib/analytics.ts` + `components/AnalyticsBeacon.tsx` (route beacon in root layout), `components/crm/charts.tsx` (dependency-free SVG charts), and 4 pages `crm/platform`, `crm/website-analytics`, `crm/downloads`, `crm/website`; counted download link now points at `/api/public/download/apk`.
- Tests added: `workflow-regression.mjs` +superadmin model assertions (strict-superset, single module owner, toggle-strip via `effectivePermissions`), HTTP assertions (ops routes 401 anonymous → 403 national_admin/councillor → 200 superadmin → 403 when module disabled → 200 re-enabled), and analytics/download rollup math (SAST day bucketing + idempotency). New `scripts/analytics-unit.mjs` (`npm run test:analytics`) covers UA classification + cookieless daily-salt hashing (64-hex one-way, rotates across days, site/ip separated, download hash not joinable to visitor hash).
- Verification (LOCAL dev only): backend `tsc` exit 0; frontend `tsc` exit 0; `next lint` exit 0 with the SAME 6 known pre-existing warnings, none from new files; `caps:check` exit 0 (16 modules mapped, ≤1 owner/perm). Backend workflow regression 44 passed / 0 failed (rolled back); analytics unit 15 passed / 0 failed. Runtime smoke against local :4000: beacon POST → 204, counted download → 302, `/api/analytics/overview` anonymous → 401 (gated), `/api/public/content` → 200. Authenticated browser acceptance of the four Platform pages previously returned 4/4 PASS.
- NEXT if asked to ship: this is uncommitted local work spanning two features (ops dashboard + the bulletin/event attachments section below). Decide commit/deploy separately and explicitly; nothing has been pushed. No production migration has been run for 031–038.

## Bulletin/event attachments — LOCAL feature work (2026-09-19)
- User redirected focus to one task: let authors insert a PDF, photo, short video, or URL link onto ward bulletins and events. Implemented locally; NOT committed, NOT deployed, and NO migration run against any database.
- Change set (uncommitted, alongside the still-uncommitted SuperAdmin migrations 031–035):
  - `backend/src/db/migrations/036_bulletin_event_attachments.sql`: adds `capture_mode` enum value `document` and a polymorphic `attachments` table (bulletin|event parent; photo/video/document via `media_assets`, or link via `url`).
  - Media pipeline extended for PDF: `transparency/mediaPolicy.ts` accepts `application/pdf` (new `document` kind, exempt from the photo/video/voice capture-policy toggles), `transparency/schemas.ts` allows `captureMode: 'document'`, `transparency/service.ts` maps `.pdf` and skips the policy gate for documents.
  - New `backend/src/modules/attachments/` (schemas/service/routes) mounted at `/api/attachments`: create/list/delete + authenticated file serving. Writes are gated by the parent's own permission and territory (`bulletin:write`+ward or `event:write`+region), plus a `requireAnyPermission` baseline; reads gated by parent publication (bulletin drafts = ward staff only; events public).
  - Frontend: `Attachment` type, `api.ts` methods (create/list/delete + blob fetch), and a reusable `components/crm/AttachmentManager.tsx` modal wired as an "Attachments" row action on `crm/bulletins` and `crm/events`.
- Verification so far: backend `tsc --noEmit` exit 0; frontend `tsc --noEmit` clean; `next lint` on the four touched client files reports no warnings/errors. NOT yet run: migration against a scratch DB, backend workflow regression, role-audit, and authenticated browser acceptance (all require a live server/DB and a signed-in session — the standing login blocker applies).
- NEXT if asked to continue/ship: run migration 036 on a scratch DB and confirm `schema_migrations` gains `036_...`; exercise create/list/delete + a published vs draft file fetch in a real superadmin/ward session; then decide commit/deploy separately. No deployment implied.

## Permission label frontend deployment (2026-09-19)
- User requested deployment of descriptive permission labels. Existing commit `1e6974584f1d50101cab0ebe39f919d408156616` contains the four source/test changes; working tree was clean. No new application commit or Git push was made. Labels change presentation only; permission identifiers and grants/revocations are unchanged.
- Candidate `udf-20260919T023411Z` built from the exact commit with same-origin `/api`. The release helper now supports frontend-only builds; no backend archive, Android sync/build, or migration was run. Static export generated 40 pages; type/lint/capability checks passed with the same six existing lint warnings. The unchanged label source previously passed 14 client regression tests and targeted lint/TypeScript checks.
- Artifact/source verification: 562 checks passed, 0 failures. Frontend SHA-256: `25a672ab3bba0dc417e60882bdece851b5524745fd4ce97ed7ee3e868ddf95d8`. Latest receipt: `.tmp-verify/release-latest.json`; immutable manifest: `.tmp-verify/udf-20260919T023411Z/artifacts/release.json`.
- Read-only production preflight passed: SSH, API/nginx/mail services active, API restart count 0, 15 GB free. Before deployment, Users/Settings HTML hashes matched the prior Resident Reports frozen output.
- DEPLOYED: frontend archive, manifest, checksums, and release-specific script uploaded and hash-verified under `/root/udf-releases/udf-20260919T023411Z/`. `deploy-permission-labels.sh` SHA-256: `4663b38331df94e8ee6f82dbbee4f3551c4c2edd5c78e503ccd5ac7ae874f5bd`; shell syntax passed locally and remotely. Unit `udf-web-release-20260919T023411Z.service` completed with `Result=success`, `ExecMainStatus=0`, `ActiveState=active`, `SubState=exited`; completion journal time `2026-09-19 04:38:11` and `DEPLOYED` marker confirmed. Do NOT rerun this completed unit/script or historical deployment scripts.
- Protected frontend backup: `/var/backups/udf/udf-20260919T023411Z/frontend.tar.gz` (9,999,144 bytes), directory mode 700 and archive mode 600; archive readability passed. Previous frontend retained at `/var/www/udf/frontend.previous-udf-20260919T023411Z`. Every prior content-addressed browser asset was preserved and compared byte-for-byte before cutover. No database backup/restore or migration was needed for this frontend-only update.
- Verification: six public routes (`/`, `/crm/`, `/crm/patrols/`, `/crm/resident-reports/`, `/crm/users/`, `/crm/settings/`) and their 29 referenced assets match release output byte-for-byte. Representative descriptive labels are present in the served JavaScript. User/role endpoints still require authentication (401 without credentials); API/database health passed. Independent off-server verifier: 597 checks passed, 0 failed.
- API/nginx/mail process IDs, restart counts, and active timestamps matched before/after; no service was restarted. API PID remains unchanged with restart count 0 and no error-priority journal entries since cutover. Backend entrypoint/package hashes also matched; uploads, configuration, database, signing keys, and Android/Play artifacts were untouched.
- Browser limitation: ordinary navigation to the release-qualified Users page redirected to `/login/`; no usable authorized staff session exists. No authentication bypass or live user/permission mutations were performed. Actual checkbox rendering/layout/runtime remain UNVERIFIED. NEXT: sign in normally, refresh Users & Roles, and perform read-only label acceptance. No redeployment is needed.
- Repository follow-up: deployment progress and the new deployment script remain uncommitted; application source stays at the existing `1e69745` commit. No new commit or Git push was performed.

## Resident Reports production deployment (2026-09-19)
- COMPLETE: user explicitly requested production deployment of the Resident Reports changes and approved a scoped local commit. Commit `947dc46337b3feb229e9ffce5c0cdd74824bc191` contains 20 related source/test files and migration 030. No Git push, Android rebuild, or Play upload is included.
- Frozen candidate `udf-20260919T021239Z` was built from that exact Git commit, excluding unrelated staged/working Android changes. Backend build, 40-page static export, type/lint checks and capability guard passed; the same six existing lint warnings remain.
- Historical immutable manifest: `.tmp-verify/udf-20260919T021239Z/artifacts/release.json` (the latest-receipt pointer now identifies the permission-label candidate above). Backend SHA-256 `993e31cb9da52e14dd72c51ef96f68be1a5866ce590a9a5c8e0b819369c04c44`; frontend SHA-256 `6ae3a1eb5a9badf975a5f7b60930365960a1dc4a4064374369c95e81632b2623`.
- Artifact/frozen-source/committed-source verification: 561 checks passed, 0 failed. Deployment script syntax passed; `deploy-resident-reports.sh` SHA-256 `dca3aae224e19bdc385a351e0c58bc472cc3e8a8b16fe0360c64ba04add5e2c3`.
- Read-only production preflight passed: SSH access, API/database health, nginx and all three mail services active; 15 GB free; migration 029 was latest before deployment.
- DEPLOYED: archives and script were uploaded and hash-verified under `/root/udf-releases/udf-20260919T021239Z/`. Unit `udf-release-20260919T021239Z.service` completed with `Result=success`, `ExecMainStatus=0`, `ActiveState=active`, `SubState=exited`; the `DEPLOYED` marker is present. Server journal completion time: `2026-09-19 04:16:57`. Do NOT rerun this unit or either completed deployment script.
- Backups: `/var/backups/udf/udf-20260919T021239Z/` (17 MB), directory mode 700 and database dump mode 600. Database dump plus backend/frontend/config archive readability passed before cutover. A full restore drill and off-box backup transfer were not performed.
- ONLY migration `030_resident_report_management.sql` and its ledger entry were applied transactionally; the ledger was independently rechecked. User/member/report/event/media row counts matched before/after migration while the API was stopped. Uploads compare identically with the retained previous application.
- Previous directories retained: `/opt/udf/backend.previous-udf-20260919T021239Z` and `/var/www/udf/frontend.previous-udf-20260919T021239Z`. Old content-addressed web assets were preserved for existing tabs. nginx/mail configuration and services were not changed; all remain active.
- Post-deployment verification: `node .tmp-verify/release.mjs verify https://crm.udf-party.co.za` passed 591 checks, 0 failures, including committed/frozen source and byte-identical public HTML/JS/CSS for `/`, `/crm/`, `/crm/patrols/`, and `/crm/resident-reports/`. New report endpoints require authentication (401 without credentials). API/database health passed; API restart count 0 and no error-priority journal entries since cutover.
- Browser limitation: ordinary release-qualified Resident Reports navigation redirected to `/login/`; no usable signed-in session was available. No login bypass or live report/task mutations were performed. Tabs, filters, counts, and authenticated browser runtime remain UNVERIFIED. NEXT: sign in normally with an authorized staff account, then perform read-only browser acceptance checks. Do not redeploy or rebuild on a generic continuation.

## Repository follow-up (2026-09-19)
- User requested a commit of the remaining staged Android release setup, verification receipts/scripts, and latest production deployment script/record. JavaScript, shell, and Python syntax checks, JSON validation, and staged whitespace checks passed. Signing keys, signing properties, and the private secrets directory remain ignored and untracked.
- This repository-only follow-up does not change the deployed source commit `947dc46`. No Git push, production redeployment, Android rebuild, signing-key generation, or Play publication is part of this request.

## Google Play release (new request, 2026-09-18)
- IN PROGRESS: user requested a Play release and explicitly approved a NEW upload key for the FIRST Play release of `com.udf.party`, plus an L3 deep review. That review returned no findings before the build-configuration changes; it is not a claim that later changes were reviewed.
- New upload key generated locally, encrypted, with owner-only permissions under the ignored `secrets/android/` directory. Never regenerate/overwrite it on continuation. No passwords or private key contents are recorded here. User must securely back up that directory outside this machine before publishing.
- Candidate: versionName `1.0.2`, versionCode `3`; signed APK plus AAB requested. Earlier debug APK/receipts remain preserved.
- BUILD SUCCESSFUL: `assembleRelease` + `bundleRelease` + `dumpReleaseManifest` completed (345 Gradle tasks). `verify-apk.py release` passed: APK/AAB identity, API 36 target, approved upload signer, non-debuggable, HTTPS-only network config, no broad media permissions, 168 web assets verified in both archives, production API baked, APK zip-alignment verified, R8 mapping preserved. Receipt: `deploy/release-progress/play-release-build.json`.
- Artifacts: `frontend/android/app/build/outputs/apk/release/app-release.apk` (30.05 MB), `frontend/android/app/build/outputs/bundle/release/app-release.aab` (30.66 MB), R8 mapping at `frontend/android/app/build/outputs/mapping/release/mapping.txt` (12.7 MB). Upload key and public certificate at `secrets/android/` (owner-only permissions, git-ignored).
- Upload certificate SHA-256: `0744136ba322327792b70495877024a9238e08e218418ea939577a4adb66d797`. Play App Signing requires the public certificate (`secrets/android/udf-upload-certificate.pem`) to be uploaded to Play Console; the private key stays local.
- Outstanding: Play Console upload/publication, device installation/runtime testing, and user backup of `secrets/android/` (key + properties + certificate). The connected emulator already has UDF installed with the debug signer, so it cannot install this release in place; a clean emulator or real device is required for runtime testing.
- Official Play requirements checked: https://support.google.com/googleplay/android-developer/answer/11926878 — new submissions require API 36 starting August 31, 2026. Installed SDK 36 and JDK 17 verified. Preparation script now pins API 36, AGP 8.10.1, Gradle 8.11.1 with official distribution checksum; native inset handling added for API 35+; unnecessary broad media permissions removed (existing system pickers retained).
- Current Git state supersedes the old no-repository blocker below: `/Users/why/UDF` is now a repository at `fa8d376`. This session did not initialize Git or make a commit. Preexisting release-progress edits/untracked receipts are preserved.
- No Play Console upload/publication, production deployment, device install, or runtime test has occurred in this request.

## Earlier continuation check (2026-09-19, before Resident Reports deployment)
The commit/deployment observations in this historical subsection are superseded by the completed Resident Reports deployment above. Android artifact observations remain applicable.
- Play-release preparation paused when the user redirected to Resident Reports status. The read-only Play Console browser check was canceled; account access, upload, and publication remain unverified. No rebuild, install, commit, deployment, or upload was performed.
- Current AAB and R8 mapping SHA-256 values match `play-release-build.json`. The release APK is MISSING at its recorded path, although output metadata remains. Do not claim it is currently available or overwrite the historical build receipt. Debug APK SHA-256 still matches `apk-build.json`.
- Upload key directory and files remain owner-only and git-ignored; the public upload certificate fingerprint matches the receipt. Off-machine backup is still unconfirmed. ADB currently lists no connected devices, superseding the earlier connected-emulator observation.
- Resident Reports management changes are present locally and uncommitted, including migration `030_resident_report_management.sql`, C3 assessment, acknowledgment/action evidence, ward/councillor summaries, task assignment and deadlines, filters, pagination, and filtered CSV export.
- Current-code checks PASSED: backend TypeScript, frontend TypeScript (`--incremental false`), backend workflow regression (39 passed, 0 failed; fixtures rolled back), and network-free client workflow regression (12 passed). These are not browser/device acceptance checks or proof of production deployment.
- The recorded production release covers migrations through 029. Deployment of the new Resident Reports changes/migration 030 and inclusion in a new Android artifact have not been established. Preserve existing unrelated working-tree changes; do not assume the earlier Play bundle contains them.

## Resume protocol
On a request such as "proceed", "proceed where last", or "continue", read this file first, validate the recorded state against current artifacts/tools, and resume the first unfinished item. Update this file after each meaningful result and before handing off. Do not repeat completed checks unless the code/artifact changed. Never treat an unverified check as passing or a continuation request as permission to bypass a blocker. Keep credentials, tokens, private keys, and personal data out of this folder.

## Previous CRM release identity (2026-09-18)
- Candidate: `udf-20260918T035445Z` (superseded by the Resident Reports release above).
- Historical manifest: `.tmp-verify/udf-20260918T035445Z/artifacts/release.json` (the latest-receipt pointer now refers to the Resident Reports candidate above).
- Frozen source: `.tmp-verify/udf-20260918T035445Z/source/`.
- Artifacts: `.tmp-verify/udf-20260918T035445Z/artifacts/`.
- Recorded backend SHA-256: `8e7387c98e1d2646425e777bd5dbfb382d8787155551aa570365bf52a5d932a7`.
- Recorded frontend SHA-256: `b77e9f6d45cfd6392ef5a51d3d03dcafb812442a55e6b201676c2d3c42ac2ff2`.
- VERIFIED: both artifact hashes match the receipt; every manifest-listed frozen and working source file matches its recorded hash. `/`, `/crm/`, `/crm/patrols/` HTML and their referenced JavaScript/CSS match frozen output byte-for-byte. `.tmp-verify/release.mjs verify` completed with 0 failures.
- Verification helper now supports a read-only `verify` mode; it does not modify release artifacts or application/authentication state.
- Browser verification origin: `http://localhost:3000`; existing session/tab only.

## Previous CRM / Android checklist
- [x] Establish durable release progress and resume instructions.
- [x] Region and Subcouncil viewport sheets visibly render; Subcouncil Wards heading.
- [x] Ward 9 inner scroll shows `SUBURBS — TAP TO VIEW DETAILS`.
- [x] Child drill/back, including Ward 9 to Bellville Landfill and back.
- [x] Region Close, map Reset with no modal, and Enter on map shape passed earlier.
- [x] Full-page floating-pill anomaly isolated to screenshot capture: viewport before/after and unchanged runtime geometry show full sheet/scrim. No observed viewport rendering defect; no CSS change made.
- [ ] Ward Close: two automation timeouts; interaction remains UNVERIFIED. Do not fake a click through script.
- [x] Dashboard `/crm/?release=udf-20260918T035445Z` renders in the existing session: 4 active members, other totals zero, activity empty states. Query navigation bypasses the observed cached-URL redirect loop; bare `/crm/` browser behavior remains UNVERIFIED. Three initial 401 console errors were followed by automatic refresh 200 and dashboard retry 200; no runtime exception observed. Screenshot then failed because browser view was hidden/unattached.
- [ ] Patrol `/crm/patrols/`: not visited after navigation failure; empty state/statistics/pagination/runtime checks UNVERIFIED.
- [ ] Filter reset intentionally UNVERIFIED; do not retry known unresponsive native empty-option automation.
- [x] Confirm artifact hashes, manifest-listed source consistency, and serving provenance: 0 failures.
- [x] Backend/frontend TypeScript checks passed; frontend lint passed with 6 warnings (campaign hook dependency, 3 image warnings, ID-card assets memoization, rating categories memoization).
- [x] User approved the L3 deep review; it returned no findings. No Git repository was initialized and no commit was made. User subsequently confirmed proceeding with CRM deployment.
- [x] Read-only production preflight: SSH works, `udf-api` active, local health endpoint returns HTTP 200 with `status: ok` and `db: true`, 15 GB disk available.
- [x] Production started with migrations 001–026. Applied 027–029 and their ledger entries in one transaction after taking backups; independently rechecked all three ledger entries after deployment.
- [x] Production patrol-status compatibility check: zero incompatible rows.
- [x] Uploaded and verified archives and deployment script at `/root/udf-releases/udf-20260918T035445Z/`.
- [x] Deployment service `udf-release-20260918T035445Z.service` completed: `Result=success`, `ExecMainStatus=0`, `ActiveState=active`, `SubState=exited`. Do NOT rerun it; its deployed marker and existing-path guards intentionally prevent duplicate deployment.
- [x] Protected backups confirmed at `/var/backups/udf/udf-20260918T035445Z/` (16 MB): database dump, backend, frontend, and configuration archives. Archive listing and database-dump listing passed; a full restore drill was NOT performed.
- [x] Public `/`, `/crm/`, `/crm/patrols/` return HTTP 200 over valid HTTPS and match frozen release HTML. Referenced JavaScript/CSS also match byte-for-byte from the operator machine. `node .tmp-verify/release.mjs verify https://crm.udf-party.co.za`: 572 integrity checks passed, 0 failed.
- [x] API running, database healthy, service restart count 0; no error-priority API journal entries since cutover. This does not replace authenticated browser runtime verification.
- [x] Old/current uploads compare identically after cutover. nginx, postfix, dovecot and opendkim all remain active; configuration was not changed.
- [x] Rebuild Android web assets targeting `https://crm.udf-party.co.za/api`: optimized build and 40 static pages completed; capability guard passed; same 6 lint warnings.
- [x] `cap:sync` completed; fresh web assets copied into `frontend/android/app/src/main/assets/public`; 7 Capacitor plugins synced; `android:assets` reapplied tracked branding and native hardening overrides.
- [x] Open `/Users/why/UDF/frontend/android` in Android Studio.
- [x] Build user-selected DEBUG TEST APK 1.0.1 (2): `:app:assembleDebug` completed successfully in 22 seconds (174 tasks; 38 executed, 136 up-to-date). Android Studio UI inspection again failed with Apple Event `-609`; the actual build used the same Android project and Gradle wrapper via CLI with installed Homebrew JDK 17. No toolchain upgrade or signing-key change.
- [x] Record old APK 1.0 (1) signing identity and hash in `deploy/release-progress/apk-baseline.json` using `verify-apk.py baseline`. Preserve this receipt; do not overwrite it with the new APK.
- [x] Tracked native override and generated app configuration use versionCode `2`, versionName `1.0.1`; new APK manifest and output metadata independently confirm that version and `com.udf.party` debug identity.
- Previous artifact was debug version 1.0 (1); its identity is retained in `apk-baseline.json`. `frontend/android/keystore.properties` remains absent. Production signing requires the existing release key/configuration or an explicit alternative decision.
- [x] `verify-apk.py verify` passed: APK v2 signature valid, same signer as the previous local APK, version increment confirmed, ZIP integrity passed, 168 packaged web files byte-identical to `frontend/out` and synced assets, bundled production API `https://crm.udf-party.co.za/api`, no remote Capacitor `server.url`. Exact artifact path/hash are recorded below and in `apk-build.json`.
- [ ] Device installation, upgrade on an actual device, and native/runtime smoke tests NOT performed. Same-signer compatibility is verified only against the prior local APK, not an unknown installed copy.

## Current blockers and next actions
1. Query-string ordinary navigation enabled dashboard verification without manual authentication/storage edits. Bare-route browser verification, patrol UI checks, and Ward Close remain unverified because browser availability/control is unreliable; do not turn these into false passes.
2. Requested APK update/build is COMPLETE. Provide the verified debug APK below; do not rebuild it on a generic continuation unless inputs changed or a new build is requested. Device installation/runtime testing and distribution were not performed; obtain appropriate direction before those actions.
3. CRM deployment is COMPLETE. Do not redeploy on a generic continuation request. Remaining browser verification requires restored browser control. The deployment preserved uploads and old content-addressed frontend assets, retained previous application directories, and made no nginx/mail configuration changes or reset/seed operations. APK work did not mutate production.
4. Historical repository blocker is superseded: the existing workspace repository was used, and the user-approved scoped Resident Reports commit is `947dc46`. No Git initialization or push was performed. Preserve unrelated staged/working changes; do not make further commits or repeat the canceled broad repository search without direction.
5. Earlier user selection was DEBUG TESTING APK. That completed artifact remains debug-only. The subsequent first-Play-release request and NEW upload-key approval are tracked in the Google Play section above; do not conflate the two signing identities.

## Completed Android artifact
- APK: `/Users/why/UDF/frontend/android/app/build/outputs/apk/debug/app-debug.apk`.
- Package/version: `com.udf.party`, `1.0.1`, versionCode `2`; debug testing build.
- Size: 34,939,688 bytes (approximately 34.94 MB).
- SHA-256: `487f87b446bb862e96ef11ef5ab051315bacf350b8cc4bc98cb1672d5b27a2c8`.
- Build command: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ./frontend/android/gradlew -p frontend/android --no-daemon --console=plain :app:assembleDebug` (from workspace root).
- Verification command: `python3 deploy/release-progress/verify-apk.py verify`.
- Receipts: `deploy/release-progress/apk-baseline.json` (previous APK) and `deploy/release-progress/apk-build.json` (updated APK, public certificate fingerprint, asset/API checks).
- Nonblocking build warnings: flatDir dependency metadata, SDK XML v4/v3 tooling mismatch, unchecked Capacitor Java operations. APK verifier emitted META-INF/JAR-entry signature warnings retained in its receipt; APK v2 verification passed. Toolchain versions and dependencies were not changed.
- Android Studio UI build completion is NOT claimed: Apple Event `-609` blocked UI control, so CLI Gradle performed the actual build. Language-server diagnostics were unavailable; the verification script executed successfully.

## Verification evidence
All paths below are relative to the workspace unless absolute.
- `.tmp-verify/release-resume-region-viewport-after-full.png`: correct viewport Region sheet.
- `.tmp-verify/release-resume-region-full-comparison.png`: full-page capture artifact.
- `.tmp-verify/release-resume-subcouncil4-viewport.png`: full Subcouncil sheet.
- `.tmp-verify/release-resume-ward9-suburbs-viewport.png`: Ward Suburbs heading after inner scroll.
- `.tmp-verify/release-resume-ward9-child-viewport.png`: child detail.
- `.tmp-verify/release-resume-ward9-child-back-viewport.png`: back to Ward 9.
- `.tmp-verify/release-verified-reset-map.png`: Reset; viewBox changed from `120.6 361.7 504.3 768.4` to `0.0 0.0 1000.0 1523.8` after at least one second.
- `.tmp-verify/release-verified-region-keyboard.png`: Enter opens Region.
- Earlier failed dashboard navigation had `<no console messages found>`. Latest successful query navigation showed three initial 401 errors, automatic refresh 200, and dashboard retry 200; no runtime exception observed.
- Latest screenshot failure: `NATIVE_BROWSER_VIEWPORT_UNAVAILABLE` (hidden/unattached browser view).

## Deployment safety
Target: `102.68.98.129`. Live backend: `/opt/udf/backend`; live frontend: `/var/www/udf/frontend`. Latest frontend deployment/recovery locations are recorded in the Permission label section above; unchanged backend deployment/recovery locations remain in the Resident Reports section. The earlier CRM deployment completed at server journal time `2026-09-18 07:03:46 UTC`.

Earlier release retained locations:
- Release archives, script and `DEPLOYED` marker: `/root/udf-releases/udf-20260918T035445Z/`.
- Root-protected backups: `/var/backups/udf/udf-20260918T035445Z/`.
- Previous backend: `/opt/udf/backend.previous-udf-20260918T035445Z`.
- Previous frontend: `/var/www/udf/frontend.previous-udf-20260918T035445Z`.

Backups remain on the production server; off-box backup transfer and full restore testing were not performed. Preserve uploads, database, configuration, credentials, mail services, and signing identity. A rollback requires explicit operational intent and must preserve uploads written since cutover; do not automatically restore the database or blindly rerun the deployment script.
